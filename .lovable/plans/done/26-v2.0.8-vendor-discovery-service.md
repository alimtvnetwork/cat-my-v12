# Plan 26 — v2.0.8 Vendor Discovery Service (Rank 2)

Slug: v2.0.8-vendor-discovery-service
Steps: 10
Status: pending
Created: 2026-07-14

## Context

Rank 2 of `.lovable/memory/v2/01-ranked-backlog.md:33` — Vendor Discovery Service. Plan 25 shipped the aggregator (`app/capture/vendor_discovery.py`) and admin gate (`app/core/config/settings_store.py:write_capture_device`); this plan closes the operator-facing loop: Settings UI selection from discovered devices, `E_CFG_UNKNOWN_DEVICE` on unknown IDs, `I_SEC_ADMIN_WRITE` on `settings.capture.device` proven end-to-end, and `/ops` surfacing the audit row.

Involved surfaces: `src/routes/settings.camera.tsx`, `src/components/hmi/DeviceDiscoveryPanel.tsx`, `src/lib/capture.server.ts`, `src/lib/capture.shared.ts`, `app/capture/vendor_discovery.py`, `app/core/config/settings_store.py`, `spec/21-app/66-v2-vendor-discovery.md`, `spec/21-app/67-v2-discovery-contract.md`, `spec/21-app/40-error-manage.md`, `src/routes/ops.tsx`.

Captured this turn: `.lovable/spec/commands/08-plan-lifecycle-done-folder.md` (archive to `done/`).

Applies guidelines: `.lovable/coding-guidelines/coding-guidelines.md` (verified present) plus every file under `spec/coding-guidelines/` and `spec/21-app/40-error-manage.md` (error-manage folder equivalent). All new codes must be registered there before use.

## Steps

1. Baseline read + gap audit: enumerate what Plan 25 already delivers vs Rank 2 acceptance criteria and write findings to `.lovable/memory/v2/plan26/00-baseline-gap.md`. See `./subtasks/26-v2.0.8-vendor-discovery-service/ss-01-baseline-gap.md`.
2. Spec update: extend `spec/21-app/66-v2-vendor-discovery.md` + `67-v2-discovery-contract.md` with the operator selection contract (`listDiscoveredDevices()` shape, selection request shape, error taxonomy), and register `E_CFG_UNKNOWN_DEVICE` in `spec/21-app/40-error-manage.md`. See `./subtasks/26-v2.0.8-vendor-discovery-service/ss-02-spec-update.md`.
3. Python: add `resolve_discovered_device(vendor, serial)` to `app/capture/vendor_discovery.py` that returns the descriptor or raises `E_CFG_UNKNOWN_DEVICE`; unit test in `tests/unit/test_vendor_discovery_resolve.py`.
4. Python: wire `SettingsStore.write_capture_device` to call `resolve_discovered_device` before persisting, so unknown vendor+serial pairs fail with `E_CFG_UNKNOWN_DEVICE` (audited on `settings.capture.device`) before any state mutation; extend `tests/unit/test_capture_device_admin_write.py` with the unknown-device denial path.
5. Server function: add `getDiscoveredDevices()` and `selectCaptureDevice({vendor, serial})` to `src/lib/capture.server.ts` (or new `capture-discovery.functions.ts` if module split is cleaner), gated by `requireSupabaseAuth` + admin `has_role`. Return typed error codes to the client. See `./subtasks/26-v2.0.8-vendor-discovery-service/ss-05-server-functions.md`.
6. UI: rework `src/components/hmi/DeviceDiscoveryPanel.tsx` and `src/routes/settings.camera.tsx` so the operator picks from the discovered list; disable selection while a run is active; surface `E_CFG_UNKNOWN_DEVICE`, `E_SEC_ROLE_DENIED`, `E_SEC_RATE_LIMITED` inline using existing HMI status tokens (no hardcoded colors). See `./subtasks/26-v2.0.8-vendor-discovery-service/ss-06-ui-selection.md`.
7. Ops surface: add a "Capture device" audit row view to `src/routes/ops.tsx` that queries the latest `I_SEC_ADMIN_WRITE` on subject `settings.capture.device` and renders `{prior, next}` JSON diff.
8. Integration tests: `tests/integration/test_vendor_discovery_selection_flow.py` covers happy path (discover → select → audit row present), unknown-device denial, non-admin denial, rate-limit denial. Playwright smoke `tests/e2e/discovery_selection.py` clicks through Settings → Camera → pick device and screenshots the resulting `/ops` row.
9. Blind-AI rescore of `spec/21-app/{50,51,66,67}.md` + newly touched specs via `scripts/rescore_plan26.py` (copy the Plan 25 SS-09 rubric). Threshold: mean ≥ 90, 0 blockers. Evidence to `spec/25-app-audit/latest/plan26/00-rescore.md`.
10. Closeout: bump to v2.89.0 (or higher if intermediate ships happen), append entries to `changelog.md`, `release_notes.md`, README banner (v2.0.8 shipped, Rank 3 next), mark Rank 2 delivered in `.lovable/memory/v2/01-ranked-backlog.md`, then `mv .lovable/plans/pending/26-v2.0.8-vendor-discovery-service.md .lovable/plans/done/26-v2.0.8-vendor-discovery-service.md` and flip `Status: pending` → `completed` in the same edit.

## Verification

- Python: `python3 -m pytest tests/unit/test_vendor_discovery_resolve.py tests/unit/test_capture_device_admin_write.py tests/integration/test_vendor_discovery_selection_flow.py` all green.
- E2E: `python3 tests/e2e/discovery_selection.py` produces screenshots showing Settings → Camera picker → `/ops` audit row.
- Build: TanStack build passes with new server functions typed end-to-end (no `any`).
- Audit: `python3 scripts/rescore_plan26.py` exits 0 (mean ≥ 90, 0 blockers).
- Logs: `I_SEC_ADMIN_WRITE` row visible in local SQLite `audit_events` with `subject=settings.capture.device` and non-empty `{prior, next}` JSON after a successful selection; `E_CFG_UNKNOWN_DEVICE` row visible after an unknown-device attempt.
- Version pin: README banner shows v2.89.0 (or the actual bumped version), CHANGELOG + RELEASE_NOTES have matching entries, `.lovable/prompts/288-next-task.md` created.

## Appended from prior pending tasks

- None. `.lovable/plans/pending/` was empty at plan creation; only open issue is `.lovable/issues/01-spec-21-blind-ai-readiness.md` which was already remediated in Plan 23 (audit mean 98.0/100, 0 blockers) and can be closed as part of Step 1 gap audit if still marked open.
