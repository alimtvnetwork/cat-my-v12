# Plan 15 - v2.0.1 Vendor Discovery Service + Settings UI Selection

Slug: v2.0.1-vendor-discovery
Steps: 15
Status: completed
Created: 2026-07-13
Baseline: v1.64.0
Target release: v2.0.1

## Context

`spec/21-app/62-v2-execution-order.md` locks v2.0.1 as Vendor discovery + Settings UI selection. Seam exists at `app/capture/vendor_device_io.py:52,71`; per-vendor `list_*_devices` helpers exist at `pylon_device_io.py:114`, `spinnaker_device_io.py:175`, `vimba_device_io.py:158`. Contract: `spec/21-app/66-v2-vendor-discovery.md`. Exit criteria: `spec/21-app/62-v2-execution-order.md` v2.0.1 section (6 items).

## Steps

1. Snapshot baseline: version, head, current tests green, `.lovable/memory/v2/plan15/00-baseline.md`.
2. [done] Author v2.0.1 discovery contract spec (request/response, validation, failure modes) at `spec/21-app/67-v2-discovery-contract.md`.
3. [done] Trace end-to-end write path with `path:line` citations at `.lovable/memory/v2/plan15/01-write-path-trace.md`.
4. [done] Gap list per exit criterion (E1..E6) with owning step at `.lovable/memory/v2/plan15/02-gap-list.md`.
5. [done] Implement E1 gaps: per-vendor `list_devices_fn` returns `[]` and logs structured warning when SDK absent (no raise).
6. [done] Implement E2 gaps: `SettingsStore` subject `settings.capture.device` persist path with `{vendor, serial}` payload.
7. [done] Implement E3 gaps: `I_SEC_ADMIN_WRITE` emit on every persist with actor, subject, prior, next.
8. [done] Implement E4 gaps: non-admin caller returns `E_SEC_DENIED`; unknown `{vendor, serial}` raises `E_CFG_UNKNOWN_DEVICE`.
9. [done] Implement E5 gaps: `/ops` renders the selection audit row within one refresh interval.
10. [done] Server-fn surface: `src/lib/capture.functions.ts` exposes `getDiscoveredDevices` and `selectCaptureDevice` with `requireSupabaseAuth` or equivalent admin gate.
11. UI: `src/components/hmi/DeviceDiscoveryPanel.tsx` lists descriptors, disables non-admin selection, surfaces errors via `E_*` codes; wired into Settings.
12. Tests unit: per-adapter empty-path (SDK absent); store admin-write success; store non-admin denied; unknown-selection error. Files: `tests/integration/test_vendor_discovery.py`, `tests/unit/test_settings_store_device.py`.
13. Tests e2e: extend `tests/e2e/ops_vendor_smoke.py` `discovery-pick` step to assert `/ops` row content + `E_SEC_DENIED` on non-admin path.
14. Verify: run `python3 -m pytest tests`, `python3 tests/e2e/axe_a11y.py`, capture logs proving `I_SEC_ADMIN_WRITE` fires; write `.lovable/memory/v2/plan15/40-evidence.md`.
15. Bump to v2.0.1, update README banner + CHANGELOG + RELEASE_NOTES, move this plan to `.lovable/plans/done/` with `Status: completed`.

## Verification

- Every exit criterion in `spec/21-app/62-v2-execution-order.md` v2.0.1 section maps to a Step 5-13 outcome and a green test in Step 14.
- Evidence bundle at `.lovable/memory/v2/plan15/40-evidence.md` links pytest output, e2e output, and a `/ops` screenshot showing the audit row.
- No changes outside: `app/capture/**`, `app/core/config/settings_store.py`, `src/lib/capture.functions.ts`, `src/components/hmi/DeviceDiscoveryPanel.tsx`, `src/routes/settings*`, `tests/**`.

## Hard rules

- No symptom patches (no bare try/except swallowing SDK errors; log + return `[]` is explicit).
- No hardware required; SDK-absent code path must be the default in CI.
- All new UI colors/spacing use `--hmi-*` tokens.
- No em dashes in any file authored by this plan.
