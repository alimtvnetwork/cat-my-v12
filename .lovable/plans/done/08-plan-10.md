# Plan 10 — Audit Remediation Execution Pass

Slug: plan-10
Steps: 10
Status: completed
Created: 2026-07-12

## Context

Plan 07 (`.lovable/plans/pending/07-audit-remediation.md`) enumerates milestones M0–M8 to clear the 14 Blockers surfaced by the v0.86.0 audit (`.lovable/memory/audit/99-audit-report.md`, `91-corrections.md`). Coding guidelines now exist at `.lovable/coding-guidelines/coding-guidelines.md`, unblocking M0. This plan converts Plan 07's milestones into 10 discrete, verifiable execution steps so the ship banner can flip **BLOCKED → READY**.

Referenced captures:

- `.lovable/spec/commands/01-plan-50-workflow.md` (planning conventions)
- `.lovable/spec/commands/02-ip-guardrail.md`, `03-domain-vocabulary.md`
- Coding guidelines: `.lovable/coding-guidelines/coding-guidelines.md`
- Error-management folder: none found under `spec/03-error-manage/` at file level — treat as absent; fall back to guideline §"Error & Logging".

No new user commands or issues were introduced this turn.

## Steps

1. **M1 — PascalCase rename migration.** Author `app/core/io/migrations/root/001_pascalcase_rename.sql` and extend `app/core/io/migrate.py` with a `PRAGMA table_info` self-check; flip audit row F-76 to RESOLVED. See `./subtasks/08-plan-10/ss-01-pascalcase-migration.md`.
2. **M2 — Task DB + Rules DB migrations.** Add `app/core/io/migrations/task/000_init.sql` and `.../rules/000_init.sql`; extend `migrate.py` with `--db {root|task|rules}` and `--dir` flags; wire call sites in `results_writer.py` and `worker/runner.py`. Flip F-07, F-08.
3. **M3 — Config resolver module.** Create `app/core/config/{__init__.py, resolver.py, sources.py, schema.py}` with seed → env → override precedence; unknown keys raise `DomainError(E_CONFIG_KEY_UNKNOWN)`. Flip F-17.
4. **M4 — AI stub + gate.** Create `app/ai/{__init__.py, gate.py, transport.py}`; extend `app/core/errors/codes.py` with `E_AI_DISABLED`, `E_AI_TRANSPORT`; enforce disabled-by-default. Fold in F-44/F-45 (consent + security codes). Flip F-42.
5. **DONE v0.92.0 — M5a — Home two-pane rewrite + memory namespace sync.** Rewrote `src/routes/index.tsx` as Jobs/Tasks two-pane; updated `.lovable/memory/04-design-system.md` to namespace `--ca-*`; verified `rg "\-\-hmi-" src/` returns 0. Flipped F-19, F-82.
6. **DONE v0.93.0 — M5b — RPC client + server functions.** Landed `src/lib/rpc/{client,guards,index}.ts` and `src/lib/jobs.functions.ts` (`listJobs`, `createJob`). `requireSupabaseAuth` deferred until Lovable Cloud is enabled. Flipped F-30.
7. **DONE v0.94.0 — M6 — Regenerate consistency report at v0.93.0.** Landed `spec/22-app-issues/consistency-0.93.0.md` with §3.5 + §3.7; A-14..A-19 corrected to PARTIAL. Flipped F-67, F-71.
8. **DONE v0.94.0 — M7 — Test pyramid seed.** Landed `pytest.ini` + 6 tests across unit/contract/integration, all green. Flipped F-54, F-55, F-56.
9. **DONE v0.95.0 — M8 — Rescore + regenerate audit report.** Reran `.lovable/memory/audit/10-scores.md` and regenerated `99-audit-report.md`; mean is 72.9 and Blocker count is 4, so the ship banner correctly remains BLOCKED.
10. **Governance close-out deferred.** Do not move this plan to `done/` or flip README to READY until Plan 09 clears `F-02`, `F-44`, `F-45`, `F-91`, raises mean ≥ 80, and re-verifies tests.

## Verification

- Step 1: `sqlite3 root.db "PRAGMA table_info(Task)"` returns PascalCase columns.
- Step 2: `pytest tests/unit/test_migrate.py::test_task_db_schema` green.
- Step 3: `resolve("worker.pool.size")` returns `source="seed"`; unknown key raises typed error.
- Step 4: `pytest tests/unit/test_ai_gate.py::test_disabled_by_default` green.
- Step 5: `rg "\-\-hmi-" src/` returns 0; Playwright screenshot shows two `role="region"` panes on `/`.
- Step 6: server-fn wired with `useServerFn` in Home; typecheck green.
- Step 7: `grep -c "^### 3\." spec/22-app-issues/consistency-0.87.0.md` returns 7.
- Step 8: `pytest -q` reports ≥ 3 passed.
- Step 9: `10-scores.md` and `99-audit-report.md` show current score 72.9 and Blockers = 4; README banner remains **BLOCKED**.
- Step 10: deferred until the blocker roster is empty and README can truthfully read **READY**.

## Appended from prior pending tasks

- `.lovable/plans/pending/02-control-automation-redesign.md` — superseded by Plans 05–07; recommend archival to `done/` during Step 10 close-out if user confirms.
- `.lovable/plans/pending/05-v1-implementation-kickoff.md` — M0–M8 completed at v0.76.0; archive to `done/` during Step 10 close-out.
- `.lovable/plans/pending/07-audit-remediation.md` — this plan is its execution vehicle; archive on completion.
