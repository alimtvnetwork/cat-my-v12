# Plan 24 - v2.0.6 Design Polish (token compliance sweep)

Status: in progress
Owner: unassigned
Baseline: v2.60.0 (v2.0.5 DB clarity shipped; durable audit bundle storage wired at v2.0.6.1 partial)
Inputs: `spec/24-app-ui-design-system/00-overview.md`, `.lovable/memory/index.md` (Core: `--hmi-*` tokens, no hardcoded hex)

## Goal

Sweep `src/components/**` and `src/routes/**` for hardcoded color / shadow / gradient / font values that bypass the `--hmi-*` token layer, and normalize every hit against `spec/24-app-ui-design-system/00-overview.md`.

## Non-goals

- No new visual concepts. Aesthetic stays "Modern Dark Industrial".
- No component API changes.
- No motion / animation redesign.

## Steps

1. Complete. Inventory: `rg -n "#[0-9a-fA-F]{3,8}|rgb\(|rgba\(|hsl\(|shadow-\[|bg-white|text-black|text-white|bg-black|bg-gradient" src/components src/routes` captured in `.lovable/memory/v2/plan24/00-inventory.md`.
2. Complete. Token gap map captured in `.lovable/memory/v2/plan24/10-token-map.md`.
3. Complete. No new tokens required. Existing `--hmi-*` / `ca-*` token layer covered all hits.
4. Complete. `src/components/hmi/**` returned 0 hardcoded color / shadow / gradient hits.
5. Complete, pass started. `src/components/ui/**` + `src/routes/**` hardcoded token hits remediated.
6. Contrast audit: rerun `python3 tests/e2e/axe_a11y.py`; every route must stay at 0 serious/critical.
7. Screenshot diff at 1280x1800 (Playwright headless) for `/`, `/setup`, `/run`, `/errors`, `/ops`, `/settings/license` - stored under `.lovable/memory/v2/plan24/screens/`.
8. Update `spec/24-app-ui-design-system/00-overview.md` compliance table (per-route hit count before/after).
9. Evidence bundle `.lovable/memory/v2/plan24/40-evidence.md`; archive plan; bump minor.

## Exit criteria

- `rg` inventory returns zero hits in `src/components/hmi/**` for hardcoded color / shadow tokens.
- Axe reports 0 serious/critical across audited routes.
- `spec/24-app-ui-design-system/00-overview.md` compliance table updated.
- `spec/21-app/62-v2-execution-order.md` v2.0.6 row status flipped to `shipped`.

## Risks

- Low. Presentation only, no logic changes. Screenshot diff catches accidental visual regressions.

## Progress (v2.62.0)

- Step 6 complete: axe 0 serious/critical across /,/setup,/run,/errors,/ops,/settings/license (`tests/reports/a11y-axe.json`). Fix: disabled ToolTile buttons in `src/routes/setup.tsx` now `aria-hidden` + `tabIndex=-1` so decorative opacity-40 does not fail WCAG color-contrast.
- Step 7 complete: screens under `.lovable/memory/v2/plan24/screens/{home,setup,run,errors,ops,license}.png` at 1280x1800.
- Remaining: Step 8 (update `spec/24-app-ui-design-system/00-overview.md` compliance table), Step 9 (evidence bundle + archive plan).

## Sealed

Sealed at v2.63.0. Evidence: `.lovable/memory/v2/plan24/40-evidence.md`. Compliance table: `spec/24-app-ui-design-system/00-overview.md`.
