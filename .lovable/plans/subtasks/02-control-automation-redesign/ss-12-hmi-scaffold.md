# SS-12 Scaffold `src/components/hmi/`

Created placeholder primitives + barrel:

- Titlebar, ModeHeader, ToolRibbon, Viewport, ConfigPanel, ActionBar, StatusLog, Counter, RoiOverlay
- `src/components/hmi/index.ts` re-exports all.

All primitives token-driven (no hex, no inline color). Ready for steps 13–21 to fill in behavior.
