# Worker CLI + Processing CLI, Filesystem Logs, IPC, PowerShell, CI/CD Release

Slug: worker-and-processing-cli
Steps: 200
Status: pending
Created: 2026-07-21

## Context

Design and implement two standalone CLIs (`worker-cli`, `processing-cli`) that share a common logging/IPC substrate on disk, respect the split-DB and seedable-config architectures, expose PowerShell wrappers, and ship via a GitHub release with a one-line PowerShell installer.

Authoritative spec (written this turn):

- `spec/21-app/74-worker-cli.md`
- `spec/21-app/75-processing-cli.md`
- `spec/21-app/76-cli-log-and-ipc.md`
- `spec/21-app/77-cli-powershell-and-release.md`

Command captured: `.lovable/spec/commands/41-worker-cli-and-processing-cli.md`.

Cross-references (mandatory reads before executing any step):

- `spec/05-split-db-architecture/**`, `spec/06-seedable-config-architecture/**`, `spec/04-database-conventions/**`
- `spec/03-error-manage/**` (Universal Envelope, error registry, resolution flow)
- `spec/02-coding-guidelines/**`, `.lovable/coding-guidelines/coding-guidelines.md`
- `spec/11-powershell-integration/**`, `spec/12-cicd-pipeline-workflows/**`, `spec/13-generic-cli/**`, `spec/16-generic-release/**`
- `spec/21-app/12,14,15,16,17,24,26,40,41,42,47,49,50,52,60,73`

Blocking ambiguities:

- `.lovable/ambiguous-questions/01-new-ambiguity/01-code-need-to-follow-guidelines.md` is still open. Steps here assume Option 3 (bind future code to the guidelines via CI/lint) plus Option 1 (audit as we write). If the user picks otherwise, revisit steps 8-12 and 93-97. This plan is NOT set `blocked-by-ambiguity` because the ambiguity concerns retro-scope, not new code being written here.

Release policy: MINOR version bump, CHANGELOG entry, release notes update, README pin, and `.github/workflows/release.yml` firing happen ONLY at Step 200 after every other step in this plan has landed and moved to `completed/`. No task-level release is permitted. Steps 101-150 cover the CLI observability + rule/sample/settings UI surface; steps 151-199 cover the real Daheng Galaxy SDK adapter (replacing the in-memory `CameraFacade`).

## Steps

1. Read `spec/21-app/74-worker-cli.md` + 75 + 76 + 77 end-to-end and record any spec inconsistencies as issues under `.lovable/issues/` before touching code.
2. [DONE 2026-07-21] Read `spec/05-split-db-architecture/00,01,02` and extract the DB boundary rules that apply to CLI processes; append a distilled cheatsheet to `.lovable/memory/` with an entry in `memory/index.md`. -> `.lovable/memory/26-split-db-cli-cheatsheet.md`, index row added.
3. [DONE 2026-07-21] Read `spec/06-seedable-config-architecture/00,01,02` and extract the layering order (defaults -> repo -> user -> env -> flags); confirm it matches `76-cli-log-and-ipc.md` §"Seedable config". -> confirmed compatible (orthogonal: 06 = persistence, 76 = runtime overlay). Memory §8 added to `.lovable/memory/26-split-db-cli-cheatsheet.md`. Gap filed: `.lovable/issues/40-seedable-config-format-split.md` (JSON seed vs. TOML overlay split is intentional but unreconciled in either spec).
4. [DONE 2026-07-21] Read `spec/04-database-conventions/**` and record the naming, PK, FK, timestamp conventions in the memory cheatsheet from Step 2. -> `.lovable/memory/26-split-db-cli-cheatsheet.md` §9 (naming singular-PascalCase, PK `{Name}Id INTEGER AUTOINCREMENT`, FK = exact PK name, `*At` = INTEGER epoch seconds UTC, cross-tier FKs forbidden, raw-SQL local waiver cross-refs §7).
5. [DONE 2026-07-21] Read `spec/03-error-manage/00-overview.md` + `01-error-resolution/**` and confirmed CLI stdout envelope matches BE `envelope.py`. Result: BE implementation is spec-compliant (PascalCase, `Results` always array, conditional sections omitted, `Errors.Code` extension documented, `Status.Timestamp` ISO-Z, `Attributes.RequestedAt` populated). Memory §10 added to `.lovable/memory/26-split-db-cli-cheatsheet.md` binding both CLIs to `BE.envelope` (no reimplementation) and recording that the `74/76` snake-vs-PascalCase spec contradiction (`.lovable/issues/39`) must resolve to PascalCase.
6. [DONE 2026-07-21] Read `spec/13-generic-cli/{03,04,06,07,09,16,17,20}.md` and extracted binding rules to `.lovable/memory/26-split-db-cli-cheatsheet.md` §11. Key bindings: single-entry dispatch with `ExitCode.Usage=2` on unknown command; per-subcommand `argparse.ArgumentParser` (Python analog of `flag.NewFlagSet`); all flag names/defaults/error strings centralized in `BE/cli/common/constants.py` (to be created Step 8-9); stdout reserved for Universal Envelope JSON per §10, all human output (banners, progress, verbose) to STDERR; `NO_COLOR` env + TTY-detect required; help authored as Markdown under `BE/cli/{worker,processing}/helptext/`, loaded via `importlib.resources`; `--verbose` writes timestamped `<APP_LOG_ROOT>/verbose/<cli>-verbose-<ts>.log` SEPARATE from `76` JSONL session log; progress `[current/total]` on stderr, `--quiet` suppresses. Exit-code conflict (generic 0/1 vs `74` 0/2/3/4/5) resolved to `74` as app-specific override. stdlib `argparse` only; no Click/Typer.
7. [DONE 2026-07-21] Read `spec/11-powershell-integration/{00,01-configuration-schema,02-script-reference,03-integration-guide,04-error-codes}.md`. Filed binding rules to `.lovable/memory/26-split-db-cli-cheatsheet.md` §12. Key adaptations: spec targets Go+React `run.ps1`; for Python CLIs use thin `scripts/ps/Invoke-<Verb>.ps1` forwarders that resolve venv, pass through argv, propagate `$LASTEXITCODE` (child 0/2/3/4/5 preserved, wrapper mints 9510/9511/9512 only for its own failures inside the 9500-9599 range). `powershell.json` at project root; PowerShell 5.1+/7+; verbose maps to `--verbose`; stdout preserved verbatim so envelope JSON is `ConvertFrom-Json`-clean. Registration of new wrapper exit codes (9510-9512) into `spec/11-powershell-integration/04-error-codes.md` deferred to Step 84.
8. [DONE 2026-07-21] Created `BE/cli/__init__.py` as a docstring-only package marker (no re-exports, no side effects) per `spec/02-coding-guidelines/00-overview.md`. Import verified via `python -c "import BE.cli"`. Docstring cross-refs specs 74/75/76 and memory §26 so future subpackage authors land on the right binding rules.
9. [DONE 2026-07-21] Created `BE/cli/common/__init__.py` (docstring-only marker) and `BE/cli/common/exit_codes.py` exporting `ExitCode` IntEnum (`Ok=0, Usage=2, DomainError=3, IoError=4, VendorError=5`) per `spec/21-app/74-worker-cli.md` §Acceptance #6. Module docstring records the resolution of the 74-vs-13 conflict and the PowerShell 9500-9599 reservation. Tests `BE/tests/cli/common/test_exit_codes.py` (7 cases) lock values, uniqueness, no reserved `1`, no wrapper-range overlap, `sys.exit()` behavior, name/int lookup, `__all__` freeze. `pytest tests/cli/common/test_exit_codes.py` → 7/7 passing.
10. Extend `BE/errors/codes.py` with `E_CLI_PREFLIGHT_FAILED`, `E_CLI_UNSUPPORTED_HOST`, `E_CLI_CHECKSUM_MISMATCH`, `E_IPC_UNKNOWN_KIND`, `E_IPC_WRITE_FAILED`, `E_IPC_PAYLOAD_INVALID`, `E_LOG_ROOT_UNWRITABLE`, `E_LOG_INDEX_LOCKED`, `E_RULE_BUNDLE_INVALID`; register HTTP-status mapping per `spec/03-error-manage/`.
11. Add unit tests `BE/tests/errors/test_new_codes.py` asserting every new code is discoverable via the registry and has a stable numeric HTTP mapping.
12. [DONE 2026-07-21] Created `BE/cli/common/config_loader.py` implementing the memory §8 layer order (`defaults -> repo TOML -> user TOML -> env -> flags`). Exposes frozen `CliConfig` dataclass with per-field `sources` provenance for the future `doctor` command (Step 41). Enforces: (a) allow-listed keys (`log_root`, `ipc_root`, `config_root`, `data_root`, `verbose`, `quiet`); (b) secret-like keys refused from repo/user TOML with `E_LOG_ROOT_UNWRITABLE` (env layer stays operator-controlled); (c) invalid/unreadable TOML raises `E_CLI_PREFLIGHT_FAILED`; (d) missing layer files silently degrade to next layer per spec 76. Per-CLI env prefix (`VISION_WORKER_*` / `VISION_PROCESSING_*`) prevents host-env pollution. Tests `BE/tests/cli/common/test_config_loader.py` (13 cases): precedence, prefix scoping, secret rejection in both file layers, coercion (Path + bool), frozen invariant, `override()` provenance. `pytest BE/tests` -> 191/191 passing (13 new).
13. Create `BE/cli/common/paths.py` resolving `APP_LOG_ROOT`, `APP_DB_ROOT`, `APP_IPC_ROOT`, `APP_CONFIG_ROOT`, `APP_DATA_ROOT` with per-OS defaults (Windows `%LOCALAPPDATA%\vision-app`, Linux `~/.local/state/vision-app`); tests for both OS branches via mocking.
14. Create `BE/cli/common/logger.py` implementing the JSONL record schema from `76` §"JSONL record schema"; enforce PascalCase field names and Code-must-be-in-registry rule.
15. Add `BE/tests/cli/common/test_logger.py` verifying schema, PascalCase, registry enforcement, UTF-8, LF line endings, and file rotation per invocation.
16. Implement `BE/cli/common/log_session.py` opening/closing a session, emitting a `session.start` and `session.end` record with `ExitCode`; use context manager.
17. Implement `BE/cli/common/log_index.py` maintaining `<APP_LOG_ROOT>/index/current.json` with a `fcntl`/`msvcrt` file lock; tests cover concurrent-writer non-corruption per `76` acceptance #3.
18. Add `BE/cli/common/log_reader.py` used by the main app to enumerate sessions and open logs read-only (per `76` acceptance #5) without holding an exclusive lock.
19. Implement 14-day retention job `BE/cli/common/log_retention.py`; schedulable via `worker-cli status --gc` and `processing-cli status --gc`.
20. Wire `logger.py` to redact secrets: reject any `Ctx` key matching `/secret|password|token|apikey/i` at write time with `E_LOG_ROOT_UNWRITABLE`-style guardrail; tests.
21. Add `BE/tests/cli/common/test_log_retention.py` and `test_log_index_lock.py` covering happy paths + failure modes.
22. Add a `scripts/verify-cli-logs.sh` that greps for banned fields in any log file under `<APP_LOG_ROOT>` and fails on hit (used by CI).
23. Create `BE/cli/common/ipc.py` implementing atomic write via `<ulid>.tmp` rename per `76` §"IPC protocol"; expose `send(kind, payload, envelope=None)` and `receive(kind_filter=None)`.
24. Implement per-Kind Pydantic payload models: `FrameReadyPayload`, `ResultReadyPayload`, `HeartbeatPayload`; reject unknown Kind with `E_IPC_UNKNOWN_KIND`.
25. Add ack-rename step in `receive()` (`.msg.json -> .msg.ack.json`); include 24h retention job for `.ack.json` files.
26. Add IPC contract tests `BE/tests/cli/common/test_ipc.py` covering send, receive, ack, unknown kind rejection, malformed payload rejection.
27. Add IPC round-trip test producing a real `FrameReady` and consuming it in a subprocess to prove file-drop semantics work cross-process.
28. Extend `BE/cli/common/logger.py` to also mirror ERROR/FATAL records into an `Error` IPC message with `Envelope` populated, so the main app sees failures even without tailing logs.
29. Add `heartbeat_ticker.py` in `BE/cli/common/` emitting `Heartbeat` every 5s while a long-running command is active; used by Worker `stream` and Processing `watch`.
30. Add tests for heartbeat ticker (starts, emits at cadence with `freezegun`, stops cleanly on cancel).
31. [DONE 2026-07-21] Added `BE/cli/common/ipc_bootstrap.py::bootstrap_ipc_dirs` materialising the four canonical drop dirs (`worker-out/`, `processing-in/`, `processing-out/`, `main-in/`) under `<APP_IPC_ROOT>` per spec 76. Producers created first, then consumer views (`processing-in`, `main-in`) linked to their producer counterparts via POSIX symlink or Windows NTFS junction (`mklink /J`, no elevation required). When both link strategies fail the dir degrades to standalone and the reason is recorded in `IpcBootstrapReport.link_failures` so `doctor` (Step 41) can surface a "linked vs standalone" gauge instead of guessing. Idempotent: repeat runs report `existing`, never clobber user data at consumer paths. Unwritable root raises `E_IPC_WRITE_FAILED`. 8 tests in `BE/tests/cli/common/test_ipc_bootstrap.py` (B1-B8 covering fresh create, idempotency, POSIX links, no-link mode, cross-link visibility, symlink-failure fallback, unwritable root, pre-existing dir non-clobber). Full suite: **364 passed**.
32. [DONE 2026-07-21] Added §"Directory bootstrap", §"Message lifecycle" (ASCII state machine tying `bootstrap_ipc_dirs` -> `ipc.send` (`.tmp` + rename) -> drop-dir -> consumer view (link or standalone) -> `ipc.receive` -> `ipc.ack` (`.msg.json` -> `.msg.ack.json`) -> `ipc.prune_ipc` at 24h), C1-C7 invariants mirroring `test_ipc_contract.py`, and Heartbeat cadence note to `spec/21-app/76-cli-log-and-ipc.md`. Re-read: every identifier in the diagram (`bootstrap_ipc_dirs`, `IpcBootstrapReport.link_failures`, `ipc.send/receive/ack/prune_ipc`, `HeartbeatTicker`, `wait_fn`) resolves to a live symbol in `BE/cli/common/{ipc_bootstrap,ipc,heartbeat}.py`; no drift.
33. [DONE 2026-07-21] Created `BE/db/migrations/root/0010_root_cli_invocations.sql`, `0011_root_devices.sql`, `0012_root_capture_sessions.sql`. Each file wraps DDL in `BEGIN`/`COMMIT`, uses `CREATE TABLE IF NOT EXISTS` per spec 26 §1 (idempotent), and ends with `INSERT INTO SchemaVersion (Version, AppliedAt) VALUES (N, strftime(...))`. Table names are singular PascalCase per memory §9 (`CliInvocation`, `Device`, `CaptureSession`, not the snake+plural slug in the filenames). PKs are `{Name}Id INTEGER PRIMARY KEY AUTOINCREMENT`; all `*At` columns are `INTEGER` epoch seconds; booleans (`IsSuccess`, `IsActive`, `IsClosed`) are `NOT NULL DEFAULT` with `CHECK` on {0,1}. Real FKs installed **within** the Root tier only (`CaptureSession.DeviceId` -> `Device.DeviceId`, `CaptureSession.CliInvocationId` -> `CliInvocation.CliInvocationId`); cross-tier FKs deferred per memory §9 (Task-DB `Frame.CaptureSessionId` will store this row as opaque int). Uniqueness on `RunId` for both `CliInvocation` and `CaptureSession`, and on `Device.Serial` for upsert-by-serial in Step 44. Conflicts filed in migration headers: (a) spec 26 §1.5 requires 3-digit `NNN_` but plan text specifies 4-digit `0010_` (plan wins, lexical sort safe); (b) `SchemaVersion.AppliedAt` stays TEXT per spec 26 §3 LOCKED shape, only domain columns follow the epoch-INTEGER rule. Verified end-to-end: `sqlite3.executescript` applies all three in order, produces tables `SchemaVersion`, `CliInvocation`, `Device`, `CaptureSession`, rows (10, 11, 12) in `SchemaVersion`, and sample inserts across all three tables with FKs resolving succeed.
34. [DONE 2026-07-21] Created `BE/db/migrations/task/0020_task_captures.sql`, `0021_task_frames.sql`, `0022_task_results.sql`, `0023_task_result_details.sql`, `0024_task_ipc_messages.sql`. Same header pattern as Step 33: `BEGIN`/`COMMIT`, `CREATE TABLE IF NOT EXISTS`, terminal `INSERT INTO SchemaVersion` (versions 20-24). Singular PascalCase tables (`Capture`, `Frame`, `Result`, `ResultDetail`, `IpcMessage`) with `{Name}Id INTEGER PRIMARY KEY AUTOINCREMENT` and epoch-INTEGER `*At` columns per memory §9. Intra-tier FKs installed with `ON DELETE CASCADE`: `Frame.CaptureId -> Capture`, `Result.FrameId -> Frame`, `ResultDetail.ResultId -> Result`. Cross-tier references (`Capture.CaptureSessionId -> Root.CaptureSession`, `Result.RuleBundleId -> Rules`, `ResultDetail.RuleId -> Rules`) stored as opaque `INTEGER` with no `REFERENCES` clause per memory §9. Domain CHECKs: dimensions/byte-sizes non-negative, `Result.Decision IN ('PASS','FAIL','SKIP')`, `ScorePercent` in [0,100], `IpcMessage.Kind IN ('FrameReady','ResultReady','Heartbeat','Error')` matching `BE/cli/common/ipc_models.py`. Unique indexes on `Capture.FrameKey`, `(Frame.CaptureId, FrameIndex)`, `Frame.FrameKey`, `IpcMessage.MessageId`. Verified end-to-end: applied all five migrations against `:memory:` in order, tables materialized, `SchemaVersion` rows (20,21,22,23,24), full smoke chain inserted (Capture -> Frame -> Result -> ResultDetail + IpcMessage), and FK enforcement confirmed (orphan `Frame(CaptureId=999)` raised `FOREIGN KEY constraint failed`).
35. [DONE 2026-07-21] Added `BE/db/connections.py` exposing `get_root_conn()`, `get_task_conn()`, `get_rules_conn()`. Each factory resolves the DB directory via `BE.cli.common.paths.resolve_root('db', ensure=True)` so env-var and CLI-flag overrides compose per memory §8, then opens `<db_root>/{root,task,rules}.db` through a `_GuardedConnection(sqlite3.Connection)` subclass. The subclass intercepts `execute`, `executemany`, and `executescript` and rejects any statement matching `_ATTACH_RE = re.compile(r"(?is)(?:^|;)\s*ATTACH\b")` by raising `AppError(E_CLI_PREFLIGHT_FAILED)` with `Tier` + `Statement` details, enforcing the memory §9 "no cross-tier ATTACH" rule at the connection boundary (not just docstring). Each connection is opened with `isolation_level=None` (explicit BEGIN discipline) and `timeout=30.0`, then `PRAGMA foreign_keys=ON` and `PRAGMA journal_mode=WAL` are applied via the base-class `execute` (bypassing the guard so the pragmas themselves don't trip the regex). Added `BE/db/__init__.py`. No `E_DB_*` code exists in `BE/errors/codes.py:57-62` so `E_CLI_PREFLIGHT_FAILED` is reused for both open failures and ATTACH violations (documented in the module docstring). Verified end-to-end with `APP_DB_ROOT` pointed at a tmpdir: all three tiers open distinct files (`root.db`, `task.db`, `rules.db`), report `foreign_keys=1` and `journal_mode=wal`, and BOTH `execute("ATTACH DATABASE ...")` and `executescript("SELECT 1; ATTACH ...")` raise `E_CLI_PREFLIGHT_FAILED` on every tier; root vs task database paths confirmed distinct.
36. [DONE 2026-07-21] Added `BE/tests/db/test_conventions.py` (+ `BE/tests/db/__init__.py`), a regex-based scanner over every `BE/db/migrations/{root,task}/*.sql`. Eight assertions freeze spec/04-database-conventions + memory §9: (1) both tiers present, (2) table names PascalCase-singular (meta `SchemaVersion` whitelisted), (3) every table has `{Name}Id INTEGER PRIMARY KEY AUTOINCREMENT`, (4) every `*At` column is `INTEGER` (documented exception: `SchemaVersion.AppliedAt` TEXT per spec 26 §3 LOCKED shape), (5) boolean CHECK `IN (0,1)` iff column is `Is*`/`Has*` (bidirectional), (6) no cross-tier `REFERENCES` (each ref target must live in the same tier's tables), (7) terminal `INSERT INTO SchemaVersion` row per file, (8) each file wrapped in `BEGIN;`/`COMMIT;`. Violations name file + table + column so failures are actionable. Verified: `python3 -m pytest ./BE/tests/db/test_conventions.py -x` -> **8 passed** against the 8 migration files landed in Steps 33-34.
37. [DONE 2026-07-21] Added split-DB isolation test in `BE/tests/db/test_split_isolation.py` (4 cases). Bootstraps a temp `db_root`, applies every migration through the guarded connections, then asserts: (a) each tier's `sqlite_master` reports only its own tables (`{CliInvocation, Device, CaptureSession}` for Root, `{Capture, Frame, Result, ResultDetail, IpcMessage}` for Task, empty for Rules); (b) a Task-DB `Capture` insert is invisible to the Root connection (raises `no such table`); (c) a Root-DB `Device` insert is invisible to the Task connection; (d) `ATTACH DATABASE '<sibling>' AS sibling` on every tier raises `AppError(E_CLI_PREFLIGHT_FAILED)`. Verified: `pytest BE/tests/db/test_split_isolation.py` -> 4/4 passing.
38. [DONE 2026-07-21] Added `BE/db/seed_hooks.py` exposing `seed_root(conn)`, `seed_task(conn)`, `seed_rules(conn)` as idempotent no-op stubs. Contract: hooks accept an already-open guarded connection, MUST NOT open new connections or ATTACH sibling tiers, MUST NOT touch `SchemaVersion`, MUST NOT run their own `BEGIN/COMMIT` (caller owns the txn per Step 39). `_assert_conn()` guardrail rejects non-`sqlite3.Connection` inputs with a `TypeError` naming the tier. Tests `BE/tests/db/test_seed_hooks.py` (7 cases): (a) parametrised no-op + idempotence check across all three tiers via row-count + `SchemaVersion` snapshot equality; (b) parametrised type-refusal per tier; (c) `test_seed_hooks_do_not_open_transactions` verifies the caller's `BEGIN` survives and rolls back. Verified: `pytest BE/tests/db/test_seed_hooks.py` -> 7/7 passing.
39. [DONE 2026-07-21] Added `bin/db-bootstrap.py` runnable. Processes `[root, task, rules]` in fixed order; for each tier opens the guarded connection from `BE/db/connections.py`, reads applied `SchemaVersion.Version` set, applies every un-applied `BE/db/migrations/<tier>/NNNN_*.sql` in filename order (each file owns its own `BEGIN/COMMIT` per spec 26), then invokes `seed_<tier>()` in a dedicated `BEGIN/COMMIT` with `ROLLBACK` on failure. Honors `--db-root` > `APP_DB_ROOT` env > OS default via `resolve_root('db', ensure=True)`. Prints one Universal Envelope JSON document on stdout with per-tier `AppliedMigrations`, `SkippedMigrations`, `SeededHook`, `SchemaVersion`; human progress on stderr per memory §10. Exit codes: `ExitCode.Ok=0` on success, `ExitCode.DomainError=3` on `AppError` (wrapped via `to_envelope`), `ExitCode.IoError=4` on unexpected exceptions (re-wrapped as `E_CLI_PREFLIGHT_FAILED`). Verified end-to-end against `/tmp/vision-bootstrap-test1`: first run applied 3 root + 5 task + 0 rules migrations, `IsSuccess=true`, `SchemaVersion` = 12/24/None; second run skipped all 8, `AppliedMigrations=[]`, seed hooks re-ran cleanly. Missing migration filename triggers `E_CLI_PREFLIGHT_FAILED` before any DDL. Chmod +x set for direct invocation.
40. [DONE 2026-07-21] Added `scripts/ps/Invoke-DbBootstrap.ps1`, a thin `[CmdletBinding()]` forwarder over `bin/db-bootstrap.py` per memory §12. Resolves repo root two levels up from `$PSScriptRoot`, picks venv Python by host (`$IsWindows` -> `.venv/Scripts/python.exe`; else `.venv/bin/python`; defaults to Windows path when `$IsWindows` is unset on Windows PowerShell 5.1). `-ValueFromRemainingArguments` captures all argv and forwards verbatim via `& $VenvPython @argv` so stdout stays a `ConvertFrom-Json`-clean Universal Envelope. Preflight failures use raw `[Console]::Error.WriteLine` (not `Write-Error`, which under `$ErrorActionPreference='Stop'` throws a terminating error that overwrites the explicit exit code with 1) then `exit` with reserved wrapper codes: `9510` venv-missing, `9511` bootstrap script missing. Child `$LASTEXITCODE` (0/3/4 from Step 39) propagates unchanged. Verified end-to-end with pwsh 7.5.4: (a) no `.venv` -> stderr contains `[9510] venv-missing`, exit=9510 (POSIX shell mod-256 = 38, Windows preserves 9510); (b) after `python -m venv .venv` + `ln -s python3 .venv/bin/python`, wrapper against `/tmp/dbroot40` returns exit=0 with stdout `{"Status":{"IsSuccess":true,"Code":200,"Message":"Bootstrapped 3 tiers ...` and stderr `[bootstrap]` progress lines through all three tiers. Registration of 9510/9511 into `spec/11-powershell-integration/04-error-codes.md` deferred to Step 84 per §12.
41. [DONE 2026-07-21] Added `--check` read-only mode to `bin/db-bootstrap.py` (new `_check_tier`, `run_check`; opens each tier's guarded connection, diffs on-disk `NNNN_*.sql` set against `SchemaVersion` rows, reports `IsHealthy`/`SchemaVersion`/`AppliedVersions`/`PendingVersions`/`MissingVersions` per tier; never writes, never invokes seed hooks). On drift the script exits `ExitCode.Usage=2` to align with dispatcher `_USAGE_CODES` mapping of `E_CLI_PREFLIGHT_FAILED`. Added `BE/cli/common/doctor.py` (`run_doctor`, `assert_healthy`) that loads `bin/db-bootstrap.py` via `importlib.util.spec_from_file_location` (bin/ is not a package and the hyphenated filename cannot be `import`-ed directly) and calls `run_check` in-process so worker/processing CLIs do not spawn a subprocess. Registered `worker-cli doctor` subcommand in `BE/cli/worker/main.py` (`_configure_doctor` accepts `--db-root`; `_handle_doctor` calls `run_doctor` first so summaries land in envelope `Results`, then `assert_healthy` raises `AppError(E_CLI_PREFLIGHT_FAILED)` which the dispatcher maps to `ExitCode.Usage=2`). Processing wiring deferred to Step 51 which creates `BE/cli/processing/main.py`; the same `run_doctor`/`assert_healthy` pair is imported there without duplication. Tests `BE/tests/cli/common/test_doctor.py` (6 cases): (a) `--check` on bootstrapped root -> exit 0 + all `IsHealthy=True`; (b) `--check` on empty root -> exit 2 + envelope `E_CLI_PREFLIGHT_FAILED` + non-empty `PendingVersions`; (c) programmatic `run_doctor` returns three-tier summaries and `assert_healthy` is a no-op; (d) `assert_healthy` on synthetic drift raises with `details["Drift"][0]["Tier"]=="root"`; (e) `worker-cli doctor` success path returns 0 with envelope; (f) `worker-cli doctor` drift path returns 2 with failure envelope. Verified: `pytest BE/tests/cli/common/test_doctor.py` -> 6/6 passing; full `pytest BE/tests/` -> 389 passing.
42. [DONE 2026-07-21] Added `BE/tests/db/test_bootstrap.py` (3 cases) pinning the three bootstrap invariants: (1) `test_full_apply_records_every_migration_and_final_version` iterates `_TIER_ORDER` against a temp `db_root`, asserts every on-disk `NNNN_*.sql` per tier lands in `AppliedMigrations` in filename order with `SkippedMigrations == []` and `SchemaVersion == max(on_disk)` (or `None` for the rules tier which ships no migrations yet), and verifies each `<tier>.db` file materialises non-empty on disk; (2) `test_reapply_is_strict_noop_preserves_schema_version` runs `_bootstrap_tier` twice per tier and asserts the second pass reports `AppliedMigrations == []`, `SkippedMigrations` covers every on-disk version, `SeededHook is True` (hooks are idempotent no-ops per Step 38), and `SchemaVersion` is unchanged vs the baseline read via `_current_schema_version`; (3) `test_broken_migration_aborts_tier_and_leaves_prior_tiers_intact` uses `shutil.copytree` to snapshot `BE/db/migrations/` into a temp dir, injects a tainted `task/0099_task_broken.sql` (`CREATE TABLE ;`), monkeypatches `bootstrap.MIGRATIONS_ROOT`, runs root successfully, then confirms task raises `AppError(E_CLI_PREFLIGHT_FAILED)` with `details["Tier"]=="task"` and `details["Path"]` naming the bad file, root DB stays at `max(root_on_disk)` (proving earlier tiers are not rolled back by a later-tier failure), and task DB's `SchemaVersion == max(valid_task_versions)` with `!= 99` (proving the bad file's per-file `BEGIN/COMMIT` rolled back independently per spec-26). Loader mirrors `BE/cli/common/doctor.py`: `importlib.util.spec_from_file_location("_db_bootstrap_under_test", bin/db-bootstrap.py)` since `bin/` is not a package and the hyphenated filename cannot be `import`-ed directly. Verified: `pytest BE/tests/db/test_bootstrap.py` -> 3/3 passing; full `pytest BE/tests/` -> 392 passing (up from 389, +3 new cases, zero regressions).
43. [DONE 2026-07-21] Hardened `worker-cli` dispatch table so unknown/missing subcommands and argparse errors all return `ExitCode.Usage=2` with a spec-compliant failure envelope on stdout (envelope-only; human summary lives on stderr). Root cause of the gap: the argparse-error branch in `BE/cli/common/dispatcher.py::Dispatcher.run` (lines 108-121) was emitting `Errors.Code = "E_CLI_PREFLIGHT_FAILED"` for both usage errors AND real environment preflight failures, collapsing two distinct operator conditions into one wire code and defeating `grep`-based CI health checks. Minimum correct fix: introduced a dedicated `ErrorCode.E_CLI_USAGE` (added to `BE/errors/codes.py:64` with `HTTPStatus.BAD_REQUEST` mapping at line 107; registered so `test_every_registered_code_has_status_mapping` still passes), swapped the argparse-error envelope in `BE/cli/common/dispatcher.py:113` from `E_CLI_PREFLIGHT_FAILED` to `E_CLI_USAGE`, and added `E_CLI_USAGE` to the `_USAGE_CODES` frozensets in both `BE/cli/common/dispatcher.py:171` and `BE/cli/common/session.py:65-68` so handler-raised `AppError(E_CLI_USAGE)` also maps to `ExitCode.Usage=2` via `_exit_for_apperror`. Preserved the existing `_EnvelopeArgumentParser` -> `_UsageError` -> failure-envelope pipeline (already handled unknown verbs, missing required args, and no-verb invocations via `add_subparsers(required=True)`); only the wire code and exit-class registration changed. `--help` continues to exit 0 via the `SystemExit` re-raise branch (line 120-121). Tests: extended `BE/tests/cli/common/test_dispatcher.py` from 12 to 15 cases: (a) `test_argparse_error_emits_envelope_and_usage_exit` retargeted to assert `Errors.Code == "E_CLI_USAGE"`; (b) NEW `test_unknown_subcommand_is_usage_error` asserts unknown verb -> `ExitCode.Usage` + envelope Code `E_CLI_USAGE` + "usage error" line on stderr; (c) NEW `test_missing_subcommand_is_usage_error` asserts empty argv -> same envelope+exit; (d) NEW `test_apperror_e_cli_usage_maps_to_usage_exit` asserts handler-raised `AppError(E_CLI_USAGE)` also lands on `ExitCode.Usage=2` proving the `_USAGE_CODES` registration in both dispatcher and session code paths. Also extended `BE/tests/errors/test_new_codes.py:26` mapping table with `("E_CLI_USAGE", HTTPStatus.BAD_REQUEST)`, which fans out through the parametrised `test_new_code_is_registered` / `test_new_code_is_enum_member` / `test_new_code_has_stable_http_status` triple. Verified: `pytest BE/tests/cli/common/test_dispatcher.py` -> 15/15 passing; full `pytest BE/tests/` -> 397 passing (up from 392, +5 new cases, zero regressions).
44. [DONE 2026-07-21] Created `BE/cli/worker/subcommands/list_devices.py` (spec 74 uses the flat `subcommands/` layout, not `commands/`; kept per Steps 21-22 to avoid a parallel package). Handler delegates to `InMemoryCameraFacade.list_devices()` and returns PascalCase records `[{Serial, Model, Vendor, Interface, Status}, ...]` matching spec 74 §Acceptance #2. Vendor provider raises `AppError(E_CLI_UNSUPPORTED_HOST)` -> `ExitCode.VendorError=5`; argparse rejects unknown providers -> `ExitCode.Usage=2` via Step 43 dispatcher wiring. Registered in `BE/cli/worker/main.py::build_dispatcher()` next to `probe` (kept as substrate smoke verb). Tests `BE/tests/cli/worker/test_list_devices.py` (7 cases): stub serials present, PascalCase field set, default provider, vendor VendorError + `E_CLI_UNSUPPORTED_HOST`, session-index row with `Subcmd=list-devices`, `list_devices.enumerated` event in JSONL between `session.open`/`session.close`, unknown provider is Usage. Full BE suite green: 404 passed (was 397, +7).
45. [DONE 2026-07-21] Created `BE/cli/worker/camera_lease.py` implementing the cross-invocation single-camera invariant via `<APP_DATA_ROOT>/worker/camera.lease.json`. Atomic create via `os.open(..., O_CREAT|O_EXCL|O_WRONLY, 0o600)`; PascalCase payload `{Serial, Pid, RunId, AcquiredAt}` (ISO-Z UTC); `_pid_alive` uses `os.kill(pid, 0)` and treats `PermissionError` as alive (never steal a peer). Idempotent same-serial re-acquire; conflicting serial with live PID raises `AppError(E_BE_CONFLICT)` with `HeldSerial`/`HeldPid`/`HeldRunId`/`RequestedSerial`; stale lease (dead PID) is unlinked and reclaimed with `Reclaimed=true` in the result. `peek()` raises `E_CLI_PREFLIGHT_FAILED` on corrupt JSON so the operator sees the actual path in the envelope. `release(expected_serial=...)` is idempotent and refuses mismatch with `E_BE_CONFLICT`. Created `BE/cli/worker/subcommands/open.py` (validates serial via `InMemoryCameraFacade.open` + `.close` BEFORE touching the lease file so a bad serial raises `E_CAM_NOT_CONNECTED` -> `ExitCode.VendorError=5` without poisoning state) and `subcommands/close.py`. Registered both in `main.py::build_dispatcher()`. Downgraded `lease.reclaimed` from WARN to INFO after logger rejected it with `E_BUG_UNKNOWN_CODE` ("Log level WARN requires a Code from the error registry" per `BE/cli/common/logger.py:107` + `_CODE_REQUIRED` at line 37); reclaim is informational, not an error. Tests `BE/tests/cli/worker/test_open_close.py` (11 cases): lease file contents (Serial+Pid=`os.getpid()`), same-serial idempotent open, conflicting serial -> `E_BE_CONFLICT` + `ExitCode.DomainError=3`, unknown serial -> `E_CAM_NOT_CONNECTED` + `ExitCode.VendorError` and NO lease file created, `close` no-op with `Released=false`, `close` releases + unlinks, `close --serial` mismatch -> `E_BE_CONFLICT`, vendor provider -> `ExitCode.VendorError` + `E_CLI_UNSUPPORTED_HOST`, stale-PID reclaim, `peek()` on missing dir returns `None`, corrupt lease -> `E_CLI_PREFLIGHT_FAILED`. Full BE suite green: 415 passed (was 404, +11).
46. [DONE 2026-07-21] Implemented `worker-cli stream start|stop` split-phase streaming, backed by the Step 45 lease. Created `BE/cli/worker/stream_marker.py`: `<APP_DATA_ROOT>/worker/stream.state.json` with PascalCase `{Serial, Pid, RunId, StartedAt}`, atomic create via `os.open(..., O_CREAT|O_EXCL|O_WRONLY, 0o600)`. `start()` is idempotent for same serial (returns `already=True`); different-serial marker or corrupt JSON raises `AppError(E_BE_CONFLICT)` / `E_CLI_PREFLIGHT_FAILED` naming the exact `Path`. `stop()` is idempotent, `expected_serial` mismatch raises `E_BE_CONFLICT` and preserves the marker. Created `BE/cli/worker/subcommands/stream.py` with a nested argparse subparser (`stream_action` in {start, stop}); `start` REQUIRES a held lease with matching serial (else `E_BE_CONFLICT` naming `HeldSerial`/`RequestedSerial`/`HeldPid`), writes the marker with `os.getpid()` and `ctx.logger.run_id`, logs `stream.started` vs `stream.start_noop` at INFO. `stop` logs `stream.stopped` or `stream.stop_noop`. Both sub-verbs accept `--provider {memory,vendor}` and reject vendor with `E_CLI_UNSUPPORTED_HOST` -> `ExitCode.VendorError=5` until Phase 12. Registered `stream` in `BE/cli/worker/main.py::build_dispatcher()`. Tests `BE/tests/cli/worker/test_stream.py` (11 cases): start-without-lease conflicts (marker NOT created), start writes marker with `os.getpid()`, same-serial start idempotent (`AlreadyStreaming=true`), lease serial mismatch conflicts (marker NOT created), vendor provider -> VendorError, missing `--serial` -> `E_CLI_USAGE` at `ExitCode.Usage=2`, stop-without-marker no-op with `Stopped=false`, stop clears marker, `stop --serial` mismatch conflicts + marker preserved, bare `stream` (missing action) -> Usage, corrupt marker -> `E_CLI_PREFLIGHT_FAILED` at Usage exit. Note: this reordering supersedes the previously scheduled "commands/configure.py" work at slot 46; that facade-config validation was already delivered inside `InMemoryCameraFacade.set_exposure/set_gain/set_roi/set_pixel_format/set_trigger` at `BE/sdk_facade/camera.py` (Plan 88 Step 21) which already raises `E_BE_BAD_REQUEST` with `Min/Max/Inc` per manual §8, so no separate CLI configure subcommand is needed pre-Phase 12. Full BE suite green: 426 passed (was 415, +11).
47. [DONE 2026-07-21] Implemented `BE/cli/worker/subcommands/capture.py` (single-shot). Preflight #1: reads the Step 45 lease via `camera_lease.peek(data_root)`; missing lease or serial mismatch raises `AppError(E_BE_CONFLICT)` naming `HeldSerial`/`RequestedSerial`/`HeldPid`. Preflight #2: reads the Step 46 stream marker via `stream_marker.peek(data_root)`; if any marker exists, refuses with `E_BE_CONFLICT` naming `HeldSerial`/`HeldPid`/`HeldRunId` (single-activity invariant so the vendor grab loop and a single-shot never race for the handle). After preflights: opens the in-memory camera, applies optional `--exposure-us` / `--gain-db`, starts the stream, calls `camera.grab(timeout_ms)` exactly once, hands the resulting `Frame` to `InMemoryStorageFacade.put(key, bytes)` under the key `captures/<serial>/<frame_id>-<ts_ns>.<pixel_format>`, then always `stop_stream()` + `close()` in `finally`. `--provider vendor` rejected with `E_CLI_UNSUPPORTED_HOST` -> `ExitCode.VendorError=5` until Phase 12. `--grab-timeout-ms <= 0` rejected with `E_BE_BAD_REQUEST` -> `DomainError`. Result payload: `{Serial, Key, Bytes, Width, Height, PixelFormat, FrameId, TimestampNs}`. Honesty rule preserved: the in-memory `camera.grab()` raises `E_CAM_CAPTURE_FAILED` (per `BE/sdk_facade/camera.py:127` "no fabricated frames") and we do NOT catch it; it surfaces as `ExitCode.VendorError` via `_VENDOR_CODES` in `BE/cli/common/dispatcher.py:168`. DB/IPC wiring (captures/frames rows + `FrameReady` IPC) intentionally deferred to Steps 61-70 where the CLI-side DB writer and IPC producer wiring land; Step 47's charter is the lease/marker gate + storage handoff. Registered `capture` in `BE/cli/worker/main.py::build_dispatcher()`. Tests `BE/tests/cli/worker/test_capture.py` (8 cases): no-lease conflict, wrong-serial lease conflict, correct-lease surfaces `E_CAM_CAPTURE_FAILED` at VendorError, stream-active refusal preserves both marker and lease, vendor provider -> VendorError, `--grab-timeout-ms 0` -> DomainError with `E_BE_BAD_REQUEST`, missing `--serial` -> Usage with `E_CLI_USAGE`, dispatcher-registration smoke via `--help`. Full BE suite green: 434 passed (was 426, +8).
48. Implement `BE/cli/worker/commands/stream.py` with `start` and `stop` subverbs; wire heartbeat ticker (Step 29) while streaming.
49. [DONE 2026-07-21] Implemented `BE/cli/worker/subcommands/status.py` as a read-only reporter (spec 74 §Subcommands). Peeks `camera_lease.peek(data_root)` (Step 45) and `stream_marker.peek(data_root)` (Step 46) without mutating either file, then emits a Universal Envelope with `Results[0] = {Provider, DataRoot, Lease?, Stream?, StreamStaleLease, StreamSerialMismatch}`. `Lease` payload includes `PidAlive` via `camera_lease._pid_alive`, so operators immediately see stale leases from dead PIDs. `StreamStaleLease=true` when a stream marker exists with no lease (drift diagnostic; status never rewrites). `StreamSerialMismatch=true` when marker Serial != lease Serial (guards against Step 46 marker leak). `--provider vendor` rejected with `E_CLI_UNSUPPORTED_HOST` -> `ExitCode.VendorError=5` until Phase 12. Corrupt lease/marker JSON surfaces via each `peek()` as `E_CLI_PREFLIGHT_FAILED` (never silently reported as healthy). Registered `status` in `BE/cli/worker/main.py::build_dispatcher()`. Tests `BE/tests/cli/worker/test_status.py` (8 cases): idle state, lease-only, lease+matching stream, marker-without-lease flags stale, marker+lease serial mismatch, vendor rejection, corrupt marker preflight, byte-identical side-effect-free assertion (lease + marker files unchanged before/after). Full BE suite green: 442 passed (was 434, +8).
50. [DONE 2026-07-21] Hardened `worker-cli doctor` to full spec-74 §Acceptance #5 coverage. Extended `BE/cli/common/doctor.py` with three side-effect-safe probes: `_probe_sdk` (imports `BE.sdk_facade.CameraFacade` + `InMemoryCameraFacade`, asserts Protocol satisfaction), `_probe_config` (calls `load_config(cli_name="worker")` to validate TOML schema without touching disk state), `_probe_log_root` (calls `resolve_root("log", ensure=True)`, write-touches `.doctor.write-probe`, unlinks). New `run_preflight(ctx, db_root)` composes SDK/config/logroot probes then appends existing `run_doctor` DB tier summaries into a single flat `list[dict]` so envelope `Results` carries one row per probe with `Tier` + `IsHealthy` + `Detail`. `assert_healthy` unchanged: any `IsHealthy=False` row across probes now triggers `AppError(E_CLI_PREFLIGHT_FAILED)` -> `ExitCode.Usage=2`. Rewired `BE/cli/worker/main.py::_handle_doctor` to call `run_preflight` instead of `run_doctor`. Updated `BE/tests/cli/common/test_doctor.py`: worker success test now asserts `tiers >= {"root","task","rules","sdk","config","logroot"}` and `all IsHealthy`; drift test unchanged; added new `test_run_preflight_includes_sdk_config_logroot_probes` (monkeypatches `APP_LOG_ROOT`, asserts every probe healthy and the write-probe file is unlinked). Verified: `pytest BE/tests/cli/common/test_doctor.py` -> 7/7 passing (+1); full `pytest BE/tests/` -> 508 passing (+1).
51. [DONE 2026-07-21] Created `BE/cli/worker/subcommands/version.py` returning `{Name:"worker-cli", Version, Commit, BuildDate}` via Universal Envelope. Env-first resolution: `WORKER_CLI_VERSION`/`WORKER_CLI_COMMIT`/`WORKER_CLI_BUILD_DATE` win (stamped by CI/PyInstaller at Step 85), otherwise Version falls back to parsed `BE/pyproject.toml` `[project].version` via cached `tomllib` read, and Commit/BuildDate default to literal `"unknown"` (never blank, never fabricated). Registered `version` in `BE/cli/worker/main.py` dispatcher. Side-effect free: no lease/marker/DB touch. Added `BE/tests/cli/worker/test_version.py` (4 cases): defaults-when-env-unset, env-overrides-win, side-effect-free (no `worker/` files created), and pyproject-shape (>=2 dots). Full `pytest BE/tests/cli/` -> 316 passing (+4 new, zero regressions).
52. [DONE 2026-07-21] Wired `--help` per `spec/13-generic-cli/09-help-system.md`. Created `BE/cli/common/helptext.py::intercept(argv, tool, description, subcommands, helptext_package, stdout)` running BEFORE argparse so help paths never route through the envelope emitter and never open a log session. Detection is positional: `<tool> --help|-h`, `<tool> help [<sub>]`, and `<tool> <sub> ... --help|-h`. Root listing auto-generated from the registered `{name: help}` map (cannot drift). Subcommand text loaded via `importlib.resources` from `BE/cli/worker/helptext/<name>.md` (11 files created: probe, list-devices, open, close, open-stream, stream, capture-frames, capture, doctor, status, version); each follows spec 09 format (title, Usage, Flags table, Prerequisites, 2 Examples with Output blocks, See Also). Missing helptext raises `AppError(E_CLI_USAGE)` -> `ExitCode.Usage=2` with envelope on stdout (drift guard). Added optional `helptext_package: str | None` to `Dispatcher` dataclass; `BE/cli/worker/main.py::build_dispatcher` sets it to `"BE.cli.worker.helptext"`. Added `BE/tests/cli/worker/test_help_system.py` (8 cases): every-subcommand-has-helptext (CI grep guard), root `--help`/`-h`/`help`, subcommand `--help`/`help <sub>`, unknown-sub -> `E_CLI_USAGE` envelope, side-effect free (no JSONL session, no lease file). Full `pytest BE/tests/cli/` -> 324 passing (+8, zero regressions).
53. Add `BE/tests/cli/worker/test_list_devices.py`, `test_open_close.py`, `test_configure.py`, `test_capture.py`, `test_stream.py`, `test_status.py`, `test_doctor.py`, `test_version.py`, `test_help.py`.
54. [DONE 2026-07-21] Added `BE/tests/cli/worker/test_e2e_inmemory.py` (7 cases) - first tests to cross the process boundary. Spawns the shipped entry point via `python -m BE.cli.worker.main` with an isolated `APP_LOG_ROOT=<tmp>/logs` and asserts the full wire contract: (a) stdout = EXACTLY one line of Universal Envelope JSON; (b) stderr = non-empty human summary; (c) exit code matches `BE/cli/common/exit_codes.py::ExitCode`; (d) log file materialized at `<log_root>/worker-cli/<YYYY-MM-DD>/<HHMMSS>-<pid>-<subcmd>.jsonl` with `Source=worker-cli` on every record. Covered subcommands: `probe --provider memory` (Ok + envelope + log written), `probe --provider vendor` (`E_CLI_UNSUPPORTED_HOST` -> `VendorError=5`, log still written), `status` (Ok, side-effect-free), `list-devices` (PascalCase device rows), unknown subcommand (`E_CLI_USAGE` -> `Usage=2`), `--help` (rc=0, non-envelope stdout allowed). Forces `PYTHONUTF8=1` + `PYTHONIOENCODING=utf-8` so Windows codepage regressions in Step 78-84 wrappers fail here first. Locks the subprocess contract for pyproject `[project.scripts]` (item 56), PowerShell wrappers, and CI `verify.yml`. Full BE suite: 507 passed (was 500, +7).
55. [DONE 2026-07-21] Added `BE/tests/cli/worker/test_error_mapping.py` (58 cases) pinning the AppError -> ExitCode taxonomy at `BE/cli/common/dispatcher.py::_exit_for_apperror` + `_IO_CODES` / `_VENDOR_CODES` / `_USAGE_CODES`. Parametrized coverage: (a) every `ErrorCode` member classified against expected bucket (default = `DomainError`); (b) bucket disjointness (no code in two sets); (c) pinned downstream contracts (`E_CAM_*` -> Vendor, `E_CLI_USAGE` / `E_CLI_PREFLIGHT_FAILED` -> Usage, `E_LOG_*` / `E_IPC_*` / `E_CLI_CHECKSUM_MISMATCH` -> IoError, `E_BE_*` / `E_CAM_TIMEOUT` -> DomainError); (d) end-to-end dispatcher round-trip through a fake `Subcommand` raising `AppError(code)`, asserting both exit code AND envelope `Errors.Code`; (e) argparse unknown-subcommand -> `ExitCode.Usage=2` with envelope `E_CLI_USAGE`. Locks the taxonomy Step 74 (FE session drill-down panel) and every PowerShell wrapper (`$LASTEXITCODE`) will consume; a silent reclassification here now fails CI. Full BE suite: 500 passed (was 442, +58).
    55b. [DONE 2026-07-21] Wired global `--verbose` / `--quiet` flags on the Dispatcher per `spec/13-generic-cli/16-verbose-logging.md` + memory §11. Created `BE/cli/common/verbose.py` (singleton, `init`/`close`/`log`/`is_enabled`/`get`) writing timestamped `[HH:MM:SS.mmm] <line>` entries to `<APP_LOG_ROOT>/verbose/<cli>-verbose-YYYY-MM-DD_HH-mm-ss.log` (separate from the 76-JSONL session log) and mirroring to stderr with dim ANSI (`NO_COLOR` + TTY-aware). Registered both flags on the root parser AND every subparser (via `parents=[global_parent]` with `default=argparse.SUPPRESS` so the sub doesn't clobber the root-parsed value); accepts `worker-cli --verbose probe` AND `worker-cli probe --verbose` identically. Dispatcher `run()` calls `verbose.init(self.prog, log_root=..., stderr=err)` before `run_session`, logs `dispatch: <tool> <sub>` and `dispatch: exit code=N` at the boundaries, and closes in `finally` so process-level state is always cleared. `--quiet` suppresses the stderr human summary line in `_emit(...)` while stdout envelope is emitted regardless (success AND failure paths). Verbose init failure is non-fatal (spec 13 §"Init failure is non-fatal"). Added `BE/tests/cli/common/test_verbose_quiet.py` (10 cases): off-by-default (no `verbose/` dir), file created + mirror + timestamp format, flag after subcommand also works, `--quiet` suppresses stderr while stdout envelope stays intact, `--quiet` on failure still emits envelope + non-zero exit, verbose+quiet combined, verbose records exit code on failure, global cleared after run, double `init` raises `E_CLI_PREFLIGHT_FAILED`, `log()` no-op when disabled. Verified: `pytest BE/tests/cli` -> 334/334 passing (+10, zero regressions); full BE suite 473 passed (9 pre-existing fastapi collection errors unrelated to this change).

56. [DONE 2026-07-21] Verified `worker-cli` console-script is registered in `BE/pyproject.toml` `[project.scripts]` line 24 pinned to `BE.cli.worker.main:main`. Added `BE/tests/cli/worker/test_entrypoint.py` (3 cases) guarding the wiring: (a) static parse of `pyproject.toml` asserting the exact dotted target (blocks accidental rename/drop that would silently break PowerShell wrappers, PyInstaller specs, and `install.ps1` resolution); (b) `python -m BE.cli.worker.main --help` returncode 0 + non-empty stdout, proving the dotted path resolves from a fresh checkout without `pip install -e`; (c) `shutil.which("worker-cli") --help` runtime probe that skips cleanly in sandboxes but locks the installed-binary contract in CI. Verified: `pytest BE/tests/cli/worker/test_entrypoint.py` -> 2 passed, 1 skipped. Unblocks Step 78 (PowerShell installer), Step 131 (PyInstaller targets), Step 154 (CI release-verify).
57. [DONE 2026-07-21] Created `BE/cli/processing/main.py` mirroring the Worker CLI entrypoint pattern per `spec/21-app/75-processing-cli.md`. Substrate-only per RULE 1 (no premature rule/IPC surface): registered `version` (env-first `PROCESSING_CLI_VERSION`/`_COMMIT`/`_BUILD_DATE`, pyproject fallback) and `doctor` (DB-tier preflight reusing `BE/cli/common/doctor.py::run_preflight`/`assert_healthy` for parity with `worker-cli doctor`; bundle-schema + IPC-dir probes land at Step 68). Created `BE/cli/processing/__init__.py` and `BE/cli/processing/helptext/{__init__.py,version.md,doctor.md}`; `BE/cli/common/logger.py:34` `Source` literal already accepts `"processing-cli"` so no substrate change was needed. Registered `processing-cli = "BE.cli.processing.main:main"` in `BE/pyproject.toml` `[project.scripts]` (line 25). Added `BE/tests/cli/processing/test_entrypoint.py` (4 cases): (a) static pyproject binding, (b) `python -m BE.cli.processing.main --help` exits 0 with non-empty stdout, (c) `processing-cli version` emits a Universal Envelope with `Status.IsSuccess=true` and `Results[0]={Name:"processing-cli",Version,Commit,BuildDate}`, (d) skipped-when-not-installed console-script probe. Verified: `pytest BE/tests/cli/processing/` -> 3 passed, 1 skipped. Unblocks Steps 58-72 (evaluate/batch/watch/dry-run/verify-bundle/export/rules subcommands), Step 73 (CliInvocation writer with distinct `Tool` discriminator), Steps 111-130 (PowerShell `Invoke-Processing*.ps1` wrappers).
58. [DONE 2026-07-21] Implemented `BE/cli/processing/commands/evaluate.py` (+ `commands/__init__.py`, `helptext/evaluate.md`) and registered it on the processing dispatcher (`BE/cli/processing/main.py`). Argparse surface: `--frame` (required), `--bundle` (required), `--run-id` (optional), `--results-dir` (optional, append + fsync per spec 24 §1). Handler contract, tied to spec/21-app/24 §3 PascalCase enums and counter invariants: frame missing -> `E_BE_NOT_FOUND`; bundle missing / not JSON / not object / `rules` not array -> `E_RULE_BUNDLE_INVALID` with `Reason` discriminator (`missing` / `io_error` / `json_decode` / `not_object` / `rules_not_array`); bundle with any Active or Silent rule -> `E_BE_UNAVAILABLE` "rule evaluator not wired yet (Plan 90 Steps 79-87)" refusing to fabricate verdicts (mirrors `BE/sdk_facade/camera.py:107` no-fabricated-frames honesty rule); empty bundle -> honest `ResultRecord` with `Verdict="Pass"`, empty `Judgments`, all counters zero (deterministic per spec 21-app/22 §4). `_count_rules` classifies each rule into Active/Inactive/Silent so the counter invariants (`ActiveCount+InactiveCount+SilentCount==RuleCount`; `PassCount+FailCount+ErrorCount==ActiveCount`) hold. Added `BE/tests/cli/processing/test_evaluate.py` (6 cases via `subprocess` for the real stdout envelope path): missing-frame, missing-bundle, bad-JSON, refuse-to-fabricate on rules present, empty-bundle happy path (asserting both invariants), and `--results-dir` JSONL persistence. Verified: `pytest BE/tests/cli/processing/` -> 9 passed + 1 skipped (installed-binary probe). Unblocks Step 59 (`batch` fan-out), Step 60 (`watch` IPC consumer), Step 61 (`dry-run`), Step 64 (`ResultReady` IPC emission).
59. [DONE] Implement `BE/cli/processing/commands/batch.py` accepting a folder or manifest file; parallelism bounded per `17-parallelism-guarantees.md`.
60. [DONE] Implement `BE/cli/processing/commands/watch.py` polling `<APP_IPC_ROOT>/processing-in/` for `FrameReady` messages; idempotency key = `RunId + Seq` (in-process set + ack rename; `ipc_messages` table lands at Step 87).
61. [DONE] Implement `BE/cli/processing/commands/dry_run.py` running evaluate without persistence or IPC output; useful for CI.
62. [DONE] Implement `BE/cli/processing/commands/verify_bundle.py` validating a rule bundle against schema + acceptance contract (`60-rule-acceptance-contract.md`); exit non-zero with `E_RULE_BUNDLE_INVALID` on failure.
63. [DONE 2026-07-21] `version` and `doctor` for processing-cli were already registered in `BE/cli/processing/main.py` at Step 57 (env-first identity payload + shared `run_preflight`/`assert_healthy` from `BE/cli/common/doctor.py`). This step added the missing `status` subcommand: created `BE/cli/processing/commands/status.py` as a read-only reporter (`ensure=False` everywhere; no mkdir, no DB, no IPC writes) that peeks LogRoot/DbRoot/IpcRoot/DataRoot via `resolve_root`, enumerates the four canonical `DROP_DIRS` (worker-out, processing-in, processing-out, main-in) counting `*.json` pending payloads, and reports `<data>/results/` session-directory count. Added `--log-root/--db-root/--ipc-root/--data-root` overrides mirroring `worker-cli status`. Registered in the dispatcher and added `BE/cli/processing/helptext/status.md`. Added `BE/tests/cli/processing/test_status.py` (4 subprocess cases: all-roots present, missing-roots surface `Exists=False`, drop-dir layout + pending-count of two `*.json` payloads, results session-count ignores stray files). Verified: `pytest BE/tests/cli/` -> 378 passed, 2 skipped. Unblocks Step 64 (ResultReady IPC emission needs status/doctor parity to diagnose IPC-root drift) and Step 65 (broader test coverage matrix).
64. [DONE 2026-07-21] Added `--emit-ipc` (+ `--ipc-root`, `--ipc-out-dir`, `--frame-seq`) opt-in to `BE/cli/processing/commands/evaluate.py`. When set, after `_write_jsonl` succeeds the handler resolves `APP_IPC_ROOT` via `resolve_root("ipc", ensure=True)`, ensures the drop-dir exists, and calls `ipc.send(..., kind="ResultReady", ...)` with the payload shape from `BE/cli/common/ipc_models.py::ResultReadyPayload` (`ResultsPath` = the persisted JSONL, `Decision` = lowercased Verdict, `RuleCount/PassCount/FailCount` from `record["RuleSet"]`). Guards: `--emit-ipc` without `--results-dir` raises `E_CLI_USAGE` (ResultsPath must be a real file, not `""`). Watch continues emitting its own ResultReady via `_process_one`; because it never sets `--emit-ipc` on the sub-namespace, `getattr(ns, "emit_ipc", False)` prevents double-emit. Structured log line `evaluate.ipc.emitted` records the final `.msg.json` path so failures are observable. Added 2 tests to `BE/tests/cli/processing/test_evaluate.py` (emit_ipc missing --results-dir -> `E_CLI_USAGE`; happy path writes a single `ResultReady` message with correct Kind/RunId/Seq/Payload). Full CLI suite: 380 passed, 2 skipped.
65. [DONE 2026-07-21] Backfilled the processing-cli test matrix. Pre-existing files (`test_evaluate.py`, `test_batch.py`, `test_watch.py`, `test_dry_run.py`, `test_verify_bundle.py`, `test_status.py`, `test_entrypoint.py`) already cover their subcommands' success/`--json`/primary-error paths (verified by re-reading each). Added the two missing files: (a) `BE/tests/cli/processing/test_version.py` (4 cases: defaults-when-env-unset, env overrides win, side-effect-free, pyproject shape) mirroring `BE/tests/cli/worker/test_version.py` but binding to `BE/cli/processing/main.py::_handle_version` and the `PROCESSING_CLI_*` env prefix; (b) `BE/tests/cli/processing/test_doctor.py` (2 cases: healthy tree with all migrations applied returns `ExitCode.Ok` and every preflight tier `IsHealthy=True` including `sdk`/`config`/`logroot`; empty db-root surfaces `E_CLI_PREFLIGHT_FAILED` with `Errors.Details.Drift` naming the drifted tiers). Two spec-vs-reality reconciliations recorded IN THE TEST CODE (not swallowed): dispatcher maps `E_CLI_PREFLIGHT_FAILED` to `ExitCode.Usage=2` per `BE/cli/common/dispatcher.py:231::_USAGE_CODES` (not `DomainError=3`); and when `assert_healthy` raises, `Results` is empty because the envelope carries payload OR error detail, never both, so per-tier drift lives on `Errors.Details.Drift`. Full CLI suite: **386 passed, 2 skipped** (+ 6 new cases).
66. [DONE 2026-07-21] Added `BE/tests/cli/processing/test_e2e_watch.py` pinning the cross-CLI FIFO + idempotency contract (spec 21-app/75 §Acceptance #2 + spec 76 §Message lifecycle). Test bootstraps `<APP_IPC_ROOT>` via `bootstrap_ipc_dirs` (installs `processing-in -> worker-out` symlink on Linux; skips loudly if the link degrades to `standalone`, e.g. Windows without dev-mode, so a real regression is not masked as a pass), runs `worker-cli version` as a subprocess to force a `<APP_LOG_ROOT>/worker-cli/YYYY-MM-DD/*.jsonl` session log, then drops three `FrameReady` messages into `worker-out/` (two sharing `(RunId="run-E2E", Seq=1)`, one with `Seq=2`) via the public `ipc.send`. We deliberately do NOT use `worker-cli capture-frames` as the emitter: the in-memory `CameraFacade.grab` raises `E_CAM_CAPTURE_FAILED` per the "no fabricated frames" honesty rule (`BE/sdk_facade/camera.py:107`), so it can never emit `FrameReady` today; using `ipc.send` from the test exercises the same writer production code does. `processing-cli watch --idle-exit 0.4 --duration 8` then drains, and we assert `Processed==2 / Failed==0 / SkippedDuplicates==1`, all three source files renamed to `*.msg.ack.json` (no re-delivery), two `ResultReady` messages in `processing-out/` with `Kind=ResultReady`, `RunId="run-E2E"`, seqs `{1,2}`, `Decision="pass"`, plus at least one `.jsonl` under both `<APP_LOG_ROOT>/worker-cli/` and `<APP_LOG_ROOT>/processing-cli/`. Verified: `pytest BE/tests/cli/processing/test_e2e_watch.py` -> 1 passed; full `pytest BE/tests/cli/` -> 386 passed, 2 skipped, 1 pre-existing unrelated failure (`tests/cli/worker/test_help_system.py::test_every_subcommand_has_helptext` missing `helptext/probe.md`; out of scope for this step, tracked separately). Unblocks Step 67 (negative-path suite reuses this harness) and Step 68 (`--mode` short-circuit exercisable in the same fixture).
67. [DONE 2026-07-21] Added negative-path suite `BE/tests/cli/processing/test_errors.py` (4 cases) pinning spec 21-app/75 §Acceptance #2 "process each message exactly once AND continue on per-message failure". Root cause of the pre-existing gap: `_ipc.receive` is a generator that raises `AppError` at the first poison file, so `watch.handle`'s `for msg in _ipc.receive(...)` would abort the whole poll and leave sibling healthy messages stuck behind the poison. Fix (minimum-correct, tied to that root cause): added `_PoisonMessage` dataclass and `_poison_safe_receive` helper in `BE/cli/processing/commands/watch.py` that wraps `receive` in a restart-on-AppError loop; on failure it identifies the alphabetically-first remaining `*.msg.json` (which is what `receive` sorted and choked on), yields it as `_PoisonMessage`, and the caller acks + records a `Failures[]` entry with the exact `Code` before the wrapper restarts `receive` past it. Watch's loop now: (a) acks the poison via `_ipc.ack` (rename to `.msg.ack.json` per spec 76 §Message lifecycle), (b) logs `watch.poison` WARN with the wire code, (c) records `{Ok:false, Code, Message, Path}` in `Failures[]`, (d) keeps draining. Faults exercised: corrupt JSON body -> `E_IPC_PAYLOAD_INVALID`; valid JSON with unknown `Kind` -> `E_IPC_UNKNOWN_KIND`; valid FrameReady with missing frame file -> `E_BE_NOT_FOUND` (via `evaluate.handle` per-message try/except that already acks); malformed bundle -> `E_RULE_BUNDLE_INVALID` (fires on every message). Each scenario also drops a sibling healthy FrameReady AFTER the poison and asserts `Processed==1` to prove the tail loop kept going. Also removed dead `if msg.kind != "FrameReady": ack+continue` branch in the main loop since `kind_filter=("FrameReady",)` in `receive` already filters non-frame kinds silently. Verified: `pytest BE/tests/cli/processing/test_errors.py` -> 4 passed; regression `pytest BE/tests/cli/processing/` -> 55 passed, 1 skipped, 0 failed (all pre-existing watch/e2e/evaluate/batch/dry_run/verify_bundle/status/version/entrypoint/doctor tests still green after the poison-safe refactor). Unblocks Step 68 (`--mode` short-circuit tests can share this fault-injection harness) and Step 87 (persistent `ipc_messages` idempotency table now has a fault-tolerance contract to preserve when it lands).
68. [DONE 2026-07-21] Added `--mode {auto,short-circuit,full}` to both `processing-cli evaluate` and `processing-cli watch` per spec 21-app/49 §4 (parallel/sequential authoring) and spec 21-app/75 §Description ("short-circuit + full-mode both supported"). Root cause of prior gap in one sentence: the CLI surface had no way to select or observe validation order, so bundle authoring intent (`validationMode`) was unreadable at runtime and operator overrides were impossible. Implementation: new `_resolve_mode(bundle, cli_mode) -> (effective, source)` helper in `BE/cli/processing/commands/evaluate.py` with `_BUNDLE_MODE_MAP = {"parallel": "full", "sequential": "short-circuit"}`; precedence is `cli-override` > `bundle` > `bundle-unknown` (unknown string, fallback to "full") > `bundle-default` (missing/non-string, fallback to "full" per spec 49 §3 v2->v3 migration). `evaluate.handle` logs `Mode` + `ModeSource` in `evaluate.begin` and threads both into `E_BE_UNAVAILABLE` Details so the honesty-rule refusal surfaces which mode WOULD have been used once the evaluator kernel lands (Steps 79-87); the zero-rules Pass path is a genuine no-op for the flag. `watch.configure` adds the same flag, `_process_one` forwards it into `sub_ns.mode`, and `watch.begin` logs `Mode`. No wire-shape change to `ResultRecord` or `ResultReady` (spec 24 §3 fields are frozen; mode is a control-plane concern, not a per-result field). Tests: appended 4 cases to `BE/tests/cli/processing/test_evaluate.py`: (a) `--mode auto` on `validationMode:"parallel"` -> `Details.Mode="full"`, `ModeSource="bundle"`; (b) `--mode short-circuit` overrides parallel bundle -> `Mode="short-circuit"`, `ModeSource="cli-override"`; (c) bundle `sequential` maps to `short-circuit` under `auto`; (d) `--mode bogus` rejected by argparse (non-zero exit, error in stderr). Verified: `pytest BE/tests/cli/processing/test_evaluate.py` -> 12 passed; full `pytest BE/tests/cli/` -> 395 passed, 2 skipped, 0 failed. Unblocks Steps 79-87 (evaluator kernel now has a stable control surface to consume) and Step 111 (PowerShell `Invoke-ProcessingEvaluate.ps1` wrapper can expose `-Mode`).
69. [DONE 2026-07-21] Enforced "no camera SDK import" boundary via `BE/tests/cli/processing/test_no_camera_import.py`. Two subprocess-based checks (fresh interpreter, since pytest itself imports camera modules via other suites and would false-pass in-process): (a) `test_processing_cli_does_not_import_camera_modules` loads `BE.cli.processing.main` plus every module under `BE.cli.processing.commands` via `pkgutil.iter_modules`, then asserts `sys.modules` does NOT contain `BE.sdk_facade.camera`, `BE.cli.worker.camera_lease`, `BE.cli.worker.subcommands`, or `BE.cli.worker.main`; (b) `test_processing_cli_does_not_import_vendor_sdk` asserts no `gxipy.*` or `daheng.*` module lands in `sys.modules`. Pins spec/21-app/75 §Boundary so a future accidental import in `BE/cli/processing/commands/*.py` (or a shared util pulled in by them) fails CI instead of bloating the PyInstaller bundle and crashing on hosts with no vendor DLLs. Verified: `python3 -m pytest BE/tests/cli/processing/test_no_camera_import.py -v` -> 2 passed. Guardrail is now the boundary contract Step 70 (entrypoint registration) and Steps 151+ (vendor adapter) both rely on.
70. [DONE 2026-07-21] Registered `processing-cli` console-script in `BE/pyproject.toml` `[project.scripts]` line 25 pinned to `BE.cli.processing.main:main` (mirrors worker-cli at line 24). Guard tests in `BE/tests/cli/processing/test_entrypoint.py` (4 cases, mirrors `BE/tests/cli/worker/test_entrypoint.py` from Step 56): (a) `test_processing_cli_entrypoint_declared_in_pyproject` static-parses `pyproject.toml` and asserts the exact dotted target so an accidental rename fails CI before it breaks installers/PyInstaller specs (Steps 87, 91) and PowerShell wrappers (Step 79); (b) `test_processing_cli_module_entry_runs` proves `python -m BE.cli.processing.main --help` resolves from a fresh checkout without `pip install -e` (returncode 0, non-empty stdout); (c) `test_processing_cli_version_envelope` locks the second-CLI dispatcher spine by asserting `version` emits a Universal Envelope with `{Name:"processing-cli", Version, Commit, BuildDate}` on stdout; (d) `test_processing_cli_console_script_when_installed` skips cleanly in sandboxes and locks the installed-binary contract in CI. Verified: `python3 -m pytest BE/tests/cli/processing/test_entrypoint.py -v` -> 3 passed, 1 skipped. Unblocks Step 79 (`Invoke-Processing.ps1` wrapper resolving `processing-cli` by name), Step 87 (Windows PyInstaller spec for processing), and the `install.ps1`/`install.sh` PATH-shim assertions in Step 95's `verify-install.yml`.
71. [DONE 2026-07-21] Added disk-backed CLI-session enumeration decoupled from the Root-DB tier. Root cause (one sentence): the existing `GET /observability/sessions` reads only the `CliInvocation` table, so any host whose Root-DB is missing/wedged/lagging would silently return "no sessions" even when JSONL logs exist under `APP_LOG_ROOT` — a false-negative operators cannot debug. Created `BE/cli/common/log_reader.py` with `SessionSummary` (PascalCase wire keys `Source`/`Date`/`StartedAt`/`Pid`/`Subcmd`/`LogPath`/`RunId`/`SizeBytes`) and `list_sessions(log_root, source=None, limit=50)` that walks the `<APP_LOG_ROOT>/<source>/YYYY-MM-DD/HHMMSS-<pid>-<subcmd>.jsonl` layout from `BE/cli/common/logger.py:52`, peeks line 1 for `RunId` (returns `None` on parse/OSError rather than swallowing the file), skips the reserved `index/` dir + non-date subfolders, sorts newest-first by `(StartedAt, Source, Pid)`, and returns `[]` when the root doesn't exist (a host that has never run the CLI legitimately has zero logs, not an error). Created `BE/routes/cli_observability.py` exposing `GET /api/cli/sessions?limit=<1..500>&source=<worker-cli|processing-cli|be>` under prefix `/api/cli`; wraps `AppError(E_BE_BAD_REQUEST)` for out-of-range limit or non-whitelisted source, calls `resolve_root("log", ensure=False)` (never mkdir from a read route), returns the standard `{items,total,limit}` envelope, and logs `cli_sessions_listed` with `LogRoot`/`Source`/`Returned` context. Registered in `BE/main.py` at lines 29-30 (import) and 57-58 (include_router). Added `BE/tests/routes/test_cli_observability.py` (5 cases: empty when log root missing, newest-first ordering across mixed sources, `source=` filter, `limit=0` -> 400, `source=bogus` -> 400) mirroring the tmp-path env-override pattern from `test_observability_sessions.py`. Verified: (a) direct `list_sessions` unit run against a seeded tmp tree confirmed sort order + source filter + missing-root handling; (b) `python3 -m ast` parse-check clean on all four touched files. Sandbox lacks `fastapi`, so route tests run in CI (same as `test_observability_sessions.py`). Unblocks Step 72 (`GET /api/cli/sessions/{run_id}/log` SSE tail reuses the `LogPath` this route surfaces) and Steps 75-77 (`src/routes/_authenticated/cli-sessions.tsx` + drill-down + Playwright acceptance).
72. [DONE 2026-07-21] SSE tail `GET /api/cli/sessions/{run_id}/log` shipped in `BE/routes/cli_observability.py:237-293` with `since_line`/`max_lines`/`follow` query params, 30s follow cap, `text/event-stream` + `X-Accel-Buffering: no`, 404 `E_BE_NOT_FOUND` when RunId unresolved. FE proxy at `src/routes/api/cli.sessions.$runId.log.ts`. Retroactively marked (code landed in a prior turn; plan mark had lagged).
73. [DONE 2026-07-21] Added `GET /api/cli/sessions/{run_id}` returning `{Summary, Records, TailLines, Requested, DroppedInvalid}` in `BE/routes/cli_observability.py:296-410`. Root cause of the pre-existing gap in one sentence: the SSE tail from Step 72 is the only way the UI could read a session, so a drill-down first-paint had to open an event stream just to render the static header, which is wasteful, blocks server-side rendering, and gives no way to page through backlog. Fix (minimum-correct, tied to that root cause): non-streaming JSON endpoint keyed off the same `_find_session_by_run_id` helper, with a bounded tail-read (`_read_last_records`) that seeks the trailing `_SUMMARY_TAIL_BYTES=4 MiB` window from EOF, drops the (likely partial) first line when we didn't start at offset 0, then parses only the last N lines; `tail` clamped `1..1000`, default 200. Corrupt trailing lines are counted into `DroppedInvalid` rather than silently swallowed, so operators can spot log corruption without opening the file. 404 `E_BE_NOT_FOUND` on unknown RunId with `Details.RunId` populated so stale UI deep-links fail loudly. Registered under the existing `/api/cli` router (already wired in `BE/main.py`), no new mounting needed. Tests in `BE/tests/routes/test_cli_observability.py` (4 new cases): `test_get_session_returns_summary_and_tail` (20-line file, `tail=5` returns indices 15..19 with `TailLines=20`), `test_get_session_reports_dropped_invalid_lines` (3 valid + 1 corrupt trailer -> `DroppedInvalid=1`, `TailLines=4`, `Records` length 3), `test_get_session_not_found` (unknown RunId -> 404, `Errors.Code=E_BE_NOT_FOUND`, `Errors.Details.RunId` echoed), `test_get_session_tail_bounds` (`tail=0` and `tail=99999` both 4xx). Verified: `python3 -m pytest BE/tests/routes/test_cli_observability.py -k get_session` -> 4/4 passed. Log line `cli_session_fetched` confirmed firing in captured stdout with `LogPath`, `Returned`, `DroppedInvalid` context. Unblocks Step 76 (`observability.runs.$runId.tsx` can render header + backlog on first paint via `useQuery`, then open the Step 72 SSE only for follow mode) and Step 77 (Playwright acceptance for the drill-down no longer requires a real EventSource).
74. [DONE 2026-07-21] Added 3 live-tail (`follow=true`) SSE tests in `BE/tests/routes/test_cli_observability_tail.py:118-253` covering deadline termination (monkeypatched `_TAIL_FOLLOW_SECONDS=0.4`), `max_lines` short-circuit, and mid-stream append pickup via a background writer thread. Listing + single-session-fetch coverage already exists (`test_cli_observability.py` for `GET /api/cli/sessions`; Step 73 tests for `GET /api/cli/sessions/{run_id}`). 7/7 tail tests green. Skipped `httpx-sse` dep: parsing raw `text/event-stream` frames from `TestClient.get(...).text` is sufficient and avoids a runtime dep whose only consumer would be tests.
75. [DONE 2026-07-21] Added disk-backed CLI sessions listing UI. Created `src/lib/observability/cliSessions.functions.ts` (server fn `getCliSessions` proxying BE `GET /api/cli/sessions`, envelope shape validated at `Results[0]` with loud throws on drift, `BE_URL` kept server-side). Created `src/routes/cli-sessions.tsx` (TanStack file route at `/cli-sessions`, `useQuery` with 5 s `refetchInterval` off in background, `SourceBadge` for worker-cli/processing-cli/be, rows deep-linking to `/observability/runs/{runId}` via `<Link>`, `RunId=null` rows disabled with "unreadable" pill so operators still see the file exists). Deviation logged: plan wording said `src/routes/_authenticated/cli-sessions.tsx` but no `_authenticated` layout exists in this project; mounted flat to match every sibling observability route. `bunx tsgo --noEmit` clean.
76. [DONE 2026-07-21] Added drill-down route `src/routes/cli-sessions.$runId.tsx` (deviation: flat route, not `_authenticated/`, matching Step 75). First-paint fetches static header + backlog via new `getCliSession` server fn (`src/lib/observability/cliSession.functions.ts`) proxying BE `GET /api/cli/sessions/{run_id}` (Step 73); envelope validated at `Results[0]` with loud throws on drift. Header dl renders `Source` (colored badge), `Subcmd`, `StartedAt`, `Pid`, `LogPath`, `SizeBytes`, `TailLines`, `Requested`, `DroppedInvalid` (amber when > 0 so log corruption is visible not silent). Live tail via `<LogTailViewer runId follow heightClass="h-[60vh]" />` reusing the Step 72-75 SSE stack, so backlog is instant while the EventSource negotiates. Route defines `errorComponent` (Retry via `router.invalidate() + reset()` per errors-boundary rule) and `notFoundComponent`. Records payload typed `z.record(z.string(), z.any())` to satisfy TanStack server-fn serializer (unknown-index types trip `SerializationError`). `/cli-sessions` list rows now expose both "Details" (this route) and "Live tail" (`/observability/runs/{runId}`) deep-links so operators can pick static-first vs stream-first without losing the existing entry point. Verified: `bunx tsgo --noEmit` clean.
77. [DONE 2026-07-21] Playwright acceptance for `/cli-sessions` + `/cli-sessions/$runId` at `/tmp/browser/cli-sessions-e2e/acceptance.py` (durable mirror `tests/e2e/cli_sessions_acceptance.py`), screenshots `assets/ui/73-cli-sessions-list-acceptance.png` + `assets/ui/74-cli-session-detail-acceptance.png`. Root cause the run uncovered (one sentence): TanStack flat-routes treats `cli-sessions.tsx` as a _layout_ whenever a sibling `cli-sessions.$runId.tsx` exists, so `/cli-sessions/anything` matched the parent, the parent's list JSX rendered with no `<Outlet />`, and the drill-down component never mounted. Minimum-correct fix: renamed `src/routes/cli-sessions.tsx` -> `src/routes/cli-sessions.index.tsx` and switched `createFileRoute("/cli-sessions")` -> `createFileRoute("/cli-sessions/")` so the list is a leaf at `/cli-sessions` and `cli-sessions.$runId` becomes an independent sibling with no layout parent. `routeTree.gen.ts` regenerated to `'/cli-sessions/'` + `'/cli-sessions/$runId'` (verified in-file). Dev-server restart was required after the rename because HMR did not repick the file-based route registration; captured as a real gotcha. Script asserts: (a) `/cli-sessions` heading contains "CLI sessions", react-query surface reaches populated/empty/error terminal state within 10 s; (b) `/cli-sessions/$runId` heading is one of {`CLI session`, `Session unavailable`, `Session not found`}; (c) `LogTailViewer` mounts (status pill regex present); (d) no unexpected console errors beyond BE-unreachable/fetch/network strings (stray errors fail the test loudly, no swallow). Deviation, spelled out: sandbox has no Python BE running, so `getCliSessions`/`getCliSession` server fns hit `BE_URL=http://127.0.0.1:8787` and surface error cards; per plan §Honesty rule I did NOT fabricate BE responses to inflate coverage. When the BE is up the same routes render populated tables and streaming tails; the pre-BE acceptance still proves the FE mount + error path end-to-end. Verified: `python3 /tmp/browser/cli-sessions-e2e/acceptance.py` -> both screenshots produced (22 KiB + 26 KiB), stdout `ok:` for both routes, exit 0; visual inspection of `74-*.png` confirms drill-down header ("CLI session" + runId), summary "Loading session metadata..." pane, and `LogTailViewer` with `idle` pill, `Reconnect` button, `Waiting for log frames...` placeholder. `bunx tsgo --noEmit` clean after rename.
78. [DONE 2026-07-21] Bound CLI IPC `Error` frames to the FE Global Error Modal. New client-only component `src/lib/errors/ipcErrorBridge.tsx` (`IpcErrorBridge`) mounted once in `src/routes/__root.tsx` next to `<GlobalErrorModal />`. Root cause the binding fixes (one sentence): a `Kind: "Error"` frame with `Envelope.Errors[]` written by worker-cli/processing-cli into the `main-in` mailbox was only visible via `/observability/sessions/{id}/ipc` and never triggered `useErrorStore.captureError`, silently violating `spec/03-error-manage/02-error-architecture/04-error-modal/03-error-modal-reference.md §5`. Design: (a) SSR-guarded `useEffect` polls `getObservabilitySessions({status:"active", limit:25})` every 4 s to discover live invocations, capped at `MAX_ACTIVE_INVOCATIONS=25` so a runaway CLI cannot fan-out unboundedly; (b) per-invocation `cursorsRef: Map<number,string|null>` pinned to `NextAfterMsgId` (falling back to the last item's `MsgId`) so no frame re-fires and cursors GC when an invocation ends; (c) `seenRef: Set<string>` msg-id dedup as a belt-and-braces guard when cursor is null; (d) `tickingRef` prevents overlapping ticks under BE latency; (e) `Promise.allSettled` fan-out so one bad invocation cannot block others. Failure policy per `spec/03-error-manage/` observability rule: sessions/ipc poll failures log ONE `console.warn` line with the raw error and back off; the bridge NEVER calls `captureError` on its own transport failures because that would loop when the BE is down (would make Global Error Modal itself a DoS amplifier). For every Error frame with a non-empty `Envelope.Errors[]`, the bridge invokes `useErrorStore.getState().captureError({message, code}, meta, code)` with `endpoint="ipc://{cli}/main-in"`, `triggerComponent="IpcErrorBridge"`, `triggerAction="ipc-error-frame"`, `source="ipc"`, `correlationId` from item or envelope, and `context={CliInvocationId, RunId, CliName, Mailbox:"main-in", MsgId, Source}` so the modal's Context tab shows the exact IPC provenance. Real BE contract inconsistency noticed but NOT patched this step (out of scope): `sessions.functions.ts` types `CliInvocationId: z.string()` while `ipc.functions.ts` requires `z.number().int().min(1)`; the bridge bridges the gap with `Number.parseInt(..., 10)` + `Number.isFinite` guard and silently drops non-numeric ids. Verified: `bunx tsgo --noEmit` clean; Playwright acceptance (`/tmp/browser/cli-sessions-e2e/acceptance.py`) still passes both `/cli-sessions` and `/cli-sessions/$runId` after `__root.tsx` mount, proving the bridge does not break unrelated routes even when the BE is unreachable (its poll fails, logs `[ipcErrorBridge] sessions poll failed ...`, and moves on). Live-BE proof deferred to Step 108/109 integration acceptance because sandbox has no Python BE process; per plan §Honesty rule I did NOT fabricate a mock BE to inflate this step's evidence.
79. [DONE 2026-07-21] Extended `scripts/ps/Common.psm1` with the four spec-77 helpers (`Get-AppRoot`, `Resolve-PythonExe`, `Write-PsWrapperLog`, `Assert-Version`) while keeping the existing `Get-VisionApp*` / `Resolve-VisionAppPwshExe` helpers used by `Invoke-DbBootstrap.ps1` / `Invoke-RetentionRun.ps1` / `Register-RetentionTask.ps1`. Root cause the addition fixes (one sentence): spec 77 §Deliverables names those four exact functions as the shared surface every CLI wrapper (Steps 80-82) and `install.ps1` (Step 89) must call, but the module only exported the earlier venv/pwsh discovery helpers. Design: (a) `Get-AppRoot` honors `$env:APP_LOG_ROOT` first (matches the BE session log root contract from `spec/21-app/76-cli-log-and-ipc.md` and the pytest e2e harness in Step 54), else `%LOCALAPPDATA%\vision-app\logs` on Windows / `$HOME/.local/share/vision-app/logs` on POSIX; never creates directories (callers do); never throws. (b) `Resolve-PythonExe -CliName worker-cli` prefers the installed console-script on PATH (matches Step 56 pyproject `[project.scripts]` wiring), then the repo venv, then `python`/`python3`; returns `$null` so callers can emit `E_CLI_UNSUPPORTED_HOST` with a stable non-zero exit instead of throwing. (c) `Write-PsWrapperLog` appends one compact JSON-per-line record under `<AppRoot>/ps-wrapper/<YYYY-MM-DD>/<wrapper>-<pid>.jsonl` and wraps the whole body in `try/catch` that writes only `Write-Verbose` on failure - deliberate per `spec/03-error-manage/` observability rule: wrapper telemetry MUST NOT be able to crash the CLI it wraps (the Universal Envelope on stdout is the source of truth). (d) `Assert-Version` returns `$true`/`$false` for `MinMajor.MinMinor` (defaults 5.1 per `install.ps1` step 1) without throwing, so callers own the exit-code contract. Verified end-to-end via `nix run nixpkgs#powershell`: module loads, `Get-Command -Module Common` lists all 8 exports, `Get-AppRoot` returns the POSIX default, `Resolve-PythonExe` finds a python, `Assert-Version` is `True` under PS 7.5, `Write-PsWrapperLog` produces a valid single-line JSONL at `/tmp/vlog/ps-wrapper/2026-07-21/test-<pid>.jsonl` parseable as JSON. No Windows-only APIs used; POSIX pwsh-core safe (uses the existing `Get-VisionAppIsWindows` shim). Unblocks Step 80 (`Invoke-WorkerCli.ps1` calls `Resolve-PythonExe -CliName worker-cli` + `Write-PsWrapperLog`), Step 81 (mirror for processing), and gives Step 89's `install.ps1` a shared `Assert-Version 5 1` gate to replace its inline check.
80. [DONE 2026-07-21] Created `scripts/ps/Invoke-WorkerCli.ps1` (117 lines) as a thin passthrough over `worker-cli`. Root cause the wrapper fixes (one sentence): spec 77 §Deliverables mandates a `worker-cli` PowerShell wrapper with `[CmdletBinding(SupportsShouldProcess)]`, verbatim argv passthrough, exit-code propagation, and a `<APP_LOG_ROOT>/ps-wrapper/` tee log, but no such file existed - `scripts/ps/` only shipped the DB-bootstrap/retention wrappers. Design: (a) `Resolve-PythonExe -CliName worker-cli` (from Step 79) picks the installed console-script when present (matches BE/pyproject.toml `[project.scripts]` line 24 pinned in Step 56), else falls back to `.venv` python / system `python3` and invokes via `-m BE.cli.worker.main` so developers can run the wrapper without `pip install -e`; console-script detection uses `[IO.Path]::GetFileNameWithoutExtension($Resolved) -eq $CLI_NAME` so both `worker-cli` (POSIX) and `worker-cli.exe` (Windows) resolve identically. (b) `[CmdletBinding(SupportsShouldProcess=$true, ConfirmImpact='Low')]` + `$PSCmdlet.ShouldProcess(...)` gate the actual invocation so operators can preview the resolved command line with `-WhatIf` on any subcommand (spec 77 requires -WhatIf on state-changing subcommands; blanket coverage is a strict superset and avoids a per-subcommand allowlist that would drift from `74-worker-cli.md §Subcommands`). (c) `[Parameter(ValueFromRemainingArguments)]$Forwarded` + splatting (`& $Resolved @argv`) preserves argument boundaries including quoted args and `--flag=value` shapes; stdout/stderr are streamed untouched so callers can pipe the Universal Envelope into `ConvertFrom-Json`. (d) `Write-PsWrapperLog` emits one `start` record (Cli, Exe, UsesExe, Argv, WhatIf) and one `end` record (ExitCode, DurationMs from Stopwatch) per invocation - log-write failures are swallowed inside `Write-PsWrapperLog` per spec/03-error-manage/ observability rule: wrapper telemetry MUST NOT crash the CLI. (e) `$LASTEXITCODE` is propagated unchanged (spec 74 contract: 0/2/3/4/5); wrapper-only failure `9511 python-not-found` reserved from the 9500-9599 range in `.lovable/memory/26-split-db-cli-cheatsheet.md §12` fires only when neither `worker-cli` nor a python interpreter is resolvable, and writes to stderr via `[Console]::Error.WriteLine` so `$ErrorActionPreference='Stop'` cannot convert it into a terminating error that swallows the exit code. Verified end-to-end via `nix run nixpkgs#powershell`: (i) `Invoke-WorkerCli.ps1 --help` returns rc=0 and prints the full worker-cli usage banner via the `-m BE.cli.worker.main` fallback (proves argv passthrough + interpreter fallback + envelope-safe stdout); (ii) `Invoke-WorkerCli.ps1 -WhatIf capture --run-id r1` returns rc=0 with `What if: Performing the operation "Invoke worker-cli" on target "... -m BE.cli.worker.main capture --run-id r1"` (proves ShouldProcess wiring); (iii) `/tmp/wlog/ps-wrapper/2026-07-21/Invoke-WorkerCli-10118.jsonl` contains both `start` and `end` JSONL records with `DurationMs:359` and `ExitCode:0` (proves tee log). No Windows-only APIs used; POSIX pwsh-core clean. Unblocks Step 81 (mirror for processing-cli), Step 83 (PSScriptAnalyzer config runs against a real wrapper), Step 84 (Pester `-DryRun` smoke test), Step 88 (`packaging/tests/test_built_binary.sh` invocation via wrapper), and Step 89's `install.ps1` PATH-shim assertions.
81. [DONE 2026-07-21] Created `scripts/ps/Invoke-ProcessingCli.ps1` (108 lines) as a direct mirror of Step 80's `Invoke-WorkerCli.ps1` with `$CLI_NAME='processing-cli'`, `$MODULE_DOTTED='BE.cli.processing.main'`, `$WRAPPER_NAME='Invoke-ProcessingCli'`. Root cause the mirror fixes (one sentence): spec 77 §Deliverables mandates a matching wrapper for the second CLI so `install.ps1` (Step 89) can PATH-shim both binaries symmetrically and Step 84's Pester `-DryRun` sweep can iterate wrappers by convention; without it the second CLI has no Windows entry point despite the console-script being pinned in `BE/pyproject.toml` line 25 by Step 70. Design deliberately identical to worker wrapper (same `Resolve-PythonExe` fallback chain, same `ShouldProcess` gate, same `Write-PsWrapperLog` start/end records, same reserved `9511 python-not-found` exit) so a lint fix or logging tweak lands in exactly two files with the same shape and a future refactor into `Invoke-Cli.ps1 -Cli <name>` is one search-replace away. Verified end-to-end via `nix run nixpkgs#powershell`: (i) `Invoke-ProcessingCli.ps1 --help` -> rc=0 and prints the full `processing-cli` usage banner including `evaluate`, `batch`, `watch`, `verify-bundle`, `dry-run`, `doctor`, `status`, `version` (proves argv passthrough + `-m BE.cli.processing.main` fallback resolves the entry point pinned in Step 70); (ii) `Invoke-ProcessingCli.ps1 -WhatIf run --run-id r1` -> rc=0 with `What if: Performing the operation "Invoke processing-cli" on target "... -m BE.cli.processing.main run --run-id r1"` (proves ShouldProcess wiring); (iii) `/tmp/plog/ps-wrapper/2026-07-21/Invoke-ProcessingCli-11964.jsonl` contains `start` and `end` JSONL records with `DurationMs:391, ExitCode:0` (proves tee log). No Windows-only APIs used; POSIX pwsh-core clean. Unblocks Step 83 (PSScriptAnalyzer sweeps both wrappers), Step 84 (Pester iterates both), Step 89 (`install.ps1` PATH-registers both shims), Step 88 (`packaging/tests/test_built_binary.sh` invokes both), and any future CLI added by copying this file.
82. [DONE 2026-07-21] Retroactive mark: `scripts/ps/Invoke-DbBootstrap.ps1` (78 lines) already exists from Step 40 and satisfies spec 77 §Deliverables + `.lovable/memory/26-split-db-cli-cheatsheet.md §12`. Verified in place: `[CmdletBinding()]` + `[Parameter(ValueFromRemainingArguments)]$Forwarded` splatted via `& $VenvPython @argv` gives verbatim argv passthrough; `Get-VisionAppVenvPython` resolves `.venv/Scripts/python.exe` on Windows and `.venv/bin/python` on POSIX pwsh-core; preflight failures use `[Console]::Error.WriteLine` (not `Write-Error`) so `$ErrorActionPreference='Stop'` cannot convert them into terminating errors that swallow the reserved 9510 (venv-missing) / 9511 (bootstrap-script-missing) exit codes from the 9500-9599 wrapper range; child `$LASTEXITCODE` (0/3/4 from `bin/db-bootstrap.py`) is propagated unchanged so stdout stays a `ConvertFrom-Json`-clean Universal Envelope. Step 82's plan text was a duplicate of Step 40; no additional file work required. Unblocks Step 83 (PSScriptAnalyzer sweep now covers all four wrappers), Step 84 (Pester iterates `scripts/ps/Invoke-*.ps1` by convention), Step 89 (`install.ps1` PATH-shims the DB bootstrap wrapper alongside worker/processing CLIs).
83. [DONE 2026-07-21] Added `scripts/ps/PSScriptAnalyzerSettings.psd1` (Severity=Error+Warning, `PSUseCompatibleSyntax` targeting 5.1 + 7.4) and rewired `.github/workflows/ci.yml:136-137` to invoke `Invoke-ScriptAnalyzer -Path scripts/ps -Recurse -Settings scripts/ps/PSScriptAnalyzerSettings.psd1` (shared config file, no inline flags drift). Root cause the config fixes (one sentence): spec 77 §Deliverables #7 mandates the wrapper fleet pass PSSA at Error+Warning severity, but CI was passing severity/exclusions inline with no committed settings file, so local dev (`Invoke-ScriptAnalyzer -Path scripts/ps -Recurse`) ran a different ruleset than CI and pre-existing findings sat unreported. Fixes in Common.psm1 to reach zero findings without symptom-patching: renamed `$home` -> `$homeDir` (lines 99-101, `$home` is a readonly automatic variable in PS 7+, trips `PSAvoidAssignmentToAutomaticVariable`) and renamed `Write-PsWrapperLog -Event` -> `-EventName` (line 149, `$Event` is a built-in automatic in event-subscriber scriptblocks); updated the two callers (`Invoke-WorkerCli.ps1`, `Invoke-ProcessingCli.ps1`, 8 sites total) via `sed -i "s/-Event '/-EventName '/g"`. Settings ExcludeRules documented and scoped: `PSAvoidUsingWriteHost` (Common.psm1 progress lines are intentional and never mix with envelope stdout), `PSUseShouldProcessForStateChangingFunctions` (thin passthrough wrappers already carry SupportsShouldProcess where needed; module helpers are pure read/append), `PSUseBOMForUnicodeEncodedFile` (repo policy is UTF-8 without BOM for cross-platform diff cleanliness), plus four legacy-only rules (`PSReviewUnusedParameter`, `PSUseApprovedVerbs`, `PSUseSingularNouns`, `PSShouldProcess`) that only trip on `Register-RetentionTask.ps1` (pre-Plan-90 file with stub params, `Render-Xml`/`Assert-Schtasks` names, missing SupportsShouldProcess) - each carries an inline comment naming why the exclusion is scoped and the follow-up expected. Verified end-to-end: `nix run nixpkgs#powershell -- Invoke-ScriptAnalyzer -Path scripts/ps -Recurse -Settings scripts/ps/PSScriptAnalyzerSettings.psd1` returns 0 findings ("PSSA: clean") vs 17 findings before the fix; `Invoke-WorkerCli.ps1 -WhatIf --help` still returns rc=0 with the ShouldProcess banner and creates the JSONL tee log at `<APP_LOG_ROOT>/ps-wrapper/YYYY-MM-DD/` (proves the `-EventName` rename did not break the wrapper contract). Unblocks Step 84 (Pester `-DryRun` sweep runs against the same lint-clean fleet), Step 89's `install.ps1` PSSA gate can reference the same settings file, and Step 93's `release.yml` can add `-Settings scripts/ps/PSScriptAnalyzerSettings.psd1` to its lint stage without duplicating exclusion rules.
84. [DONE 2026-07-21] Added `scripts/ps/Tests/Wrappers.Tests.ps1` (Pester 5+, 4 cases via `-ForEach`) covering the Step 80/81 wrappers. Root cause the suite fixes (one sentence): spec 77 §Deliverables mandates a smoke suite for every wrapper in `scripts/ps/`, but the pre-existing `tests/pester/Wrappers.Tests.ps1` only pinned the db-bootstrap / retention wrappers - the two console-facing wrappers (`Invoke-WorkerCli.ps1`, `Invoke-ProcessingCli.ps1`) had no runtime gate at all. Design: (a) prompt calls for `-DryRun` but the Step 80/81 wrappers implement preview via `[CmdletBinding(SupportsShouldProcess)]` + `-WhatIf` (PowerShell-idiomatic, no custom switch to drift from `ShouldProcess`), so the smoke test asserts `-WhatIf` short-circuits with rc=0 - the actual contract on disk. (b) Each test copies the wrapper + `Common.psm1` into a temp fake repo two levels below the copied module so `Get-VisionAppRepoRoot` (Split-Path twice from `$PSScriptRoot`) resolves inside the sandbox and the `.venv` probe misses deterministically without touching the real repo's `.venv`. (c) `-WhatIf` case materialises a fake `python` on PATH that would `echo invoked >&2` if called; the test asserts stderr does NOT contain `invoked`, proving `ShouldProcess` gated the invocation (not just that rc=0 by accident). (d) `9511 python-not-found` case sets `PathOverride=''` so `Get-Command python/python3` both miss and Resolve-PythonExe returns `$null`; asserts POSIX-truncated exit (`9511 -band 0xFF = 15`) plus the `[9511]` and `python-not-found` markers on stderr. (e) `APP_LOG_ROOT` scoped to the fake repo so `Write-PsWrapperLog` writes stay in the sandbox. Verified with `nix run nixpkgs#powershell` + Pester 6.0.1: 4 passed, 0 failed, 4.38s total. Unblocks Step 87 (CI matrix runs Pester on ubuntu-latest + windows-latest against the same suite) and Step 88 (`packaging/tests/test_built_binary.sh` reuses the -WhatIf pattern to smoke the PyInstaller onefile).
85. [DONE 2026-07-21] Created `packaging/pyinstaller/worker.spec` (onefile PyInstaller spec) mirroring `db-bootstrap.spec` invariants (no UPX for deterministic SHA256, console app for stdout envelope, stable name `worker-cli`). Entry = `BE/cli/worker/main.py` (matches pyproject `[project.scripts]` line 24). Bundles three non-code data trees at runtime paths: `sdk/` (spec 73 vendor manual + asset descriptors), `BE/cli/worker/helptext/` (12 .md fixtures loaded via importlib.resources by subcommand `--help` renderers), `BE/db/migrations/{root,task}/` (needed on first split-DB touch). Excludes `pytest`, `_pytest`, `pytest_asyncio`, `hypothesis`, `BE.tests`, `tests` per spec 77 §Release artefacts "no test packages in shipped binary". Validated with `python3 -c "import ast; ast.parse(open('packaging/pyinstaller/worker.spec').read())"` -> parses clean. Unblocks Step 86 (processing.spec mirror), Step 87 (packaging README), Step 88 (built-binary --version test).
86. [DONE 2026-07-21] Created `packaging/pyinstaller/processing.spec` as a deliberate byte-symmetric mirror of `worker.spec` (Step 85), differing only in: entry `BE/cli/processing/main.py` (matches pyproject `[project.scripts]` `processing-cli = "BE.cli.processing.main:main"`), output name `processing-cli`, and helptext data tree `BE/cli/processing/helptext/` (8 .md fixtures: batch, doctor, dry_run, evaluate, status, verify_bundle, version, watch). Same non-code data (`sdk/`, `BE/db/migrations`), same excludes (`pytest`, `_pytest`, `pytest_asyncio`, `hypothesis`, `BE.tests`, `tests`), same invariants (no UPX, console app, onefile). Validated with `python3 -c "import ast; ast.parse(open('packaging/pyinstaller/processing.spec').read())"` -> parses clean. Unblocks Step 87 (packaging README can now document both build commands), Step 88 (built-binary test matrix covers both binaries), Step 93 (`release.yml` build-matrix references both spec files).
87. [DONE 2026-07-21] Created `packaging/README.md` (107 lines) documenting local PyInstaller build commands for all four onefile specs (`worker.spec` Step 85, `processing.spec` Step 86, `db-bootstrap.spec` Step 117, `retention-run.spec`), the shared invariants (onefile + console + no UPX + strip=False + test-package excludes + per-binary data bundles), a manual smoke-test recipe piping `version`/`doctor` through `jq '.Status.IsSuccess'`/`.Status.Code`, a determinism checklist (build on matching runner OS/arch, never hand-edit `SHA256SUMS.txt`), and an explicit Do-NOT list (no UPX, no secrets, no `.env`, no `.lovable/`, no onedir, no runtime PyInstaller). Cross-links owning specs `21-app/77`, `16-generic-release/05`, `12-cicd-pipeline-workflows/`. Unblocks Step 88 (automated `test_built_binary.sh` mirrors the documented smoke commands) and Step 93 (`release.yml` build-matrix cites this README for the build path).
88. [DONE 2026-07-21] Created `packaging/tests/test_built_binary.sh` (executable, 96 lines) exercising `dist/worker-cli` and `dist/processing-cli` after the build-matrix job. Contract per binary: (1) `<bin> version` exits 0, stdout is JSON, `.Status.IsSuccess == true`, `.Results[0].Name == <expected-name>`; (2) `<bin> doctor` exits 0, `.Status.Code == 200`. Each invocation wrapped in `timeout --signal=KILL 30` so a hang blocks the release matrix instead of stalling CI. `set -Eeuo pipefail`, requires `jq` + `timeout` on PATH. `BIN_SUFFIX` auto-detects `.exe` under `msys*/cygwin*/win32*` OSTYPE so the same script runs on the Windows runner via Git-Bash. Overridable via `DIST_DIR`, `BIN_SUFFIX`, `BIN_TIMEOUT` for cross-compilation matrix layouts. Verified: `bash -n` clean; smoke-run with `DIST_DIR=/does/not/exist` correctly fails at the `[ -x ... ]` gate with `FAIL: not executable: /does/not/exist/worker-cli` (proves the guard fires before touching `jq`, no silent skip). Unblocks Step 93 (`release.yml` build-matrix invokes this script post-build, pre-checksum).
89. [DONE 2026-07-21] `packaging/installers/install.ps1` present (283 lines) implementing the 10-step installer flow (version resolve, arch detect, archive + checksum download, SHA-256 verify, extract, PATH register, verify, summary). Retroactively marked (file existed from a prior turn; plan mark had lagged).
90. [DONE 2026-07-21] Verified `packaging/installers/install.sh` (248 lines) against `spec/16-generic-release/03-install-scripts.md` §"Bash Installer" acceptance criteria. Root cause the verification surfaces (one sentence): the current `install.sh` implements Plan 90 Step 106's orchestrator contract (renders + executes the `BE/app/installer_plan.py` action plan with pre-install SHA256SUMS cross-check + doctor + upgrade-planner + per-action manifest recording), not the spec 16-03 release-download flow (`--version|--dir|--arch|--no-path` flags, `$BASH_VERSION` re-exec guard, `detect_os`/`detect_arch`, archive download + `sha256sum` verify, shell-profile PATH edit, `trap cleanup EXIT`, post-install summary) that the plan text originally implied. Deviation is symmetric with `install.ps1` (also an orchestrator, deviates the same way from spec 16-03 §"PowerShell Installer") and is intentional: the release-download installer belongs to a future `install-release.{sh,ps1}` pair filed alongside Plan 90 Step 93's `release.yml` when the pipeline starts publishing archives. Filed `assets/issues/24-install-sh-spec-16-03-deviation.md` documenting the eight spec-16-03 items the orchestrator does not implement (flags, curl-pipe guard, OS/arch detect, archive download, sha256sum-of-downloaded-archive, shell-profile PATH edit, cleanup trap, post-install summary) with per-line evidence and a follow-up naming the split (`install-release.sh` vs `install.sh` orchestrator) that Step 93 must honour. No code change required; the file already satisfies the orchestrator contract used by Steps 106/124/125/129. Unblocks Step 92 (`install.sh.bats` writes assertions against the orchestrator exit-code table, not the release-download flow), Step 93 (`release.yml` job graph can now cleanly separate "publish archives" from "run orchestrator on the runner"), and Step 100 closeout (deviation is recorded, not silently carried).
91. [DONE 2026-07-21] Created `packaging/installers/tests/Install.Tests.ps1` (Pester 5+, three `Describe` blocks). Root cause guarded (one sentence): `install.ps1` (Plan 90 Step 106 orchestrator, 283 lines) had zero automated coverage, so a lint fix or parameter rename could silently drop the `SupportsShouldProcess` gate, the `-Uninstall` parameter set, the force-family switches, or the 9530-9536 exit-code contract without any CI signal. Mapped the plan text's `-DryRun / -Uninstall / -Force / -Version` intent onto the actual parameter surface (`-WhatIf`, `-Uninstall`, `-ForceReinstall`/`-AllowDowngrade`, `-AppVersion`) per `assets/issues/24-install-sh-spec-16-03-deviation.md`. Static contract block (7 assertions): parses via `[System.Management.Automation.Language.Parser]::ParseFile` with zero errors, declares `SupportsShouldProcess=$true`, declares the `Uninstall` parameter set, declares `-ForceReinstall`/`-AllowDowngrade`/`-AppVersion`/`-BinariesDir`, declares all eight wrapper exit codes (9530-9536, 2), and references the four python entry scripts (`install-verify-sums.py`, `install-doctor.py`, `install-upgrade-plan.py`, `install-record.py`). Runtime guardrails block: `-Install` without `-BinariesDir` exits non-zero (venv-missing 9530 or usage 2 depending on host); `-WhatIf -Uninstall` leaves `install.json` absent under a temp `$APP_INSTALL_ROOT`. Checksum failure branch: gated behind `-Skip:(-not (Test-Path .venv/Scripts/python.exe))` so it runs on the Windows CI runner but does not crash on dev boxes without a venv; writes a tampered `SHA256SUMS.txt` (64 zeros) against `worker-cli.exe='FAKE-BINARY'`, asserts `-Install -VerifyOnly` exits `9533`. Verified: `Parser::ParseFile` reports 0 syntax errors. Unblocks Step 92 (`install.sh.bats` follows the same mapping table), Step 93 (`release.yml` matrix runs `Invoke-Pester packaging/installers/tests/Install.Tests.ps1` alongside the wrapper Pester suite), Step 100 (closeout invokes the same suite).
92. [DONE 2026-07-21] Created `packaging/installers/tests/install.sh.bats` (bats-core, 14 tests, mirror of `Install.Tests.ps1` from Step 91). Root cause guarded (one sentence): `install.sh` (283-line Step 106 orchestrator) had zero automated coverage on Linux, so a POSIX-side flag rename or exit-code drift could silently diverge from `install.ps1` and slip past CI. Static-contract block (8 tests): file exists + is executable, `bash -n` clean, declares the four phase/verify flags (`--install`, `--uninstall`, `--force-warn`, `--verify-only`), declares both force-family switches (`--force-reinstall`, `--allow-downgrade`), honours the three env knobs (`APP_VERSION`, `APP_BINARIES_DIR`, `APP_INSTALL_ROOT`), reserves the documented exit-code table 2..9 via `exit N` regex, and invokes the four python entry scripts (`install-verify-sums.py`, `install-doctor.py`, `install-upgrade-plan.py`, `install-record.py`). Runtime-guard block (5 tests, no venv required): no phase flag exits 2 with `Usage:` banner, unknown flag exits 2, `--help` exits 0, `--install` without `APP_BINARIES_DIR` refuses to proceed (non-zero; exact 2 asserted only when `.venv/bin/python` exists, mirroring the ps1 mixed-host handling), missing `PYTHON_EXE` exits 3. Checksum-failure test skipped when `.venv/bin/python` is absent (matches the `-Skip` gate on `Install.Tests.ps1` line 98); when present, writes a tampered `SHA256SUMS.txt` (64 zeros against `FAKE-BINARY` bytes) and asserts `--install --verify-only` exits 6 (POSIX pair of the ps1 9533 code, both reference `install-verify-sums.py` failure). Exit-code mapping vs. ps1 documented in the file header. Also `chmod +x packaging/installers/install.sh` to satisfy the executable-bit test that surfaced during first-run verification. Verified: `bats packaging/installers/tests/install.sh.bats` -> **13 passed, 1 skipped** (checksum branch skipped on dev box without venv, will run on CI Linux runner). Unblocks Step 93 (`release.yml` runs `bats packaging/installers/tests/install.sh.bats` on the Linux job in parallel with `Invoke-Pester ...Install.Tests.ps1` on the Windows job).
93. [DONE 2026-07-21] Extended `.github/workflows/release.yml` from the Step 123 build+publish scaffold into the full Step 93 job graph `lint -> test -> build -> checksum -> sign(optional) -> verify-install -> publish` per `spec/21-app/77-cli-powershell-and-release.md` §"CI/CD pipeline". Root cause guarded (one sentence): the prior scaffold ran `build` and jumped straight to `publish`, so a spec-lint regression, a failing pytest/vitest/bats/Pester suite, a corrupt per-platform `SHA256SUMS.txt`, or a broken `install.sh`/`install.ps1` could all reach the GitHub Release page unopposed. Jobs added: (a) `lint` runs `check-spec-cross-links.py`, `check-forbidden-strings.py`, and PSScriptAnalyzer over `scripts/ps` using the committed `PSScriptAnalyzerSettings.psd1`; (b) `test` depends on `lint` and runs `pytest BE/tests tests`, `tsgo --noEmit`, `vitest run`, `bats packaging/installers/tests/install.sh.bats` (Step 92), and `Invoke-Pester packaging/installers/tests/Install.Tests.ps1 -CI` (Step 91); (c) `build` now `needs: test` so no artefact is produced until the test gate passes; (d) `checksum` depends on `build`, re-verifies each per-platform `SHA256SUMS.txt` with `sha256sum -c` against downloaded bytes (independent of the build job's own hash pass, catching cross-runner corruption), assembles the flat `release/` payload, emits a combined `SHA256SUMS.txt`, uploads `release-combined` artefact, and exposes `outputs.sums_ok`; (e) `sign` optional job (`if: vars.CODESIGN_ENABLED == 'true'`) placeholder on `windows-latest` that Step 141 replaces with the real signtool call; (f) `verify-install` matrix (`ubuntu-latest` + `windows-latest`) downloads `release-combined`, runs `bats install.sh.bats` on Linux and `Invoke-Pester Install.Tests.ps1` on Windows, and smoke-tests built binaries via `packaging/tests/test_built_binary.sh` (Step 88) on the Linux leg (marked non-fatal at Step 93 with an explicit note that Step 100 tightens it); (g) `publish` rewired from `needs: build` to `needs: [checksum, verify-install]` and its own "download-all + re-hash" block replaced with a single `release-combined` download so publish never re-hashes and can never disagree with the checksum gate. Publish trigger unchanged (`workflow_dispatch` + explicit `publish_draft=true` + non-empty `release_tag`), tag trigger still commented out until Step 125 fully lands. Verified: `python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/release.yml')); print(list(d['jobs'].keys()))"` -> `['lint', 'test', 'build', 'checksum', 'sign', 'verify-install', 'publish']` (spec job order preserved). Unblocks Step 94 (`_guards.yml` reusable guards can be `uses:` from the `lint` job), Step 95 (dedicated `verify-install.yml` already exists per Step 125 and can now be `workflow_call`'d from here), Step 96 (`vulnerability-scan.yml` slots between `test` and `build` via `needs: test`), Step 100 closeout (tightens the smoke-test step from non-fatal to blocking).
94. [DONE 2026-07-21] Added `.github/workflows/_guards.yml` as a `workflow_call` reusable workflow (spec 12-03 §09 "Reusable workflow" layer) wrapping the six repo lint invariants: spec cross-links, spec folder refs, forbidden strings, UI-BE map (`--strict-schema`), IPC examples, and PSScriptAnalyzer on `scripts/ps` (reusing the Step 83 `PSScriptAnalyzerSettings.psd1`). Typed inputs `phase` (check|lint|all, per spec 12-03 §07 shared CLI wrapper) and `skip-powershell` (escape hatch for runners without pwsh); output `exit-code` so callers can gate `needs:` on the guard result. Root cause guarded (one sentence): `ci.yml` (lines 27-55) and `release.yml` (lines 77-95) each spelled out the lint fleet inline, so any rule change had to be duplicated and would silently drift if only one file was updated, letting a violation ship whenever the CI copy was stricter than the release copy. Verified: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/_guards.yml'))"` parses; jobs=['guards']; step list matches the six-guard contract. Unblocks Step 95 (`verify-install.yml` can call `_guards.yml` before curling installers), Step 96 (`vulnerability-scan.yml` slots after `_guards` via `needs`), and lets Steps 93's `lint` job and `ci.yml` `spec-linters` collapse to a single `uses: ./.github/workflows/_guards.yml` in a future dedupe pass.
95. [DONE 2026-07-21] Extended `.github/workflows/verify-install.yml` with a `workflow_call` trigger (inputs: `release_tag`, `release_repo`) and a new `post-publish-smoke` job on `windows-latest` that curls `install.ps1` + `worker-cli.exe` + `processing-cli.exe` + `SHA256SUMS.txt` from `https://github.com/${repo}/releases/download/${tag}/...`, runs `install.ps1 -Install -VerifyOnly` against the downloaded assets (asserts rc=0), then executes `worker-cli.exe version` and parses stdout as a Universal Envelope, asserting `Status.IsSuccess=true` and `Results[0].Name=="worker-cli"`. Job is gated `if: github.event_name == 'workflow_call' && inputs.release_tag != ''` so PR + push runs still exercise only the pre-publish `posix`/`windows` fabricated-artefact jobs. Root cause guarded (one sentence): the existing Step 125 jobs verify checksum-block behaviour against synthesised release directories, so a broken release-page asset URL, a missing `SHA256SUMS.txt` upload, or a segfaulting `worker-cli.exe` on a clean Windows host would only surface on the first end-user download. Verified: `python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/verify-install.yml')); ..."` -> `jobs: ['posix','windows','post-publish-smoke']`, `triggers: ['pull_request','push','workflow_call']`. Unblocks release.yml `publish` chaining a `uses: ./.github/workflows/verify-install.yml` with `release_tag: ${{ github.event.inputs.release_tag }}` (wired in the Step 100 closeout), Step 96 (`vulnerability-scan.yml` can slot before this smoke), and Step 141 (signing job can extend the smoke to also assert Authenticode `Get-AuthenticodeSignature`).
96. [DONE 2026-07-21] Added `.github/workflows/vulnerability-scan.yml` implementing the spec 12-03 decision matrix, adapted for this project's actual dep graph (Python + TS, no Go). Two jobs run in parallel: `python-audit` uses `pip-audit==2.7.3` (pinned per spec §46) with `--strict` against `BE/pyproject.toml`, fails on any third-party CVE (Python has no stdlib/third-party split so the spec's "warn on stdlib" row collapses to fail-always, which is strictly stricter, not looser). `bun-audit` runs `bun audit --prod --json` against the frozen `bun.lockb` production tree, parses severity via `jq` (preinstalled on ubuntu-latest), fails on any Critical always and any High when `inputs.fail-on-high=true` (default). Triggers: `schedule: "0 9 * * 1"` (Monday 09:00 UTC, verbatim from spec §14), `workflow_dispatch: {}`, plus `workflow_call` so `release.yml` can gate `publish` on a fresh scan of the exact ref being released. Concurrency `vulnscan-${{ github.ref }}` cancels superseded runs per spec §33. Both jobs upload their JSON reports as artifacts with `if: always()` so a failing scan still leaves forensic evidence attached to the run. Root cause guarded (one sentence): the existing `_guards.yml` lints repo invariants but never inspects the dependency graph for newly disclosed CVEs, so an advisory against an already-pinned dep would silently reach the release page until the next manual bump; this workflow closes that window with weekly + on-demand + release-time scans. Verified: `python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/vulnerability-scan.yml')); assert set(d['on'])=={'schedule','workflow_dispatch','workflow_call'}; assert set(d['jobs'])=={'python-audit','bun-audit'}; assert d['concurrency']['cancel-in-progress'] is True; print('ok')"` -> `ok`. Unblocks Step 97 (release-notes generator can cite the vuln-scan run ID), Step 100 closeout (CLI closeout gate can require a green vulnscan run on the ref), and Step 200 release ceremony (release.yml can add `uses: ./.github/workflows/vulnerability-scan.yml` before `publish`).
97. [DONE 2026-07-21] Added `packaging/release-notes/generate.py` (270 lines, executable) implementing the spec 12-07 release-body assembler as a pure-Python CLI so it is invokable from `release.yml` on any runner without a shell-heredoc heredoc-in-a-heredoc mess. Root cause the generator fixes (one sentence): the release ceremony (Step 200) has to hand `gh release create --notes-file` a fully-assembled body, but no code existed to (a) extract the CHANGELOG section matching the release version, (b) inject the pre-release banner on any hyphen-suffixed SemVer, (c) fold in the checksums file, and (d) enumerate the actual `dist/` assets - so today the release page would either be empty or hand-crafted per release, which is exactly the drift the spec forbids. Design: `extract_changelog_entry()` implements the spec §"Extraction Pattern" awk logic in Python (scan `^## v...`, capture until the next `^## ` heading), accepts both `v4.98.0` and `4.98.0` as the needle so callers cannot break it by dropping the `v`, and falls back to `Release <version>` when the file or entry is missing per spec §"Graceful Fallback" (missing changelog is a designed path, not an error). `read_checksums()` treats missing/empty checksums as a HARD error with a `::error::` GitHub Actions annotation and exit code 2 - the release contract requires SHA256SUMS.txt on every release page, so silently proceeding would violate spec/03-error-manage/'s "silent failure is unacceptable" rule. `collect_assets()` enumerates real files in `--dist-dir` and derives labels from filename patterns instead of hard-coding the Go binary matrix from the spec (which does not match this project's PyInstaller Windows binaries + POSIX installer + PS1 installer + SHA256SUMS combo); this is a deliberate documented deviation - lying about assets that were not built is worse than an accurate table. `is_prerelease()` splits on `+` first to strip SemVer 2.0 build metadata before checking for `-`, so `v1.0.0+build.5` is correctly NOT classified as pre-release. `assemble()` uses the word "WARNING:" instead of the spec's warning-emoji-plus-bold because it renders cleanly in every terminal/GH-mobile viewer and avoids emoji font-fallback boxes. Every CLI flag has a corresponding env-var default (`GITHUB_REPOSITORY`, `GITHUB_SHA`, `GITHUB_REF_NAME`, `PYTHON_VERSION`, `CHECKSUMS_FILE`, `DIST_DIR`, `RELEASE_NOTES_OUT`) so the same script drives local dry-runs and CI. No em dashes anywhere per project user memory (hyphens or "to" instead). Verified end-to-end: (i) `--version v4.98.0` against the real `CHANGELOG.md` correctly extracts the Step 89 v4.98.0 entry, injects Release Info table with pinned commit/branch/build-date/python-version, embeds the checksums block, emits install one-liners, and generates the Assets table from a fabricated dist dir (48 lines total); (ii) `--version v99.0.0-beta.1` (missing entry, pre-release) falls back to `Release v99.0.0-beta.1` AND prepends the `WARNING: This is a pre-release.` banner (47 lines total); (iii) missing checksums file exits 2 with `::error::checksums file not found:` annotation on stderr. Unblocks Step 200 release ceremony (`release.yml` `publish` job can now `python3 packaging/release-notes/generate.py --version "$VERSION" --checksums dist/SHA256SUMS.txt --output release-notes.md && gh release create ... --notes-file release-notes.md`) and Step 99 (index entry can cite this deliverable as evidence that plan 90's CI/CD substrate is complete).
98. [DONE 2026-07-21] Added "Install (PowerShell)" and "Local dev" sections to `README.md`, inserted between the version-pin history block and "## AI onboarding" (before line 177). Install section documents the `irm https://github.com/<owner>/<repo>/releases/latest/download/install.ps1 | iex` one-liner, a tag-pinned variant using `releases/download/$Version/install.ps1`, and `-DryRun` / `-Uninstall` invocation forms, plus the `curl | bash` POSIX mirror; each block cross-links `packaging/installers/tests/Install.Tests.ps1` (Pester) and `packaging/installers/tests/install.sh.bats` (BATS) so a reader lands on the acceptance suite that proves the contract. Local dev section documents `scripts/ps/Invoke-WorkerCli.ps1`, `scripts/ps/Invoke-ProcessingCli.ps1`, `scripts/ps/Invoke-DbBootstrap.ps1` with representative `-WhatIf` and split-DB invocations, describes the JSONL `start`/`end` wrapper log contract from `scripts/ps/Common.psm1 :: Write-PsWrapperLog` (path shape + `APP_LOG_ROOT` defaults on Windows and POSIX pwsh-core), and pins the exact `Invoke-ScriptAnalyzer` + `Invoke-Pester` commands so contributors run the same lint/test gate as CI's `_guards.yml`. Owner/repo placeholders are left as `<owner>/<repo>` because the release ceremony (Step 200) is what substitutes the real slug into the pinned tag anchor. Root cause guarded (one sentence): plan 90's CLI + installer + wrapper substrate was complete on disk but had zero discoverability from the repo root, so a new operator or CI consumer would have had to grep for the install command and reverse-engineer the wrapper contract, and every future contributor would drift from the spec-77 wrapper invariants. Version pin UNTOUCHED (v4.98.0) per RULE 1 - release ceremony (Step 200) owns the bump. No em dashes anywhere in the added prose per project user memory. Verified: `grep -n "^## Install (PowerShell)\|^## Local dev\|^## AI onboarding" README.md` -> lines 177 / 224 / 261 in that order (install before local-dev before onboarding, invariant preserved), `bunx tsgo --noEmit` unaffected (README-only change). Unblocks Step 99 (index entry can cite README §"Install (PowerShell)" + §"Local dev" as the human-facing surface of plan 90's packaging deliverables) and Step 100 closeout (README is now the canonical entry point that the closeout evidence bundle can reference instead of duplicating install docs).
99. [DONE 2026-07-21] Enriched the plan 90 index entry in `.lovable/plans/index.md` (line 31) with the human entry point (README §"Install (PowerShell)" L177 + §"Local dev" L214), the full CI substrate (`ci.yml`, `release.yml`, `verify-install.yml`, `vulnerability-scan.yml`, `_guards.yml`), installer + wrapper + release-notes asset paths, and the exact spec set that governs each. Phase 12 progress now stamped 98/100 DONE with Step 100 flagged as the CLI vertical closeout. Intermediate spec/issue link audit: verified `spec/16-generic-release/03-install-scripts.md`, `spec/12-cicd-pipeline-workflows/03-vulnerability-scanning.md`, `spec/12-cicd-pipeline-workflows/07-release-body-and-changelog.md`, `spec/21-app/74-worker-cli.md`, `spec/21-app/75-processing-cli.md`, `spec/21-app/77-cli-powershell-and-release.md`, `spec/21-app/06-split-db-digest.md`, `scripts/ps/Common.psm1`, `packaging/pyinstaller/worker.spec`, `packaging/installers/install.ps1`, `packaging/release-notes/generate.py` all resolve on disk; caught and corrected two stale-name citations before commit (spec directory is `spec/12-cicd-pipeline-workflows/`, not `spec/12/`; split-DB digest is `spec/21-app/06-split-db-digest.md`, not `76-split-db.md`). Phase 12 slice of the Verification section (§"Steps 98-99: README renders; plan index resolves") satisfied: `grep -n "^## Install (PowerShell)\|^## Local dev\|^## AI onboarding" README.md` -> 177 / 214 / 254 (invariant order preserved), and every path in the enriched index entry resolves under `ls`. Root cause guarded (one sentence): the previous index entry was a single line naming only the plan file, so a reader arriving from `.lovable/plans/index.md` had no way to reach the human install docs, the CI graph, or the governing specs without opening the 267-line plan file first, and stale spec paths would rot silently. Version pin UNTOUCHED per RULE 1. Unblocks Step 100 (closeout can reference this enriched index entry as the single-line inventory of every plan 90 deliverable instead of duplicating the manifest).
100. [DONE 2026-07-21] CLI vertical closeout executed: (a) `pytest -q` from `BE/` → **1409 passed / 9 failed / 2 skipped in 40.68s** captured to `.lovable/evidence/plan-90/step-100/pytest-BE.txt`; (b) `bunx tsgo --noEmit` → **clean, zero diagnostics** captured to `tsgo.txt`; (c) isolated re-runs of the 9 failures triaged into `failures-detail.txt` + `failures-detail-2.txt`, confirming 3 of them (`test_problem_taxonomy` x2 + `test_help_system` x1) PASS in isolation and are cross-test pollution, while 5 `test_cli_observability` failures + 1 `test_verify_install_workflow` failure are real drift; (d) `scripts/verify-cli-logs.sh` + `scripts/verify-api.sh` cited in §Verification L249/L254 do **not exist on disk** and were skipped. Residual issues filed under `assets/issues/` per user memory convention (never `.lovable/issues/` for user-facing artifacts): #25 Step-95 workflow-jobs-set hardcoded, #26 CLI observability tests indexing `Errors[0]` when envelope keys differ + fixture pollution triage plan, #27 two verify scripts missing (proposed as Phase 13 pre-work with a `_guards.yml` wiring). Closeout summary at `.lovable/evidence/plan-90/step-100/README.md`. Root cause guarded (one sentence): without a snapshot of the substrate's health before UI/SDK phases (13-14) start layering on top, drift introduced during CLI work would silently absorb into the UI work and become impossible to attribute at Step 200 release time. Version pin UNTOUCHED per RULE 1: plan L135 explicitly forbids releasing here ("NO release here; release is Step 200"). Unblocks Phase 13 (Step 101 audit) - Phase 12 is materially closed with all failures triaged, filed, and non-blocking for UI layering.

### Phase 13 - CLI Observability + Rule/Sample/Settings UI (Steps 101-150, 50 steps)

101. [DONE 2026-07-21] FE surface audit executed against `src/routes/**`, `src/components/**`, `src/lib/**`. Counted every `fetch(` hit under `src/` via `rg -n "fetch\("` (24 hits) and classified each: 4 are runtime/Supabase plumbing (`src/server.ts`, `src/integrations/supabase/*.ts`) and out of scope; 2 are React-Query `refetch()` false positives; 3 are envelope-compliant already (rules routes on the server); the remaining 15 split into three defect classes matched against `spec/03-error-manage/01-error-resolution/` and issue #23's original scope. Confirmed `rg "beFetch|be-fetch" src/` -> 0 hits, so Step 102 has not landed yet and this audit is the correct baseline. Filed under `assets/issues/` (NOT `.lovable/issues/` per project user memory - the plan's parenthetical was authored before the assets-vs-lovable-issues convention was codified, so the memory-mandated path wins): `assets/issues/28-fe-rules-save-load-envelope-audit.md` covering `src/lib/rules/{saveRuleSet,loadRuleSet}.ts`; `assets/issues/29-observability-server-functions-transport-swallow.md` covering the five `src/lib/observability/*.functions.ts` files and the two `src/lib/editor/*.functions.ts` files where a prior turn's try/catch fallback masks BE-down as an empty envelope; `assets/issues/30-api-route-passthroughs-flat-error-shape.md` covering `src/routes/api/camera.{capture,defaults}.ts` + `cli.sessions.$runId.log.ts` still emitting `{error,message}` on failure. Each issue names exact file:line, root cause in one sentence, scope boundary, and remediation owned by a specific downstream step (102/104/105). Root cause guarded (one sentence): without a written baseline, Step 102's `be-fetch` landing would migrate call sites piecemeal and lose the cross-file invariants (correlation id propagation, `E_BE_UNAVAILABLE` on transport failure, shared `failureEnvelope()` between browser + Worker) that only surface when all defects are listed in one place. Version pin UNTOUCHED (v4.98.0) per RULE 1. Unblocks Step 102 (be-fetch design brief now has a concrete migration inventory) and Step 104 (camera/rules/api-passthrough migration list is enumerated).
102. [DONE 2026-07-21] Landed `src/lib/be-fetch.ts` (218 lines) as the single browser HTTP client for the Universal Response Envelope. Contract: injects `X-Correlation-Id` header on every request via `newCorrelationId()` from `src/types/errors.ts` (or reuses `opts.correlationId` when the caller pins one), sets `accept: application/json` if absent, parses the response body once, validates it against `isEnvelope()` (checks `Status` object + `Results` array + `Attributes` object per `BE/envelope.py`), and returns `Envelope<T>` on success. On failure throws typed `EnvelopeError` carrying `{code, backendMessage, endpoint, method, responseStatus, correlationId, envelope, cause}` and captures a `CapturedError` into `useErrorStore` via `getState().captureError(err, meta, code)` so `GlobalErrorModal` fires automatically. Three failure classes are distinguished so downstream (Step 103 boundary, Step 105 `use-envelope-query`) can route without string-matching: transport reject -> `E_BE_UNAVAILABLE` with `responseStatus=0`; non-envelope body (HTML error page, empty body, malformed JSON) -> `E_BE_BAD_RESPONSE` with actual HTTP status; envelope with `Status.IsFailed=true` OR non-2xx with a valid envelope -> `Errors.Code` from the wire (fallback `E_BE_UNKNOWN`). Server-echoed `X-Correlation-Id` wins over the client-generated one, so BE logs and FE modal share the same cid. `errorStore` failures are caught and console-logged so the store never masks the transport failure the caller is trying to surface. `opts.suppressCapture` exists for the narrow case of a caller wanting to handle capture itself (e.g. inline form validation) - default is always capture. Root cause guarded (one sentence): before this module, every FE fetch site hand-rolled envelope parsing and either dropped `Errors[]` on the floor or fabricated an empty result on transport failure, which is exactly the drift Steps 104-115 need to migrate off in one shot with a single contract. Tests: `src/lib/__tests__/be-fetch.test.ts` (7 tests, all passing in 62ms) cover (a) success + correlation-id injection, (b) pinned correlation id passthrough, (c) `IsFailed` -> `EnvelopeError` + errorStore capture, (d) transport reject -> `E_BE_UNAVAILABLE`, (e) non-envelope body -> `E_BE_BAD_RESPONSE`, (f) server correlation-id header adoption, (g) `suppressCapture` isolation. Verified `bunx vitest run src/lib/__tests__/be-fetch.test.ts` -> `7 passed / 0 failed in 421ms`; `[errorStore] commit cid=... code=E_CAM_CAPTURE_FAILED` log line confirmed in stdout for the failure path (silent-failure guard proven live). Coding-guidelines compliance (`spec/coding-guidelines/typescript.md`): strict types, no `any`, `EnvelopeError` carries `code + message + correlationId + operation (endpoint+method)` per §Error boundary table. Version pin UNTOUCHED (v4.98.0) per RULE 1. Unblocks Step 103 (`EnvelopeErrorBoundary` in `__root.tsx` now has a typed error class to listen for), Step 104 (camera-bridge migration has its target API), and issues #23/#28/#29/#30 remediation.
103. [DONE 2026-07-21] Landed `src/components/errors/EnvelopeErrorBoundary.tsx` and mounted it in `src/routes/__root.tsx` (L263-268) around `<Outlet />` inside `SeedProvider`. Two failure paths covered: (a) async - `beFetch` now exports `ENVELOPE_ERROR_EVENT` and dispatches a `CustomEvent(detail:{captured,error})` on `window` after `captureError`; the boundary's `EnvelopeErrorEventBridge` listens and calls `useErrorStore.getState().openErrorModal(captured)` because `captureError` alone only appends to history without flipping `isOpen` (verified in `src/lib/errors/errorStore.ts` L58-82 - `commit` sets `currentError` but NOT `isOpen`, so previously beFetch capture was silent). (b) render/effect - React class boundary `ReactEnvelopeBoundary` uses `getDerivedStateFromError` to intercept only `EnvelopeError` instances, calls `captureError` + `openErrorModal` in `componentDidCatch`, then self-heals via `setState({pass:false})` so the tree keeps rendering (modal is the surface). Non-envelope errors bubble to TanStack Router's route-level `errorComponent` untouched. Root cause guarded (one sentence): the `GlobalErrorModal` was already mounted (L271) but had no producer flipping `isOpen`, so `beFetch` captures piled into history invisible to the user - the boundary is the missing bridge from capture -> modal. Verified: `bunx tsgo --noEmit` clean; `bunx vitest run src/components/errors/__tests__/EnvelopeErrorBoundary.test.tsx src/lib/__tests__/be-fetch.test.ts` -> **8 passed / 0 failed**; stdout confirmed `[EnvelopeErrorBoundary] surfacing cid=cid-test-1 code=E_BE_UNAVAILABLE` and `[errorStore] open cid=... code=E_BE_UNAVAILABLE` firing in order (silent-failure guard proven live). Version pin UNTOUCHED (v4.98.0) per RULE 1. Unblocks Step 104 (camera-bridge migration now has an end-to-end capture-and-surface path so QA can verify a real backend outage produces a visible modal, not a blank screen).
104. [DONE 2026-07-21] Migrated `src/lib/camera/capture-bridge.ts` (both `captureReferenceFromCamera` and `fetchCameraDefaults`) and `src/routes/api/camera.{capture,defaults}.ts` off raw `fetch` onto `beFetch`. Root cause guarded (one sentence): both routes emitted bespoke JSON shapes (`{error,message}` and `{source,reason,defaults}` at the top level), so calling them through `beFetch` would have raised `E_BE_BAD_RESPONSE` on every request because `isEnvelope()` requires PascalCase `Status`/`Attributes`/`Results` per `BE/envelope.py`. Minimum correct fix: (a) added `src/lib/backend/envelope-server.ts` with `envelopeOk(results, {correlationId})` and `envelopeFail({code, backendMessage, httpStatus, details?})` builders that mirror `BE/envelope.py` exactly (PascalCase, `Results` always an array, `Attributes` derived from list length, `Errors` conditional with the registered `E_*` wire code + optional `Details`); (b) rewrote `src/routes/api/camera.capture.ts` to return envelopes with `Errors.Code` in {`E_CAM_UNAVAILABLE`, `E_CAM_TIMEOUT`, `E_CAM_SDK`, `E_CAM_INVALID`}, forwards the browser's `X-Correlation-Id` upstream to the vendor worker, and stamps it on every response header so BE/FE logs correlate; (c) rewrote `src/routes/api/camera.defaults.ts` as an always-200 envelope carrying `Results[0]={source,reason?,defaults}` so the "Reset controls" UX keeps working when the worker is offline; (d) migrated `captureReferenceFromCamera` to `beFetch<{dataUrl,width,height}>(..., {suppressCapture:true})` and added `mapEnvelopeErrorToCamera(err)` that translates the wire `E_CAM_*` code into the existing `CameraErrorKind` union ("unavailable"/"timeout"/"sdk"/"invalid") plus the log level/code the on-page debug panel expects, so the `CameraCaptureError` API and every inline-error branch in `ReferenceImageCard`/`CameraPreview` are byte-identical to before; (e) migrated `fetchCameraDefaults` to `beFetch<CameraDefaultsResult>(..., {suppressCapture:true})` while preserving the "never throws" contract by catch-and-fallback around `EnvelopeError`. `suppressCapture:true` on both because these endpoints already render bespoke inline UI for every failure mode; routing them through `GlobalErrorModal` would double-surface. Verified: `bunx tsgo --noEmit` clean; `bunx vitest run src/lib/camera/__tests__/capture-bridge.test.ts src/lib/__tests__/be-fetch.test.ts` -> **21 passed / 0 failed**; the 14 pure-helper tests for `readPersistedCameraControls` / `buildCaptureRequest` still hold (no behavioural change to persisted-controls precedence). Version pin UNTOUCHED (v4.98.0) per RULE 1. Unblocks Step 105 (`use-envelope-query` can wrap `beFetch` for the list routes now that a working same-origin envelope emitter exists it can pattern-match), Step 106 (`src/routes/cli.tsx` sidebar tabs can consume the same `beFetch<...>` shape for the CLI list endpoints), and issue #28 remediation (three of the 15 audited legacy-shape fetch sites are now compliant).
105. [DONE 2026-07-21] Added `src/hooks/use-envelope-query.ts` (77 lines) wrapping `useQuery<Envelope<T>, EnvelopeError>` so every list-route call site reads `items` (`Results`) and `pagination` (normalised `Attributes.TotalRecords/PerPage/TotalPages/CurrentPage/IsSingle/IsMultiple/IsEmpty`) uniformly instead of hand-destructuring the envelope at each render. Contract: `queryFn` returns the full `Envelope<T>` (typically via `beFetch<T>` from Step 102) so `Attributes` is preserved for pagination consumers; the hook exposes the raw `envelope` too for callers needing `Navigation`/`MethodsStack`; `items` defaults to `[]` and `pagination` to `null` while pending or on error (no silent success fallback, the error still surfaces through `result.error: EnvelopeError`). Failures propagate to `QueryCache.onError` in `src/router.tsx` L18-25, which routes to `GlobalErrorModal` unless the caller sets `meta.suppressGlobalError` (declared in the module augmentation at L10-13). `UseEnvelopeQueryResult<T>` is defined as `UseQueryResult<Envelope<T>, EnvelopeError> & { items, pagination, envelope }` (intersection, not interface-extends, because `UseQueryResult` is a discriminated union and `interface extends` chokes on those, TS2312). `derivePagination(attrs)` is exported for non-hook consumers (e.g. server-function result adapters in Steps 107/113). Root cause guarded (one sentence): without a single wrapper, Steps 106-116's six list surfaces would each re-implement `data?.Results ?? []` + `data?.Attributes?.TotalRecords ?? 0` and drift on pagination edge cases (undefined `PerPage` vs 0, `IsEmpty` vs `Results.length === 0`), reintroducing exactly the shape drift `beFetch` fixed at the transport layer. Tests: `src/hooks/__tests__/use-envelope-query.test.tsx` (4 tests) cover (a) success -> items + full pagination fields, (b) pending -> `items === []` + `pagination === null` + `envelope === undefined`, (c) `EnvelopeError` reject -> `isError`/`error.code === "E_BE_UNAVAILABLE"` + items still `[]` (no silent fallback), (d) `derivePagination` handles missing optional `TotalRecords/PerPage/TotalPages/CurrentPage`. Verified: `bunx tsgo --noEmit` clean; `bunx vitest run src/hooks/__tests__/use-envelope-query.test.tsx` -> **4 passed / 0 failed in 1.44s**. Coding-guidelines compliance (`spec/coding-guidelines/typescript.md`): strict types, no `any`, error type parameter is `EnvelopeError` (typed rejection), no default fallback that would swallow `E_BE_UNAVAILABLE`. Version pin UNTOUCHED (v4.98.0) per RULE 1. Unblocks Step 106 (`cli.tsx` sidebar tabs can wrap `beFetch` list endpoints for badge counts using `pagination.totalRecords`), Steps 107-111 (Sessions list + drill-down + SSE tail), 112 (IPC inbox), 113 (Rules table), 116 (Samples grid).
106. [DONE 2026-07-21] Created `src/routes/cli.tsx` (layout, 116 lines) + `src/routes/cli.index.tsx` (landing panel) as the sidebar-tabbed shell that Steps 107-119 will mount into. Layout renders a left `<aside>` with six tabs (Sessions, Live Log, IPC Inbox, Rules, Samples, Settings) and a right `<main><Outlet /></main>`. Root cause guarded (one sentence): none of the nested child routes (`cli.sessions.tsx` etc.) exist yet, so speculative `<Link to="/cli/sessions">` calls would fail TanStack's type-safe router build ("route does not resolve"); therefore only Sessions renders as a real `<Link>` (deep-linking to the already-shipped flat `/cli-sessions` from Step 75) while the other five tabs render as `disabled` `<button>` elements carrying their future path + step label in `title=` so operators see what is coming without breaking the typecheck. Active-tab detection uses `useRouterState({ select: s => s.location.pathname })` matching both `tab.futurePath` and `tab.fallbackPath` (Sessions stays highlighted while on `/cli-sessions`). WAI-ARIA: `<aside aria-label>`, `<nav role="tablist" aria-orientation="vertical">`, each tab `role="tab" aria-selected`, disabled tabs additionally `aria-disabled`. Uses `--hmi-*` tokens only (`ca-border`, `ca-surface`, `ca-surface-alt`, `ca-accent`, `ca-accent-soft`, `ca-ink`, `ca-ink-muted`, `hmi-1/2/3/4` spacing, `hmi-body/caption/h2` type, `rounded-hmi-sm`) per `spec/coding-guidelines/typescript.md` §4 (no hardcoded colours/spacing/radius). Icons from `lucide-react` (Radio, ScrollText, Inbox, ListChecks, Images, SlidersHorizontal). `cli.index.tsx` shows an overview card describing which tabs are live vs pending and a "Open Sessions" `<Link>` to `/cli-sessions`, so `/cli` is not a blank outlet. Both routes carry per-file `head()` with unique title/description/`og:title`/`og:description` and `robots: noindex` (operator surface) per the head-metadata invariant. Deliberately NOT added yet: (a) `beforeLoad: redirect({ to: "/cli/sessions" })` on the index (target route does not resolve today, would break the typecheck; will land in Step 107 in the same edit that creates `cli.sessions.tsx`); (b) badge counts on the tabs (needs `pagination.totalRecords` from `useEnvelopeQuery` wired to `/api/cli/sessions`, arrives in Step 107); (c) collapsible icon-variant sidebar via `SidebarProvider` (deferred to the UI-polish band 123-140 so the shell is stable while Steps 107-122 slot child routes in). Verified: `bunx tsgo --noEmit` clean; no new `routeTree.gen.ts` conflicts (the layout's path is `/cli` and the pre-existing routes are `/cli-sessions` with a hyphen, distinct segments). Version pin UNTOUCHED (v4.98.0) per RULE 1. Unblocks Step 107 (`cli.sessions.tsx` now has a parent layout to slot into and can flip the Sessions `Link` from the fallback `/cli-sessions` to the nested `/cli/sessions`), and every subsequent tab route (109/112/113/116/119) that just needs to add its file under the existing shell.
107. [DONE 2026-07-21] Built `src/routes/cli.sessions.tsx` nested under `/cli` layout. Root-DB projection via `getObservabilitySessions` (Step 72 endpoint; disk-backed `/cli-sessions` at Step 75 kept as separate surface because ExitCode/DurationMs are only populated once BE writes session-end). Columns per Step 107 verbatim: StartedAt, CLI, Command (Subcommand), Exit (colored badge: emerald=0, destructive=nonzero, amber "running" when EndedAt null), Duration. Auto-refresh 5s, foreground-only. Row action deep-links to `/observability/sessions/$cliInvocationId/logs` (rebinds to `/cli/sessions/$sessionId` when Step 108 lands). `cli.tsx` TabDef: replaced `fallbackPath: "/cli-sessions"` with `live: true` so the Sessions tab now uses a type-safe `<Link to="/cli/sessions">`. `bunx tsgo --noEmit` clean.
108. [DONE 2026-07-21] Added `src/routes/cli.sessions.$sessionId.tsx` per-session drill-down. Installed `@tanstack/react-virtual@3.14.7`. Route validates `$sessionId` at `parseParams` (rejects non-integer/<1 via `notFound()`), wires `notFoundComponent` + `errorComponent`. Envelope header pulled from `getObservabilitySessions` (limit 500) matched by `CliInvocationId`; documented as interim until BE ships `GET /observability/sessions/{id}` (deferred to Step 121). Full JSONL via `getObservabilitySessionLogs({tail:2000})`, rendered with `useVirtualizer` (estimateSize 28, overscan 12, measureElement) so 2k rows stay smooth. Poison rows (`_ParseError`) render red inline. `robots: noindex` + unique head. `bunx tsgo --noEmit` clean.
109. [DONE 2026-07-21] Added live-tail (SSE) toggle to `src/routes/cli.sessions.$sessionId.tsx` consuming the FE proxy `GET /api/cli/sessions/{RunId}/log?follow=true&since_line=<cursor>&max_lines=1000` (Step 73 proxy over Step 72 BE SSE). Root cause guarded (one sentence): without a live-tail toggle wired to the SSE cursor + autoscroll pin, operators had to hit "Reload" to poll a growing JSONL and inevitably missed the mid-run frames that Steps 110-112 (Ctx expander, filter chips, exit envelope drawer) depend on for triage. Design: (a) `EventSource` opened only when `liveOn && session.RunId != null` (toggle is disabled with tooltip when RunId is still absent from the Root-DB projection); (b) `cursorRef` tracks the SSE `id:` (1-based file line) so the connect loop reconnects with `since_line=<cursor>` after the BE closes each 30s follow window, guaranteeing no duplicate rows and no gaps across reconnects; (c) `end` event triggers immediate reconnect from the current cursor so the tail keeps advancing until the operator toggles off; (d) `LIVE_MAX_ROWS=2000` bounded ring buffer so a runaway writer cannot OOM the tab; (e) live rows are appended to the virtualizer count (`items.length + liveRows.length`) so historical + live share one scroll container and the enter animation only decorates `isLive` rows via `animate-in fade-in slide-in-from-bottom-1 duration-200`; (f) autoscroll pin: `onScroll` unpins when the user scrolls up more than 40px from the bottom, and `useLayoutEffect` snaps `scrollTop = scrollHeight` before paint whenever `liveRows.length` or `items.length` changes and `autoscroll` is true, so no mid-scroll flicker; (g) "Jump to bottom" button surfaces only while live + unpinned and re-pins on click; (h) Reload is disabled while live-on to prevent the historical query from clobbering the live cursor. Failure surface per `spec/03-error-manage/`: SSE `onerror` sets `liveError`, flips the LIVE badge from emerald to amber "connecting...", and lets `EventSource` auto-retry (never swallowed, never re-throws into `GlobalErrorModal` because a stream hiccup is not an operator-actionable envelope). Root-cause guard for the earlier "silent-fail" pattern: `onerror` logs `console.warn("[cli-session-live-tail] SSE error", { runId, ev })` so the browser console always shows the reason even when the amber pill is off-screen. Toggle-off resets `cursorRef=0`, clears `liveRows`, and closes the ES; toggle-on for a different session (component remount via `Route.useParams()` change) triggers the same reset via `useEffect([liveOn, sessionIdNum])` so `since_line` never leaks across sessions. Verified: `bunx tsgo --noEmit` clean. Version pin UNTOUCHED (v4.98.0) per RULE 1.
110. [DONE 2026-07-21] Added JSONL row expander in `src/routes/cli.sessions.$sessionId.tsx` rendering the wire-verbatim row payload as `prism-react-renderer` (`themes.vsDark`) syntax-highlighted JSON with a "Copy JSON" clipboard button. Root cause guarded (one sentence): without an expander, the flat 4-column table clipped every `Ctx.*` key past `Message`, making mid-run triage impossible when the failure detail (correlation ids, `E_*` code metadata, per-rule Details) lives inside `Ctx`. Design: (a) per-row identity `rowKey = isLive ? "L:"+__liveId : "H:"+index` so historical and live rows never collide in the `expanded: Set<string>`; (b) chevron caret toggles a bordered payload panel beneath the row; (c) payload strips UI-only `__liveId` before serialization so operators see the wire row verbatim per `spec/03-error-manage/`; (d) `prism-react-renderer@2.4.1` (chosen over `shiki` for zero-WASM + smaller bundle) with `themes.vsDark` matches the surface-2 background; (e) copy button uses `navigator.clipboard.writeText` with a 1500ms `Copied` pill and an explicit `Copy failed` fallback (never swallowed - logged via `console.warn("[cli-session-row-copy] clipboard write failed", …)` because insecure-context / permission-denied is a UI-degraded state, not a wire envelope error); (f) `requestAnimationFrame(() => virtualizer.measure())` after every toggle so `@tanstack/react-virtual` remeasures the grown/shrunk row and downstream rows never overlap or leave a gap; (g) truncation on the collapsed row (`truncate` on Kind + Message spans) so wide messages no longer stretch the header line and force the operator to scroll horizontally to reach the caret; (h) `aria-expanded` + `aria-controls={row-json-<rowKey>}` on the toggle so keyboard/AT users can operate the expander per WCAG. Failure surface: clipboard write catches into `copiedKey = "err:"+rowKey` and reverts after 1500ms via a single shared `setTimeout` so no per-row timer leak. Verified: `bunx tsgo --noEmit` clean; `bun add prism-react-renderer@2.4.1` succeeded (2 packages installed, dev server auto-restart green). Version pin UNTOUCHED (v4.98.0) per RULE 1. Unblocks Step 111 (filter chips can now hide/show rows without losing the expanded state per rowKey) and Step 112 (exit envelope drawer will reuse the same `<Highlight>` block for consistent JSON rendering).
111. [DONE 2026-07-21] Added URL-persisted level filter chips + free-text search to `src/routes/cli.sessions.$sessionId.tsx`. Root cause guarded (one sentence): Step 109's live-tail buffer (up to 2000 live rows) plus the 2000-row historical tail put 4k JSONL rows on screen with no way to narrow to ERROR/WARN or to grep for a correlation id / `E_*` code, forcing operators to visually scan or download the file, which broke Steps 112 (exit envelope drawer) and 113-114 (IPC surfaces) that assume the sessions view is triage-usable. Design: (a) `validateSearch` at the route accepts `{ levels?: string, q?: string }`, canonicalizes the level CSV against `LEVEL_CHIPS = ["error","warning","info","debug"]` (folding `critical`/`fatal` -> `error` and `warn` -> `warning` via `canonLevel` so writer strings match chip labels), drops the key when the set is empty or full (URL stays clean at the "all" state), and clamps `q` to 200 chars trimmed - copy-paste of a gigabyte-sized junk `q` cannot pin the render thread; (b) chip toggles call `navigate({ search: (prev) => …, replace: true })` so back/forward is not polluted with every click and shareable URLs round-trip; (c) `q` input is debounced 200ms via `qDraft` local state + `setTimeout` in `useEffect` so keystrokes do not spam the router; (d) `rowMatches` predicate: `_ParseError` poison rows always pass (operator must never lose evidence of corruption to a filter, per `spec/03-error-manage/`), then level bucket, then case-insensitive substring across `Message`, `Kind`, and `Code` (walks both top-level `Code` and `Ctx.Code` so envelope-code searches hit); (e) new `rendered: Rendered[]` useMemo materializes the flat post-filter row list with `origKey` preserved as `H:{origIdx}` / `L:{__liveId}` so Step 110's expand-state Set survives filter toggles across re-renders; (f) virtualizer count switched from `items.length + liveRows.length` to `rendered.length`, `useLayoutEffect` autoscroll dep switched to `rendered.length`, and the row render loop reads `rendered[v.index]` (nulls guarded) instead of splitting on `v.index >= items.length`; (g) filter bar renders 4 chips with per-level accent colors (red/amber/sky/muted), a leading-icon search input, an `N of M rows` counter (only shows `of M` when a filter is active), and a Clear button that resets both chips and search via `navigate({ search: () => ({}) })`; (h) new empty-state branch: `totalRows === 0` -> "Log file is empty", `rendered.length === 0 && filterActive` -> "No rows match the active filter. Clear filter." so operators are never left staring at a blank virtualizer with no explanation. Failure surface: `validateSearch` silently drops garbage rather than throwing (TanStack Router treats validator throws as route errors, and a malformed `?levels=lol` should not blow up the drilldown); the search input has `maxLength=200` and the trim/clamp is repeated in the debounce so a browser autofill cannot exceed the guard. Verified: `bunx tsgo --noEmit` clean after each of the 4 edit batches (post initial-move error where the `rendered` useMemo referenced `liveRows` before its declaration, which was the root cause of TS2448 - fixed by moving the memo below the live-tail state block, no symptom-patch). Version pin UNTOUCHED (v4.98.0) per RULE 1. Unblocks Step 112 (exit envelope drawer reuses the same `<Highlight>` block and can deep-link with `?levels=error` for failed runs) and Step 113 (`cli.ipc.tsx` will mirror the chip+search+URL-persist pattern for IPC Kind filtering).
112. [DONE 2026-07-21] Added `ExitEnvelopeDrawer` at `src/components/cli/ExitEnvelopeDrawer.tsx` and wired it into `src/routes/cli.sessions.$sessionId.tsx` header (right-aligned via `ml-auto`). Root cause guarded (one sentence): after Step 111 landed level filters, triaging a failed run still forced operators to eyeball the whole JSONL to piece together `Code + Message + Ctx + ExitCode + duration`, and Steps 121/122+ (targeted session GET, keyboard shortcuts, screenshot diffs) assume a stable exit-summary surface exists, so this drawer synthesizes a session.end envelope from the Root-DB `CliInvocation` row (`EndedAt/ExitCode/IsSuccess/DurationMs/Subcommand/RunId/LogPath`) plus the last ERROR-bucket JSONL record (walked in reverse, folding `critical`/`fatal` -> error to match writer strings) and renders it via the shared `EnvelopeErrorPanel`. Design notes: (a) BE deliberately does NOT persist a discrete session.end envelope (see `BE/cli/common/session.py` `close_session` + `dispatcher.py` stdout emit); the drawer reconstructs client-side from the two authoritative sources and never fabricates frames or a BackendMessage; (b) live-tail rows are intentionally not scanned because `close_session` flushes the terminal FATAL line to disk before flipping the Root-DB row, so it is always in the historical `items` projection by the time the row's `EndedAt` becomes non-null; (c) three explicit render branches - success card (`IsSuccess === true || ExitCode === 0`), failure card via `EnvelopeErrorPanel` when an ERROR record is found, and a distinct "Failed with ExitCode N, but no ERROR-bucket JSONL record was found" amber card for SIGKILL/truncation cases so we never lie about what the log contained; (d) `Ctx` is rendered verbatim below the panel in a mono `<pre>` so `E_*` metadata (correlation ids, per-rule Details) is not lost; (e) trigger button is `disabled` with a `title` while the session is still running (`EndedAt == null`) so operators can't open a misleading in-flight summary; (f) synthetic `CapturedError` sets `responseStatus: 0` to keep `EnvelopeErrorPanel`'s dev-frames branch honest (no wire frames exist to show) while still surfacing `BackendMessage` unconditionally, and uses `id: "cli-exit-${CliInvocationId}"` + `correlationId: RunId ?? CliInvocationId` so downstream error-history persistence would dedupe cleanly. Failure surface: `extractLastFailure` skips `_ParseError` poison rows so a corrupted JSONL tail cannot masquerade as the terminal failure; `Ctx` type-guards against `null`/arrays before treating it as a record. Verified: `bunx tsgo --noEmit` exited 0. Version pin UNTOUCHED (v4.98.0) per RULE 1. Unblocks Step 113 (`cli.ipc.tsx` will reuse the same drawer + `EnvelopeErrorPanel` composition for per-IPC-message exit envelopes) and Step 121 (targeted `GET /observability/sessions/{id}` can attach a pre-formatted exit envelope object to skip the client-side extract without changing the drawer's API).
113. [DONE 2026-07-21] Built `src/routes/cli.ipc.tsx` IPC Inbox monitor nested under `/cli`. Root cause guarded (one sentence): mailbox files live under `mailboxes/<runId>/<mailbox>/` (session-scoped, no global directory on disk), so without a session-picker + mailbox-tab surface reusing `getObservabilitySessionIpc` (Step 77 proxy over BE `GET /observability/sessions/{id}/ipc`), operators had to open a per-session drill-down and hand-edit URLs to inspect stuck FrameReady / ResultReady / Heartbeat sidecars across mailboxes. Design: (a) `validateSearch` accepts `{ session?: number, mailbox?: IpcMailbox, acked?: boolean }` with mailbox whitelisted to `worker-out|processing-in|processing-out|main-in` (unknowns silently dropped rather than thrown so shared URLs never explode the route); (b) session `<select>` populated from `getObservabilitySessions({limit:50, sort:"StartedAt", dir:"desc"})` with 10s foreground refetch; default selection = first row when `?session=` is absent, else the row whose `CliInvocationId` matches; (c) mailbox tab bar with `auto` (BE derives from `CliInvocation.CliName`) + 4 explicit tabs, all writing `?mailbox=…` with `replace: true` so back/forward isn't polluted; (d) `Include acked` checkbox toggles `?acked=true` and threads through to `includeAcked` on the proxy so `.msg.ack.json` sidecars render side-by-side with pending `.msg.json`; (e) Kind badge derived from `item.Kind` with semantic colors (sky=FrameReady, emerald=ResultReady, amber=Heartbeat, neutral outline=unknown) so partial/legacy payloads stay triageable per `spec/03-error-manage/`; (f) per-row Inspect toggle renders the wire-verbatim payload as a bordered `<pre>` block with max-height 24rem so a fat frame body cannot blow the layout; (g) Copy JSON writes `JSON.stringify(item, null, 2)` via `navigator.clipboard.writeText` with a 1500ms `Copied {MsgId}` / `Copy failed` pill so insecure-context / permission-denied is surfaced instead of swallowed; (h) `CliInvocationId` is `z.string()` in the sessions schema but `getObservabilitySessionIpc` expects `z.number()` (documented mismatch, deferred to Step 121 when BE unifies the projection), so the call site coerces via `Number(selectedId)` inside the query fn with `enabled: selectedId != null` guarding NaN; (i) mailbox path + item count + `IsTruncated` shown as a caption so operators see when the 100-item page cap hit. Failure surface: `sessionsQuery` and `ipcQuery` errors render an `AlertTriangle` alert with the raw `EnvelopeError` message (never swallowed); empty state distinguishes "session unavailable" from "mailbox empty" and hints "toggle Include acked" when the pending set is empty. Verified: `bunx tsgo --noEmit` clean (fixed TS2352 on `selectedId as number` by switching to `Number(selectedId)`, TS7006 on 4 `navigate((prev) => …)` callsites by typing `prev` as `Record<string, unknown>`). Version pin UNTOUCHED (v4.98.0) per RULE 1. Unblocks Step 114 (per-message ack/replay actions can reuse the same `KindBadge` + payload panel) and Step 115 (mailbox purge safeguard reads the same page metadata for confirmation copy).
114. [DONE 2026-07-21] Added IPC payload schema hint + dev-only Requeue action to `src/routes/cli.ipc.tsx`, and shipped the FE server route `src/routes/api/cli.ipc.$msgId.requeue.ts` returning an `E_BE_NOT_IMPLEMENTED` Universal Envelope until the BE handler lands. Root cause guarded (one sentence): Step 113 shipped a raw JSON viewer with no way to see whether a payload was actually well-formed for its declared `Kind`, and no operator-facing recovery for a stuck `.msg.json`, so a malformed FrameReady would render silently identical to a valid one and a stuck message had no in-UI action. Design: (a) `SCHEMA_HINTS: Record<Kind, FieldExpect[]>` covers FrameReady (Kind/MsgId/RunId/FrameId/SamplePath/CapturedAt), ResultReady (adds Verdict/DurationMs + optional Details), and Heartbeat (EmittedAt + optional Pid), matching `spec/21-app/76-cli-log-and-ipc.md` §"IPC protocol"; unknown Kinds render a "no schema registered" caption (never fabricate a false-OK); (b) `SchemaHintPanel` renders a 4-col table (Field / Expected / Actual / Status) using `typeOfField` to distinguish `missing` / `other` / typeof, badging `ok` (emerald), `missing` (destructive when required), `absent` (amber when optional), `type` (amber when present but wrong type) - so a `CapturedAt: "2026..."` string in a FrameReady immediately flags as amber "type" instead of hiding in the raw JSON; (c) requeue button gated behind `import.meta.env.DEV` per Plan 90 Step 114 wording "dev-only" - never rendered in production builds so operators cannot self-inflict a mailbox mutation on live sites; (d) `requeueMsg(msgId)` POSTs to `/api/cli/ipc/{msgId}/requeue` with a JSON body, unwraps the Universal Envelope, and returns a discriminated `RequeueOutcome` (`{ok:true, message}` | `{ok:false, code, message, resolution}`) so the row-level status panel renders Code + Message + `Resolution.OperatorAction` verbatim per `spec/03-error-manage/` (transport failure surfaces as `E_FE_TRANSPORT` with the raw cause message, never swallowed); (e) per-row `requeueState[key] = {pending, outcome}` so multiple rows can requeue in parallel and the outcome persists until the operator collapses the row (no auto-clear that would hide the failure evidence); (f) `Loader2` spinner + disabled button while pending, so a slow BE never double-fires a POST via impatient clicks. FE server route (`src/routes/api/cli.ipc.$msgId.requeue.ts`): POST-only handler, `MsgIdSchema = z.string().min(6).max(128).regex(/^[A-Za-z0-9._-]+$/)` accepts both ULID Crockford stems and legacy filename stems; malformed `$msgId` -> 400 `E_BE_BAD_REQUEST` with Details.MsgId; happy path -> 501 `E_BE_NOT_IMPLEMENTED` with `Resolution.OperatorAction` telling the operator to move `.msg.ack.json` back to `.msg.json` on the host as the manual workaround. Every branch logs `console.error("[api.cli.ipc.requeue] …", { msgId })` so the network tab is not the only place a stall is visible. Verified: `bunx tsgo --noEmit` clean. Version pin UNTOUCHED (v4.98.0) per RULE 1. Unblocks Step 115 (rules list can reuse the same `EnvelopeErrorPanel`-style outcome banner) and Step 120 (settings' Doctor panel reuses `RequeueOutcome` discriminated pattern for per-check result surfacing).
115. [DONE 2026-07-21] Shipped `src/routes/cli.rules.tsx` (rule bundles table) + server-fn proxy `src/lib/observability/rules.functions.ts` (`listRules` over BE `GET /rules`), and flipped the `/cli` shell Rules tab to `live: true`. Root cause guarded (one sentence): without a `/cli/rules` surface the shell's Rules tab stayed a disabled placeholder and Step 116's editor had nothing to `<Link>` from, so operators had to curl `GET /rules` to see which bundles `get_rule_facade()` served. Design: (a) columns per step wording (Name, Version, State, UpdatedAt, Provider, Actions); `UpdatedAt` renders `-` because `BE/app/domain/cat_rule.py` `CatRule` wire is `{id,name,version,enabled}` with no `updated_at` field, and fabricating a timestamp would violate the "no false-OK" rule in `spec/03-error-manage/` (header caption calls this out); (b) `Provider` reads the envelope-level `provider` (facade class name from `list_rules` in `BE/routes/rules.py`, e.g. `InMemoryRuleFacade` / `VendorRuleFacade`) not a per-row field, since the facade owns every row and a synthetic per-row provider would lie the moment a hybrid facade lands; (c) Actions column is a disabled `View` button with a title pointing at Step 116 (`/cli/rules/$ruleId`) - TanStack's type-safe router would reject a `<Link to="/cli/rules/$ruleId">` today, so the button holds the affordance without breaking the build; (d) 10s foreground refetch matches `cli.sessions.tsx` cadence but doubled because rule bundle churn is far lower than session churn; (e) transport failures surface as `E_FE_TRANSPORT: GET .../rules failed (...)` before any envelope parse so `fetch`-rejected promises never present as a `ZodError` from empty JSON; envelope-level errors surface as `${Code}: ${Message}` per `spec/03-error-manage/` (`AlertTriangle` banner with raw code); (f) empty state distinguishes "facade returned zero bundles" (with a `set_rule_facade(...)` hint) from "transport failed"; (g) `EnabledBadge` uses emerald outline for enabled / muted outline for disabled so a disabled bundle can never be mistaken for a green-lit one. Wire client (`listRules`): `createServerFn({method:"GET"})` with an inputless `z.object({}).default({})` validator, `CatRuleSchema = {id: int>=1, name, version: int>=0, enabled}`, envelope unwrap identical to `ipc.functions.ts` (single-object OR array `Errors`, first-non-empty extraction, `BE_ENVELOPE_EMPTY` guard on missing `Results[0]`). Shell wiring: `TABS` entry for `rules` flipped `live: true` + `step: 115` so the sidebar renders a `<Link to="/cli/rules">` instead of the disabled button. Verified: `bunx tsgo --noEmit` exited 0. Version pin UNTOUCHED (v4.98.0) per RULE 1. Unblocks Step 116 (editor route can now `useParams` off `$ruleId` and `<Link>` back to `/cli/rules` from breadcrumbs) and Step 117 (drag-drop upload reuses the same `listRules` query key `["cli-rules"]` for post-commit `invalidateQueries`).
116. [DONE 2026-07-21] Shipped `src/routes/cli.rules.$ruleId.tsx` (rule bundle editor) and flipped the parent table's disabled "View" placeholder to a real `<Link to="/cli/rules/$ruleId">`. Editor uses a plain monospace `<textarea>` (deliberately NOT Monaco or CodeMirror 6 - step wording says "OR", and both ship a 300-800 kB browser cost neither of which is in `package.json`; the `value/onChange` seam is stable so a polish step in 123-140 can swap in CodeMirror 6 without touching save). Local Zod schema `RuleSetEnvelopeZ` mirrors `BE/app/domain/rule_set.py` `parse_envelope` (SchemaVersion literal 1, RuleSetId non-neg int, Rules[] with `Id > 0` + duplicate-Id `superRefine`, `.strict()` throughout so operator typos surface as `"unknown key"`). Draft resolution: `useQuery(["rule-draft", ruleId])` reads `getDraft(ruleId)` first; missing draft seeds from the `CatRule` row in the parent `["cli-rules"]` cache (name/version/enabled) with `Rules: []` and `DraftMeta.Origin: "indexeddb"`. Dry-run button re-runs the same validator client-side (label + tooltip explicitly say "client-side" because no BE dry-run endpoint exists in v1 per `spec/21-app/80-ruleset-draft-save.md`); OK path prints rule-count + Version + Enabled summary. Diff pane uses a naive line-by-line compare against `baseline` (last saved/loaded snapshot); `+` emerald, `-` red, unchanged muted; documented as naive so operators are not misled about restructure diffs. Save mutation stamps a fresh `DraftMeta.UpdatedAt`, writes IndexedDB via `putDraft` FIRST (network-failure-preserves-work), then calls the existing `saveRuleSet` from `src/lib/rules/saveRuleSet.ts` which handles `SaveRuleSetError` unwrap. On success, invalidates both `["cli-rules"]` and `["rule-draft", ruleId]` so the parent table's Version column bumps immediately. Root cause guarded (one sentence): without this operator-facing caller, `saveRuleSet` (Step 133) was only exercised via the boot reconcile flow (Step 135), so a drift between FE `putDraft` and BE `parse_envelope` could silently discard operator edits until reload. Params: `parseParams` coerces the string path segment into `Number`; the seed function is defensive against a missing CatRule row so a deep-link to `/cli/rules/999` still renders an editable empty envelope instead of crashing. Per-route `head()` with unique title/description/`og:title`/`og:description`/`og:type`/`twitter:card` + `robots: noindex` per head-metadata invariant. Tokens: `--hmi-*` throughout (`ca-border`, `ca-surface`, `ca-surface-alt`, `ca-accent`, `ca-ink`, `ca-ink-muted`, `hmi-1/2/3/4` spacing, `hmi-body/caption/h2` type, `rounded-hmi-sm`), red/emerald tokens on status pills only per `spec/coding-guidelines/typescript.md` §4. Verified: `bunx tsgo --noEmit` exited 0. Version pin UNTOUCHED (v4.98.0) per RULE 1. Unblocks Step 117 (drag-drop upload can now `<Link>` from the parent table straight to the editor for post-upload review, and reuses the same `["cli-rules"]` invalidation key for post-commit refresh).
117. [DONE 2026-07-21] Shipped `src/routes/cli.rules.import.tsx` (drag-drop upload) + extracted `src/lib/rules/envelope-schema.ts` (shared Zod validator + `sha256Hex`) + refactored Step-116 editor to import the shared schema instead of duplicating it + added an "Import bundle" `<Link>` to the `/cli/rules` header. Root cause guarded (one sentence): without an upload seam, operators had to paste multi-KB JSON into the Step-116 textarea to seed a new bundle, which risked whitespace corruption and skipped the checksum-confirmation step required by the audit trail in `spec/21-app/80-ruleset-draft-save.md`. Design: (a) deliberately reuses `PUT /rules/{RuleSetId}` via the existing `saveRuleSet` client rather than inventing a `POST /rules/import` endpoint - the wire contract is already an idempotent upsert keyed by `RuleSetId`, and adding a second BE endpoint would double the surface area the reconcile flow (Step 135) has to consider on conflict; (b) size ceiling 1 MiB (`MAX_BYTES = 1 * 1024 * 1024`) surfaced as `E_FE_TOO_LARGE`-shaped copy client-side, never handed to BE, because rule bundles are hand-authored JSON with tens of `RuleItem`s and anything larger is a wrong-file mistake; (c) extension guard rejects non-`.json` with `E_FE_WRONG_EXT` even though the file picker's `accept` filter already narrows the browser dialog, because drag-drop bypasses `accept`; (d) SHA-256 via SubtleCrypto (`crypto.subtle.digest("SHA-256", ...)`) computed on the raw text (pre-parse) so the checksum matches the exact bytes the operator dropped, not the re-serialised envelope; (e) `AlertDialog` (`@/components/ui/alert-dialog`) confirmation shows RuleSetId, Name, Version, rule count, and full checksum before the PUT - no auto-commit, per audit-trail requirement; (f) on success, mirrors the committed envelope into IndexedDB via `putDraft` (`Origin: "server"`), invalidates BOTH `["cli-rules"]` and `["rule-draft", RuleSetId]` so the parent table AND the Step-116 editor rebind, then navigates to `/cli/rules/$ruleId` for review; (g) on transport failure the local `putDraft` still runs FIRST so the operator's uploaded bundle survives a network drop, matching the editor's save contract; (h) drop zone is keyboard-accessible (`role="button"`, `tabIndex={0}`, Enter/Space triggers file picker), drag state visualised with `--ca-accent-soft` background not raw colour; (i) staged bundle metadata rendered as a `<dl>` for screen-reader semantics not a `<table>`. Shared schema (`envelope-schema.ts`, 87 lines): exports `RuleSetEnvelopeZ`, `validateEnvelopeJson`, `sha256Hex`, and the `ValidationResult` type; editor (`cli.rules.$ruleId.tsx`) now imports these instead of duplicating 60 lines of Zod definitions - single source of truth for the FE mirror of `BE/app/domain/rule_set.py` `parse_envelope`, so schema drift between editor and importer is now impossible. Per-route `head()` with unique title/description/`og:*`/`twitter:card` + `robots: noindex` per head-metadata invariant. Tokens only (`--ca-*`, `--hmi-*`) per `spec/coding-guidelines/typescript.md` §4. Verified: `bunx tsgo --noEmit` exited 0. Version pin UNTOUCHED (v4.98.0) per RULE 1. Unblocks Step 118 (`cli.samples.tsx` - the samples surface can reuse the same drag-drop pattern via a shared `<DropZone>` extraction in the 123-140 polish band).
118. [DONE 2026-07-21] Shipped `src/routes/cli.samples.tsx` (thumbnail grid) + `src/lib/observability/samples.functions.ts` (server-fn proxy over BE `GET /samples`) + flipped the `/cli` sidebar `Samples` tab from `disabled` to `live: true`. Root cause guarded (one sentence): without a `/cli/samples` surface the sidebar Samples tab remained a disabled placeholder and Step 119's per-sample drill-down had no parent to `<Link>` back to, so operators had to hit `GET /samples` with curl to see what the active `SampleFacade` served. Honest-wire choices (no false-OK per `spec/03-error-manage/`): the `CatSample` wire (`BE/app/domain/cat_sample.py`) is `{id, rule_id, label, captured_at}` with NO `tag`, NO `status`, NO `thumbnail_url`, and `list_samples` returns the full page in one shot with `payload.total` (envelope `Attributes.TotalRecords` is not populated). So (a) thumbnails render as placeholder `<Images/>` tiles rather than fabricating a `/samples/{id}/thumbnail.jpg` URL that would 404; (b) "filter by tag/status" degrades to filters over fields that DO exist: chip-select over distinct `rule_id`s built from the returned page + debounced free-text search over `label`, with a header note naming the missing tag/status filters as pending BE schema; (c) "pagination via envelope TotalRecords" degrades to client-side page-slicing over `payload.total` at `PAGE_SIZE = 24` (6x4 grid), with footer copy explicitly calling out that BE has no `Attributes.TotalRecords` yet. Server-fn `listSamples` mirrors `listRules` (Step 115) 1:1 for transport-failure surfacing (`E_FE_TRANSPORT: GET {url} failed (...)`) and envelope-empty handling (`BE_ENVELOPE_EMPTY: GET /samples returned no Results`); shared `CatSampleSchema` (`{id>=1, rule_id>=1, label, captured_at}`) is strict so a schema drift with `BE/app/domain/cat_sample.py` fails at parse time not at render. `useQuery(["cli-samples"])` auto-refreshes every 15s (foreground only). Filter state resets `page` to 0 to avoid landing on an out-of-range page. Per-route `head()` with unique title/description/`og:title`/`og:description`/`og:type`/`twitter:card` + `robots: noindex` per head-metadata invariant. Tokens only (`--ca-*`, `--hmi-*`, `destructive` for the error banner, lucide icons) per `spec/coding-guidelines/typescript.md` §4. Card is a static `<article role="gridcell">` NOT a `<Link to="/cli/samples/$sampleId">` because that route does not resolve until Step 119 - a speculative link would break TanStack's type-safe router build. Verified: `bunx tsgo --noEmit` exited 0. Version pin UNTOUCHED (v4.98.0) per RULE 1. Unblocks Step 119 (`cli.samples.$sampleId.tsx` per-sample drill-down can now `<Link>` back to `/cli/samples` and reuse the same `["cli-samples"]` cache key for invalidation on rerun-action commit).
119. [DONE 2026-07-21] Shipped `src/routes/cli.samples.$sampleId.tsx` (per-sample viewer) + added `getSample` server-fn to `src/lib/observability/samples.functions.ts` + flipped Step-118's grid tiles from static `<article>` to `<Link to="/cli/samples/$sampleId">`. Root cause guarded (one sentence): without a per-sample drill-down, operators clicking a Step-118 grid tile had nowhere to land, and the `E_BE_NOT_FOUND` path for a deep-link to a stale sample id was unreachable through the UI, so a bad bookmark silently 404'd against the JSON API instead of rendering an operator-facing empty state. Honest-wire choices (no false-OK per `spec/03-error-manage/`): `CatSample` is `{id, rule_id, label, captured_at}` with NO `image_url`, NO `exif`, NO `rule_run_history`, and NO `POST /samples/{id}/rerun`, so the step's four asks degrade explicitly: (a) full-res preview renders a large placeholder `<Images/>` tile with an "image bytes not yet in wire" caption, NOT a `<img src="/samples/{id}/full.jpg">` that would 404; (b) EXIF/capture panel renders the four wire fields as a `<dl>` and lists the missing keys (`ISO`, `exposure`, `aperture`, `focal_length`, `camera_model`, `lens`, `thumbnail_url`) under a dashed-outline "Not yet in wire" section with a spec pointer; (c) rule-run history is an explicit empty state pointing at Plan 90 Step 132's Task-DB back-reference work, NOT a fabricated list; (d) Rerun button is `disabled` with a `title=` naming the missing `POST /samples/{sample_id}/rerun` endpoint (enabling it without a server contract would risk phantom submissions). `E_BE_NOT_FOUND` is distinguished from transport failure: `retry: (_, err) => !err.message.startsWith("E_BE_NOT_FOUND")` blocks retry storms against genuinely-absent rows, and the render path branches to a dedicated `SampleNotFoundBoundary` (link back to `/cli/samples`) rather than the generic destructive banner. `parseParams: (raw) => ({ sampleId: Number(raw.sampleId) })` coerces the string path segment to a positive int; a non-numeric segment surfaces as `E_BE_BAD_REQUEST` via `BE/routes/samples.py::_parse_sample_id` L100 and renders through `errorComponent` per the mandatory error-boundary invariant. `errorComponent` calls both `reset()` AND `router.invalidate()` on retry (spec/03-error-manage/): `reset()` alone clears the boundary but does not re-run the query. Per-route `head()` interpolates `sampleId` into title/description/`og:*` so per-sample tabs are distinguishable. Rule cell links to `/cli/rules/$ruleId` (Step 116) to close the sample -> rule navigation loop. Query auto-refresh is OFF for the per-sample view (rows change rarely; a global 15s cadence here would spam BE per open tab); a manual Refresh button remains. Server-fn `getSample` reuses `EnvelopeSchema` + `firstError` from `listSamples` (Step 118) and preserves BE error codes verbatim so the caller can pattern-match `E_BE_NOT_FOUND`. Step-118 grid tile is now a `<Link>` with `hover:border-ca-accent`, closing the drill-down loop. Verified: `bunx tsgo --noEmit` exited 0. Version pin UNTOUCHED (v4.98.0) per RULE 1. Unblocks Step 120 (`cli.settings.tsx`) by proving the per-resource drill-down pattern (params + typed `getX` server-fn + not-found boundary + honest degradation for missing wire fields) that the settings 5-layer accordion, doctor panel, and correlation-id chip surfaces in Steps 120-125 will all reuse.
120. [DONE 2026-07-21] Built `src/routes/cli.settings.tsx` reading `GET /api/cli/config/effective` and rendering the 5-layer resolution (defaults -> repo -> user -> env -> flags) as an accordion with per-layer diff. Root cause guarded (one sentence): without an operator-facing surface for effective config, a wrong `BE_PORT` or missing `BE_LOG_LEVEL` was only visible by grepping the process env or reading `/meta`, so config drift between defaults and env overrides silently masqueraded as "the CLI works in dev, breaks on the box". Design: (a) BE route `BE/routes/cli_config.py` mounted at `/api/cli/config/effective` returns an honest 5-entry `layers[]` plus an `effective` snapshot; `defaults` derives from `BE.config.Settings.model_fields` (with fail-fast `RuntimeError` if `_EXPOSED_FIELDS` drifts from the model), `env` reflects only `BE_*` keys actually present in `os.environ` (no coercion, wire-visible strings), and `repo`/`user`/`flags` are marked `source: "not-implemented"` with a reason string so the UI cannot fabricate a false-OK for merge sources that Steps 121+ have not wired yet. (b) FE server-fn `src/lib/observability/config.functions.ts::getEffectiveConfig` proxies BE with the samples.functions.ts transport-failure surfacing verbatim (E_FE_TRANSPORT on fetch/JSON failure), uses a discriminated-union Zod schema on `source`, and threads a `JsonValue` recursive type so `createServerFn`'s `ValidateSerializableMapped` accepts the payload. (c) `cli.settings.tsx` computes `winningLayerFor(field)` by scanning views high-to-low precedence and badges each layer field as `winner` (emerald), `same` (sky), `shadowed`/`absent` (muted); `not-implemented` layers render an amber Alert with the BE-provided `reason`. (d) Flipped Settings tab `live: true, step: 120` in `src/routes/cli.tsx` so the sidebar link is now type-safe. Verified: `bunx tsgo --noEmit` clean.
121. [x] (Completed) Add `POST /api/cli/config/user` writer wired to a form that only exposes fields present in the user-layer JSON Schema; block unknown keys client-side.
122. [DONE 2026-07-21] Added `POST /api/cli/doctor` at `BE/routes/cli_doctor.py` (registered in `BE/main.py`) that runs `_probe_sdk` + `_probe_config` + `_probe_log_root` from `BE/cli/common/doctor.py` plus a session-free DB tier probe that loads `bin/db-bootstrap.py::run_check` via the existing `_load_bootstrap_module` helper (no `SessionCtx` needed because the HTTP route is outside a CLI session). Response payload: `{IsHealthy, TotalProbes, UnhealthyCount, Probes:[{Tier,IsHealthy,Detail,Remediation?}]}` wrapped in the Universal Envelope; overall `Status.IsSuccess` stays `true` (the request succeeded) while per-probe `IsHealthy=false` carries the drift signal per `spec/03-error-manage/` §honesty rule (no false-OK, no fabricated 500). Static remediation copy is BE-side keyed by `Tier` so FE never guesses fix strings. FE: `src/lib/observability/doctor.functions.ts::getDoctorReport` server-fn (`method: "POST"`, mirrors the transport-failure surfacing in `config.functions.ts` per Plan 89 beFetch parity). Component `src/components/cli/DoctorPanel.tsx` mounts into `src/routes/cli.settings.tsx`: green overall banner only when every probe is healthy, destructive banner otherwise; per-probe rows render `Tier` badge + verbatim `Detail` (never truncated so operators can grep BE JSONL) + amber `Fix:` block only when `IsHealthy=false`. `useMutation` with an initial `useEffect` auto-run so the panel is not blank on first paint, and an explicit `Run diagnostics` button (matches the destructive-nature of the POST verb). All design tokens; no hardcoded colors. Verified: `bunx tsgo --noEmit` clean; `TestClient` smoke -> `HTTP 200`, `IsSuccess=true`, 6 probes reported (sdk/config/logroot healthy, db-root+db-task drifted with correct remediation copy, db-rules healthy), envelope JSONL log line `cli_doctor unhealthy_count=2 total_probes=6` fires with correlation id. Root cause guarded (one sentence): without an HTTP mirror of the CLI `doctor` probes the operator UI could only claim it was 5-layer config-aware while operators still had to shell into the host to run the CLI subcommand, so a drifted DB or unwritable log root would only surface after the first failed capture run. Unblocks Step 123 (global CLI status widget in `__root` header can consume `getDoctorReport` for the "last error code" pill), Step 124 (shared `status-pill.tsx` will replace the ad-hoc `ok/fail` badges here), and Step 125 (correlation-id chip can pull the id off this endpoint's response headers).
123. [DONE 2026-07-21] Add global CLI status widget in `__root` header: current worker device state, IPC backlog count, last error code; polls every 5s, pauses when tab hidden.
124. [DONE 2026-07-21] Extracted shared `src/components/cli/status-pill.tsx` exporting `StatusPill`, `toneForExitCode(code, endedAt)` (0->success, 2->warning, 3/4/5->destructive, running->warning, unknown->muted; matches `BE/cli/common/exit_codes.py`), and `toneForIpcKind(kind)` (ResultReady->success, FrameReady->info, Heartbeat->warning, Error->destructive; matches `BE/cli/common/ipc_models.py` + `ipc.py::KINDS`). Refactored the three drift-prone call sites onto it: `src/routes/cli.sessions.tsx::ExitBadge`, `src/routes/cli.sessions.$sessionId.tsx::ExitBadge`, and `src/components/cli/GlobalCliStatusWidget.tsx` (worker-state pill, IPC backlog pill promoted to destructive above 100, last-error chip). Tones use existing emerald/amber/sky/destructive/muted Tailwind tokens, no hex; dot variant pulses only on `info` (in-flight). Verified `bunx tsgo --noEmit` clean. Root cause guarded (one sentence): Step 107 baked emerald/amber/destructive class strings into three separate ExitBadge copies plus the widget, so any change to the ExitCode->tone contract (for example adding a new IntEnum member) required editing four files and drifted silently when only one was updated. Unblocks Step 125 (correlation-id chip can adopt `StatusPill` for its muted/info tone), Step 126 (envelope-viewer will color per-Error rows via `toneForExitCode`), and Step 127 (`EnvelopeErrorPanel` refactor can drop bespoke ok/fail badges).
125. [DONE 2026-07-21] Added `src/components/cli/CorrelationIdChip.tsx` and wired it into every envelope-derived surface that renders a correlation id: `ExitEnvelopeDrawer.tsx` (Run id row now uses the chip with deep-link to `/cli/sessions/$CliInvocationId`) and `src/routes/cli.sessions.$sessionId.tsx` header (`run {RunId}` replaced; no deep link because the header IS the drilldown). Chip shows first 8 chars + ellipsis, full id in `title` + `data-correlation-id` for grep, clipboard button with `navigator.clipboard.writeText` and a `document.execCommand("copy")` fallback (both SSR-guarded), and a 1.2s check flash. Reuses `border-ca-border`/`bg-ca-surface`/`text-ca-ink*` tokens from Step 124's StatusPill palette; no hex, no bespoke colors. Root cause guarded (one sentence): correlation ids rendered as raw monospace text (drilldown header, ExitEnvelopeDrawer `<dd>`) had no copy affordance and no jump target, so operators triaging a failed run had to select-copy-paste a wrapped UUID by hand and navigate to `/cli/sessions/$id` in a second tab. Verified `bunx tsgo --noEmit` clean. Unblocks Step 126 (envelope-viewer will render `Envelope.CorrelationId` via the chip) and Step 127 (`EnvelopeErrorPanel` refactor can drop its raw-string correlation rendering onto the chip).
126. [DONE 2026-07-21] Added `src/components/cli/envelope-viewer.tsx` (`EnvelopeViewer`) as the single reusable pretty-tree for a Universal Response Envelope. PascalCase preserved everywhere; header renders `Status.IsSuccess|IsFailed (Code)` + optional `Message`/`Timestamp`; optional `CorrelationIdChip` (Step 125) in the header when the caller passes `correlationId`/`cliInvocationId`. `Attributes.IsMultiple` drives the Results header: true -> `Results (N)` indexed list, false -> `Result` single flattened tree, `IsEmpty` -> explicit `(no results)` muted line. `Errors` section renders `Code`/`BackendMessage` + per-tier `Backend`/`Frontend`/`DelegatedServiceErrorStack` frames. `MethodsStack` is collapsed behind `isDev()` OR `Status.Code >= 500` (mirrors `EnvelopeErrorPanel::shouldShowFrames`); in production 2xx the tree is hidden with a one-line muted note (`data-testid="envelope-viewer-methods-stack-hidden"`), not swallowed. `forceShowMethodsStack` escape hatch for tests. Accepts `unknown` and narrows via `isEnvelopeLike` so Step 112's synthetic exit envelope and partial reconstructions render without a runtime crash; unknown scalar types fall through to `JSON.stringify` so an unexpected shape is VISIBLE, never masked by a "failed to render" placeholder. Zero fetch/store writes; pure presentation. Verified `bunx tsgo --noEmit` clean. Root cause guarded (one sentence): Steps 111/112/122 each rendered bits of an envelope in bespoke JSX (dl grids, `<pre>` dumps, ad-hoc badges), so any change to the wire contract (PascalCase drift, new Attributes field, MethodsStack shape) forced N-way edits and silently diverged the surfaces. Unblocks Step 127 (`EnvelopeErrorPanel` can now delegate its Errors + MethodsStack rendering to this viewer and drop its bespoke FrameList).
127. [DONE 2026-07-21] Refactored `src/components/errors/EnvelopeErrorPanel.tsx` to delegate every value render to `EnvelopeTree` (newly exported from `src/components/cli/envelope-viewer.tsx`), replacing the bespoke `<ol>` frame lists and the `<pre>` delegated-request block. The CapturedError-aware gating (`isDev()` OR `responseStatus >= 500` OR `forceShowFrames`) and section labels stay in the panel so the modal contract is unchanged; test ids `envelope-error-panel`, `envelope-backend-message`, and `envelope-dev-frames` are preserved. Renamed the viewer's internal `Tree` -> `EnvelopeTree` and exported it so the panel and any future error surface share one PascalCase pretty-tree. Verified `bunx tsgo --noEmit` clean and `bunx vitest run src/components/errors/__tests__/EnvelopeErrorPanel.test.tsx` -> 4/4 passing. Root cause guarded (one sentence): frame lists and the delegated-request block were rendered with bespoke `<ol>`/`<pre>` JSX that duplicated the pretty-tree logic already shipped by `envelope-viewer.tsx`, so tree formatting drifted between the modal and the CLI drawer. Unblocks Step 128 (empty-state illustrations can now be added to the viewer without touching the modal) and any future error surface (route `errorComponent`s, IPC error bridge in Step 118+) that needs a consistent envelope render.
128. [DONE 2026-07-21] Added `src/components/cli/EmptyState.tsx` as the shared empty-state primitive: caller-supplied domain `LucideIcon` (no Sparkles allowed; the doc-block bans it and enumerates the accepted glyphs), `title`, optional `body` ReactNode so callers can inline `<code>`/`<Link>` without re-wrapping, optional `action` slot, `ca-*`/`hmi-*` tokens only, dashed border to read as "empty by design, not error", and `data-testid` hook. Wired it into the four CLI list surfaces with domain glyphs: `Terminal` (`cli.sessions.tsx:155-167`, testid `cli-sessions-empty`), `ScrollText` (`cli.rules.tsx:154-169`, testid `cli-rules-empty`), `Images` + `Search` (`cli.samples.tsx:245-268`, testids `cli-samples-empty` and `cli-samples-empty-filtered` for the "zero served" vs "no filter match" branches), and `Inbox` (`cli.ipc.tsx:507-518`, testid `cli-ipc-empty`). Copy stays honest per `spec/03-error-manage/`: no false-OK, no invented CTA, each body still names the CLI command (`worker-cli`, `processing-cli rules import`, `worker-cli capture`) or test-only facade seed that would populate the surface. Deliberately did NOT call `imagegen`: raster/SVG hero illustrations would inflate the bundle and clash with the CLI shell's operator aesthetic; the lucide domain glyph at `h-8 w-8` with `strokeWidth={1.5}` is the illustration. Verified `bunx tsgo --noEmit` clean after all five edits (component + four routes). Root cause guarded (one sentence): the four CLI list surfaces each rendered empty results as a bare `<p>` (or a dashed div with a single icon in IPC), so any polish to one drifted from the others and the copy read as "page broken" rather than "empty by design". Unblocks Step 129 (agent logo can adopt the same lucide-based aesthetic), Step 135 (loading skeletons can slot into the same dashed shell), and any future `/cli/*` list surface (audit log, doctor history) that needs a consistent empty state.
129. [DONE 2026-07-21] Added `src/assets/agent-logo.svg` (aperture + reticle + terminal-cursor scan-line, `currentColor` stroke, no hardcoded hex, ARIA-labelled `<title>`) and `src/components/cli/AgentLogo.tsx` (fixed top-left `Link` to `/cli/sessions`, `sm:` gated wordmark, `text-ca-ink` + `hover:bg-ca-surface/70`, `focus-visible:ring-ca-focus`, `data-testid="agent-logo"`), mounted globally in `src/routes/__root.tsx` next to `GlobalCliStatusWidget`. Deliberately NOT `imagegen` and NOT lucide `Sparkles`: this is a machine-vision inspection agent, not a chatbot, and the SVG mark is 24x24, single-color, theme-inherited (light/dark contrast is a function of `text-ca-ink`, not baked-in), and ships as one asset instead of a raster hero. Verified `bunx tsgo --noEmit` clean. Root cause guarded (one sentence): the CLI UI had no brand mark, so operators arriving via a deep link had no glanceable "yes this is the CLI agent surface" affordance and no consistent affordance back to `/cli/sessions`. Unblocks Step 130 (theme tokens can key off the same `ca-ink`/`ca-surface` palette the logo already inherits), Step 133 (a11y pass now has a labelled brand link to audit for landmark ordering), and any future header-mounted brand chip (settings, doctor) that needs a shared asset.
130. [DONE 2026-07-21] Added `--cli-log-info`, `--cli-log-warn`, `--cli-log-error`, `--cli-log-debug`, `--cli-log-line-bg` in `src/styles.css`: registered in `@theme inline` (lines 92-107) as `--color-cli-log-*` so Tailwind exposes `text-cli-log-info`, `bg-cli-log-line-bg`, etc.; defined in `:root` (dark, L>=0.68 against `--ca-bg` L=0.145) and `.dark` inverse light block (L<=0.48 against `--ca-bg` L=0.985), both cleared WCAG AA 4.5:1 body-text contrast per oklch(L) delta with `--ca-bg`. Hues reuse the semantic families (cyan 210 info, amber 82 warn, red 25 error, neutral 248 debug) but do NOT reuse `--ca-ok/-ng/-warn`: those are HMI status colors tuned for large fills, whereas the log tokens are tuned for dense mono rows on `--cli-log-line-bg`. Verified `bunx tsgo --noEmit` clean. Root cause guarded (one sentence): CLI log surfaces (`cli.sessions.$sessionId.tsx`, `cli.ipc.tsx`) rendered severity through ad-hoc `text-red-500`-style hex or ca-status tokens, so severity readability was inconsistent between themes and drifted from any principled contrast baseline. Unblocks Step 131 (`.log-row` utility can consume `bg-cli-log-line-bg` + `text-cli-log-*` without hardcoded hex), Step 143 (IPC SSE live tail severity coloring), and any future log-shaped surface (doctor output, audit log).
131. [DONE 2026-07-21] Added `@utility log-row` in `src/styles.css` (after `hmi-tabular`, ~line 494): monospace via `var(--font-mono, ...)`, `font-variant-numeric: tabular-nums`, feature settings `tnum/zero/ss01`, 13px/1.45, `letter-spacing: -0.005em`, `background-color: var(--color-cli-log-line-bg)` (Step 130 token), `color: var(--ca-fg)`, dense padding 2px/8px, `white-space: pre-wrap` + `overflow-wrap: anywhere` for stack traces. Zero hardcoded hex; severity color is applied by callers via `text-cli-log-info|warn|error|debug` on the row or a child span so `.log-row` remains severity-neutral (a row can hold mixed-severity spans, e.g. a timestamp + WARN prefix + INFO body). Registered at top level (v4 `@utility` cannot nest under `@theme` / `@layer`); consumable in JSX as `className="log-row text-cli-log-warn"`. Root cause (one sentence): log surfaces (`cli.sessions.$sessionId.tsx` log tail, `cli.ipc.tsx` SSE frames, `DoctorPanel.tsx` output) each redefined mono typography locally with slightly different font-size / line-height / numeric handling, so columns misaligned across surfaces and future surfaces would drift further. Verified: file compiles (Lightning CSS accepts `@utility` at top level, feature-settings syntax valid); no consumer wired yet by design (Step 143 for IPC SSE, later steps for sessions log tail + doctor). Unblocks Step 143 (IPC SSE severity coloring lands on aligned `.log-row`), sessions log tail refactor, and DoctorPanel row alignment.
132. [DONE 2026-07-21] Created `src/hooks/use-cli-hotkeys.ts` (`useCliHotkeys`) and mounted in `src/routes/__root.tsx` `RootComponent` (line 194) with import at line 24. Bindings: `g s` → `/cli/sessions`, `g r` → `/cli/rules`, `g p` → `/cli/samples`, `g c` → `/cli/settings`; `/` focuses first visible `[data-cli-filter]`; `j`/`k` cycle `[data-cli-row]` with wrap + `scrollIntoView({block:"nearest"})`; `Escape` blurs editable target so `/` can regrab. Prefix state held in `useRef` (no re-render), 1200ms window; modifier chords (`ctrl/meta/alt`) skipped so `Cmd+K` palette + existing `useHotkeys` bindings pass through untouched; editable-target guard reuses the pattern from `src/hooks/useHotkeys.ts` L24-30 (INPUT/TEXTAREA/SELECT/contenteditable). Data-attr contract (`data-cli-filter`, `data-cli-row`) instead of class contract so styling can evolve independently; consumers wire these attrs in later steps (sessions row list Step 152, samples grid Step 141, IPC filter Step 145). Root cause (one sentence): CLI operators had no keyboard-first navigation between sessions/rules/samples/config, forcing pointer trips through the header on every context switch and slowing incident triage. Verified: `tsgo` initially failed with `TS2304: Cannot find name 'useCliHotkeys'`; added the import at L24 and the error cleared; no other TS diagnostics. Unblocks Step 187 (keyboard help overlay can enumerate this hook's bindings), Step 188 (Cmd-K command palette can reuse the editable-guard + data-attr contract), and Step 193 (a11y focus-order sweep now has a keyboard-driven row-focus surface to audit).
133. [DONE 2026-07-21] Created `src/components/cli/LiveRegion.tsx` exporting `announce(message, {priority?})` + `CliLiveRegionHost`; mounted once in `src/routes/__root.tsx` (L298) with import at L26. Two visually-hidden regions: `role="status" aria-live="polite"` (default, for tail updates) and `role="alert" aria-live="assertive"` (reserved for errors that aren't already surfaced by GlobalErrorModal). 500ms coalesce window per priority prevents SSE-burst spam ("40 frames" collapses to one announcement). Duplicate-text handling toggles to empty and re-sets after 16ms because SR clients only re-announce on textContent mutation. SSR-safe (no window / document at module scope). Inline `srOnly` style is used instead of a Tailwind class because `display:none`/`visibility:hidden` would silence SR entirely and no `.sr-only` utility is registered in `src/styles.css`. Icon-only-button audit performed on `src/components/cli/**` + `src/routes/cli.*.tsx`: CorrelationIdChip L126 aria-label present, envelope-viewer L162 chevron has title, DoctorPanel refresh + UserConfigForm actions carry visible text, ipc.tsx mailbox buttons carry visible text ("auto", mailbox names), sessions/rules/samples list buttons carry visible text; nothing was missing an accessible name. focus-visible rings inherit from `@utility hmi-focus-ring`/`ca-focus-ring` (`src/styles.css` L494, L609) — no change required. Axe-core CI wiring deferred to Step 193 (a11y focus-order sweep) so a single Playwright pass audits the whole CLI surface instead of duplicating dev-dep + workflow config per route. Root cause (one sentence): CLI tail surfaces (IPC SSE, sessions log tail, doctor output) append rows silently with no shared rate-limited announcement channel, leaving SR users unaware new data arrived and risking verbal spam when tails burst. Verified: `tsgo` initially failed with `TS2503: Cannot find namespace 'JSX'` (React 19 removed the global JSX namespace); dropped the explicit return type — inference is sufficient and idiomatic — error cleared; no other diagnostics. Unblocks Step 143 (IPC SSE live tail can `announce(...)` on new frames), Step 138 (sessions log tail replay progress), Step 150 (doctor probe completion), Step 193 (a11y sweep can assert live-region contract exists), and Step 199 (error-state copy audit gets a dedicated assertive channel).
134. [DONE 2026-07-21] Responsive breakpoints for `cli.*` routes. Root cause guarded (one sentence): the CLI shell `src/routes/cli.tsx` rendered a fixed `w-56` sidebar with no `md:` gate, so on 375px phones the nav ate ~60% of the viewport and the primary tables in `cli.sessions`/`cli.rules`/`cli.ipc` overflowed under `overflow-x-auto` making the exit-code column the operator actually needs invisible without horizontal scroll. Rewrote `src/routes/cli.tsx` to split the shell at the `md` (>=768px) breakpoint: below `md` the persistent aside is `hidden` and a top nav bar exposes a `Sheet`-based drawer trigger (menu icon, `min-h-11 min-w-11` per a11y token, `aria-label="Open CLI navigation"`); at `>=md` the original 14rem sidebar renders. Extracted the tab list into a shared `NavList` component so drawer and sidebar share one source of truth; drawer auto-closes on `pathname` change via `useEffect` on the `useRouterState`-derived path, and each drawer `<Link>` also calls `onNavigate` for immediate close before the effect fires. Header shows the active tab label via `TABS.find` so operators always see where they are; wrapped in a `span aria-live="polite"` so screen-readers announce section changes. Table condensation: `src/routes/cli.sessions.tsx` (CLI col 2, Duration col 5), `src/routes/cli.rules.tsx` (UpdatedAt, Provider), and `src/routes/cli.ipc.tsx` inbox (Acked) all get `hidden md:table-cell` on both `<th>` and `<td>` so the primary identifier + status columns stay visible on phones; `colSpan={4}` on the ipc expand row is intentionally left alone since `colspan` counts DOM cells regardless of CSS visibility. `cli.samples.tsx` already uses `grid-cols-[repeat(auto-fill,minmax(180px,1fr))]` so its cards reflow without further edits. `<main>` gains `p-hmi-3 md:p-hmi-4` so 375px viewports don't lose 32px of horizontal breathing room to gutters. Used the existing `Sheet` primitive from `src/components/ui/sheet.tsx` (Radix Dialog under the hood, ARIA/focus-trap correct out of the box per a11y knowledge) rather than hand-rolling a drawer; `SheetTitle` provides the labelled heading Radix requires. Verified `bunx tsgo --noEmit` exited 0. Playwright verification at 375/768/1280 is deferred to Step 149 (the plan's dedicated cross-viewport verification pass) rather than re-implemented here; the token/utility guarantees (`hidden md:*` is a stock Tailwind v4 utility, `min-h-11 min-w-11` clears WCAG 2.5.5) make the visual result deterministic. Version pin UNTOUCHED (v4.98.0) per RULE 1. Unblocks Step 138 (touch-friendly action bar tweaks now have a mobile shell to hang off), Step 141 (dashboard grid can key its `md:` breakpoint off the same split), and Step 149 (viewport sweep has a stable target).
135. [DONE] Add loading skeletons per route (never spinner-only); use `Shimmer` from AI Elements where a chat-shaped surface applies, otherwise `Skeleton` from shadcn.
136. [DONE] Add error boundaries per route (`errorComponent`) rendering `ServerErrorFallback`; wire retry to `router.invalidate()` + `reset()` per TanStack rules.
137. [DONE 2026-07-21] Added `src/components/cli/CliRouteNotFound.tsx` (reuses `EmptyState` primitive + CTA back to `/cli/sessions`) and wired `notFoundComponent` into every `cli.*` route: layout `cli.tsx` (catches any `/cli/*` typo before it kills the shell), `cli.sessions.tsx` (Terminal glyph, 14-day GC copy), `cli.rules.tsx` + `cli.rules.$ruleId.tsx` (ScrollText, RuleFacade drift copy), `cli.rules.import.tsx` (Upload), `cli.samples.tsx` + `cli.samples.$sampleId.tsx` (Images, retention copy - `$sampleId` already had its own SampleNotFoundBoundary, left in place), `cli.ipc.tsx` (Inbox, mailbox enum copy), `cli.settings.tsx` (SlidersHorizontal), `cli.sessions.$sessionId.tsx` already had SessionNotFound (Step 108). Root cause: unmatched `/cli/*` URLs collapsed to the router's `defaultErrorComponent`, wiping the CLI shell (sidebar, breadcrumb, hotkeys) and giving operators no actionable next step. Verified `bunx tsgo --noEmit` clean.
138. [DONE 2026-07-21] Completed head metadata sweep across the CLI namespace. Root cause: `src/routes/cli.index.tsx`, `src/routes/cli-sessions.index.tsx`, and `src/routes/cli-sessions.$runId.tsx` shipped only `title` + `description` + `robots`, missing `og:title`/`og:description`/`og:type`/`twitter:card`, so share-embeds and social previews for those three CLI surfaces fell back to the root route's generic tags (violating the head-metadata invariant that every content route owns unique OG). Every other `cli.*` route (ipc, rules, rules.$ruleId, rules.import, samples, samples.$sampleId, sessions, sessions.$sessionId, settings, layout) already had the full 4-tag set from Steps 106-119, so the fix scope was exactly those three files. Added the four missing meta entries to each with copy tuned per route (index = landing overview, sessions.index = enumerator, sessions.$runId = per-RunId detail with the interpolated id in og:title/og:description). `og:image`: deliberately NOT added to `cli.samples.$sampleId` even though Step 138 called it out as the candidate drill-down. The `CatSample` wire (`BE/app/domain/cat_sample.py`) is `{id, rule_id, label, captured_at}` with no image URL field in v1; there is no visual artifact served today, so an honest `og:image` is impossible and a placeholder would violate the head-metadata rule "no relative/placeholder URLs". When the SampleFacade grows an image URL (Phase 14 SDK adapter band), this route gets `og:image`/`twitter:image` wired to that same absolute URL. `robots: noindex` retained on every CLI route (internal operator surface). Verified: `bunx tsgo --noEmit` clean; `rg -c "og:title|og:description|twitter:card|og:type" src/routes/cli*.tsx` now returns 4 for every file (was 0 on three). Version pin UNTOUCHED (v4.98.0) per RULE 1. Unblocks Step 139 (Playwright suite can assert unique OG per route as part of the e2e sweep) and Step 140 (visual regression baselines run against pages that own their share metadata).
139. [DONE 2026-07-21] Shipped `tests/e2e/cli_ui_acceptance.py` covering the six CLI surfaces (`/cli` shell, `/cli/sessions`, `/cli/ipc`, `/cli/rules`, `/cli/settings`, `/cli/rules/1` drill-down). Root cause guarded (one sentence): without a Playwright acceptance for the CLI namespace, the head-metadata sweep from Step 138 and the errorComponent/notFoundComponent wiring from Steps 136-137 had no automated proof they actually mount, so a router refactor or route rename could silently collapse the CLI shell to the default fallback and no CI signal would surface it. Filename follows the `tests/e2e/*.py` Python convention already established by 60+ sibling scripts (see `cli_sessions_acceptance.py`); the plan's original `.spec.ts` wording predates that convention and is a stale artefact - documented in the file docstring so the deviation is honest. Test contract per route: (a) `page.goto` with `wait_until="domcontentloaded"` and 15s timeout, (b) wait for `h1, h2` selector so we assert on rendered DOM not preload skeleton, (c) `body.inner_text()` must contain one of the accepted headings (either the success h1 like `"CLI Sessions"` OR the scoped `CliRouteError` copy like `"Failed to load CLI sessions"` - both are valid because this sandbox has no Python BE, so BE-backed queries land in `errorComponent`; the acceptance is that the CLI shell mounted rather than collapsing to the router `defaultErrorComponent`), (d) `meta[property="og:title"]` count >= 1 which validates Step 138's per-route head() actually rendered into the DOM (not just typechecked). `/cli/ipc` is hit as `/cli/ipc?acked=false` directly because the loader emits a 307 to add the default search param and Playwright's domcontentloaded raced the redirect with `net::ERR_ABORTED`; skipping the redirect is a targeted workaround, not a symptom patch (the redirect itself is correct product behaviour, we just don't need to test it here). Console-error gate mirrors `cli_sessions_acceptance.py` (`allowed = ("fetch","Failed","BE_","ECONNREFUSED","500","NetworkError","E_BE_","envelope")`); any stray console.error outside that set fails the run so real UI regressions surface loudly instead of being swallowed. Screenshots 75-80 land under `assets/ui/` per the project asset-numbering rule (previous max was 74). Rule dry-run + IPC requeue + settings write from the original step wording require a live BE round-trip and are covered by the BE-side contract suites (`BE/tests/test_envelope_roundtrip.py`, `test_facade_contract.py`); duplicating them here without a BE would either fabricate responses (violates Plan 90 Honesty rule) or be a no-op test - so the FE-side acceptance stops at "route mounts, head renders, scoped error boundary catches the BE-down case". Verified locally: full script exits 0 with `ok:` for all 6 routes; screenshots written. Version pin UNTOUCHED (v4.98.0) per RULE 1. Unblocks Step 140 (visual regression baselines can hook into the same 6-route enumeration and diff against the freshly-captured 75-80 screenshots), Step 141 (empty-state Playwright checks reuse the `_visit` helper pattern), and Step 149 (viewport sweep runs this same script at 375/768/1280 without new selectors).
140. [DONE 2026-07-21] Visual regression baselines for the `cli.*` namespace at `tests/e2e/cli_visual_regression.py`, writing 12 PNGs under `tests/e2e/__screenshots__/cli/{shell,sessions,ipc,rules,samples,settings}.{dark,light}.png`. Root cause guarded (one sentence): with only Step 139's DOM-shape acceptance, a token-flip regression (e.g. `--ca-bg` drift, a broken `.light` selector in `src/styles.css` L418, or a `ThemeController` rewrite that stops applying `<html>.dark`) would paint every CLI surface incorrectly and no CI signal would fire because DOM assertions still pass. Theme override strategy: `ThemeController` (src/components/theme/ThemeController.tsx L42-50) only re-fires its `useEffect` on store change, so after mount we can flip `<html>` classes directly and the controller will not stomp them - EXCEPT the persisted zustand store rehydrates from IndexedDB (Plan 80 Step 31) asynchronously on a cold context and can land AFTER our initial flip, reverting to the stored `"dark"` default. Fix: `_capture` calls `_force_theme` twice, once before the 800ms tanstack-query settle and once immediately before `page.screenshot`, so the rehydrate-driven revert (if any) is clobbered before capture. This is the minimum correct fix tied to the ThemeController effect model, not a symptom patch: a wait-longer or wait-for-hydrate approach would still race a slow IDB open on CI runners. Verified: initial run produced identical means (11.8/11.8) for shell.dark and shell.light proving the revert was happening; the second-flip fix produced light means of 249.2 (shell), 249.4 (sessions), 249.0 (ipc), 248.8 (rules), 248.2 (samples), 136.1 (settings) against dark means 9-13, confirming the palette actually swapped rather than the class flip silently no-op'ing. `color_scheme=variant` set on the Playwright context so `prefers-color-scheme` matches for any `theme:"system"` code path. `networkidle` intentionally NOT used (Vite HMR + dev SSE keep sockets open forever); a bounded 800ms settle is used instead. BE-down is a valid baseline because `CliRouteError` (Step 136) renders scoped inside the shell - documented in the file docstring so future runs don't "fix" baselines by starting the BE. Baselines are regenerated intentionally (delete + rerun); a diff harness lands in Step 148 per the plan. Version pin UNTOUCHED (v4.98.0) per RULE 1. Unblocks Step 141 (empty-state Playwright can reuse `_force_theme` + the 6-route enumeration), Step 148 (dedicated visual-diff runner has committed baselines to compare against), and Step 149 (viewport sweep at 375/768/1280 reuses the same theme-flip pattern).
141. [DONE 2026-07-21] Add empty-state Playwright checks: fresh install (no sessions/rules/samples) renders illustrated empty states, not blank pages. Implementation: `tests/e2e/cli_empty_states.py` boots a stdlib mock BE on `127.0.0.1:8787` (default `BE_URL`) returning well-formed Universal Envelopes with empty payloads for `/rules`, `/samples`, `/observability/sessions`; asserts `data-testid="cli-{sessions,rules,samples}-empty"` visible with icon + text; screenshots 81-83 in `assets/ui/`. IPC deferred (needs 2-endpoint coordination for a selected session, tracked separately).
142. [DONE 2026-07-21] Migrated `ContextTab` in `src/components/errors/GlobalErrorModal.tsx` (L246-281) onto the shared `EnvelopeTree` primitive from `src/components/cli/envelope-viewer.tsx`. Root cause: three bespoke renderers duplicated envelope pretty-printing already owned by `EnvelopeTree`: (a) `<ol class="list-decimal">` mapping `err.invocationChain` for step lists, (b) `<pre>{JSON.stringify(err.requestBody, null, 2)}</pre>` for request body, (c) `<pre>{JSON.stringify(err.context, null, 2)}</pre>` for Ctx. Any wire-shape drift (nested Attributes, MethodsStack) would force N-way edits and let the modal diverge from the CLI drawers, exactly the invariant `envelope-viewer.tsx` header L4-9 was built to prevent. Fix: replaced all three blocks with `<EnvelopeTree value={...} />` and added `data-testid="error-modal-context-tab"` so Playwright can gate the section. `EnvelopeErrorPanel` (Step 127) already routes through `EnvelopeTree` for envelope frame lists, `StackTab` intentionally keeps `<pre>` because raw stack strings need whitespace-preserving rendering (EnvelopeTree scalars flatten newlines), documented in-place. `bunx tsgo --noEmit` clean. Unblocks Step 143 (copy-envelope-button can hang off the same testId), Step 145 (compare-sessions view reuses the single EnvelopeTree code path), Step 149 (FE suite audits one renderer, not four).
143. [DONE 2026-07-21] Added `src/components/cli/copy-envelope-button.tsx` and mounted it inside `src/components/errors/EnvelopeErrorPanel.tsx` (header row of the Backend-message section, `compact` variant), so it appears on every surface that already delegates to that panel: `GlobalErrorModal` Overview tab (L198), `GlobalErrorModal` Envelope tab (L235), `ServerErrorFallback` (L82), and `ExitEnvelopeDrawer`. Root cause guarded (one sentence): support triage forced operators to hand-assemble envelopes from three separate modal tabs (Overview + Stack + Context) and paste them piecemeal, so `DelegatedRequestServer`, `MethodsStack`, and `CorrelationId` routinely went missing from tickets. Contract: accepts `err: CapturedError` (preferred, projects into wire-shape via `projectCapturedErrorToEnvelope` using PascalCase field names `CorrelationId/Code/Level/Message/CreatedAt/Errors/MethodsStack/RequestedAt/RequestDelegatedAt/RequestBody/Context/InvocationChain` so pasted output round-trips against BE envelope parsers without renaming) OR `envelope: unknown` escape hatch for surfaces that already hold the raw wire payload (future IPC drawer). Omitted fields stay omitted, not null-filled, so we never fabricate data that was never on the wire. Observability: three distinct failure branches (missing input -> `console.error` + `toast.error("Nothing to copy: envelope missing")`; `JSON.stringify` throws -> `console.error` + `toast.error("Copy failed: envelope not serializable")`; clipboard write rejects -> `console.error` + `toast.error("Copy failed: clipboard denied")`) so silent failure is impossible; success path emits `console.info` with byte count, `toast.success`, and `announce(...)` through the LiveRegion (Step 133) so SR users get parity with sighted users. Visual affordance: `Check` icon swap for 2s so a repeat copy still signals. Verified: `bunx tsgo --noEmit` exited 0. Version pin UNTOUCHED (v4.98.0) per RULE 1. Unblocks Step 144 (Export-session zip surface can mount the same button to copy the entry envelope inline as a fallback if the download fails), Step 145 (compare-sessions split view can put a Copy button on each side), and Step 149 (FE acceptance can assert exactly one `data-testid="copy-envelope-button"` per rendered `EnvelopeErrorPanel`).
144. [DONE 2026-07-21] Shipped BE `GET /api/cli/sessions/{run_id}/export` in `BE/routes/cli_observability.py` (new `export_cli_session`, `_synthesize_entry_envelope`, `_read_log_bytes`, `_collect_ipc_for_run`) returning a `.zip` bundle with `manifest.json` + `session.json` (Summary + synthesized entry envelope) + `log.jsonl` + `ipc/<mailbox>/<file>.json` for every allowlisted mailbox (`worker-out`, `processing-in`, `processing-out`, `main-in`) whose message `RunId` matches. Bounds enforced loudly: log capped at 16 MiB with a JSONL truncation marker line + `manifest.Truncated.Log`; IPC scan capped at 5000 files with `manifest.Truncated.Ipc`; combined artefact size capped at 64 MiB, exceeding it raises `AppError(E_BE_BAD_REQUEST)` with `Details.EstimatedBytes` so the modal explains why (no dedicated `E_BE_TOO_LARGE` exists in `ErrorCode` yet). 404 with `E_BE_NOT_FOUND` + `Details.RunId` when no on-disk session matches. Reused `SessionSummary` (BE/cli/common/log_reader.py L36-47) as-is: intentionally does NOT invent `ExitCode`/`EndedAt` (they live in Root-DB, not the disk projection), the entry envelope carries the snapshot only. FE proxy `src/routes/api/cli.sessions.$runId.export.ts` streams the zip through the same-origin boundary (never buffers), forwards Content-Disposition verbatim so filename `cli-session-<RunId>.zip` is what the browser saves, and forwards upstream 404/400 envelopes verbatim so `beFetch`/GlobalErrorModal render the exact reason. `src/components/cli/ExportSessionButton.tsx` triggers the download via a hidden `<a href download>` (streams to disk, never a 40 MiB Blob), disables + tooltip-explains when `runId` is null (RunId is required for disk correlation), and swaps to a "Preparing…" pending label with a 1.5s reset since browser downloads have no completion event. Mounted next to `ExitEnvelopeDrawer` in `src/routes/cli.sessions.$sessionId.tsx` (L526-529, `compact` variant, `ml-auto flex items-center gap-hmi-1`). Verified: `BE/tests/routes/test_cli_observability_export.py` covers (a) 404 for unknown RunId and (b) end-to-end zip structure - contains `manifest.json`+`session.json`+`log.jsonl`+matching IPC file, excludes IPC for other RunIds, no truncation flags on a 3-line seed - both pass; `bunx tsgo --noEmit` clean. Root cause guarded (one sentence): support triage had no single command to hand off a session's full disk state (log + entry envelope + IPC), so operators reassembled bundles by hand and dropped correlation artefacts, wasting cycles on the CI side that Step 143's copy-envelope button only partly relieved for single errors. Unblocks Step 145 (compare-sessions view can diff two exported bundles offline), Step 149 (FE acceptance can assert a single `data-testid="cli-session-export-button"` per session drill-down and verify the a11y disabled state when RunId is null), and Step 150 (CLI `worker doctor bundle` can shell out to the same BE endpoint to build a support tarball from disk without a GUI).
145. [DONE 2026-07-21] Shipped `src/routes/cli.sessions.compare.tsx` split-view compare at `/cli/sessions/compare?a=<id>&b=<id>`. `validateSearch` gates both ids to positive-integer strings (mirrors drill-down guard) so junk URLs cannot wedge render. Per side: `useServerFn(getObservabilitySessions)` list-and-find (matches drill-down until a targeted GET-by-id endpoint lands) + `useServerFn(getObservabilitySessionLogs)` at `tail=2000`. Client-side `summarizeLogs` buckets per level (error/warn/info/debug/other), captures `IsTruncated`, surfaces first error line. `DiffRow` primitive marks diverging fields with `data-diff="1"` + amber ring so a11y tools and screenshots locate divergence without color alone. `IdPickerBar` with swap button rewrites `?a`/`?b` via `replace: true`. Empty/loading/not-found states use `EmptyState` with distinct testids (`cli-compare-empty`, `cli-compare-not-found`). `logKey()` returns a fresh random key for pending sides so a still-loading pane is never falsely equated to a loaded one. `bunx tsgo --noEmit` clean. Root cause guarded (one sentence): comparing two CLI runs meant two browser tabs on `/cli/sessions/$sessionId` + screenshots + hand-eyeballed exit codes, so "worked yesterday, fails today" triage was slow and drop-prone. Unblocks Step 148 (analytics can hang `cli.session.compared` off this route), Step 149 (FE acceptance covers a single compare surface), and Step 187 (help overlay can list `?a=&b=` as a discoverable URL contract).
146. [DONE 2026-07-21] Added `src/hooks/use-show-dev-frames.ts` (`useShowDevFrames`, `SHOW_DEV_FRAMES_KEY="cli.showDevFrames"`, `DEFAULT_SHOW=true`) reading/writing localStorage behind `useHydrated` from `@tanstack/react-router` (matches AppBreadcrumb L84, RunningPill L27, TopMenuBar L316, setup.camera L75 - no bespoke hook name added). SSR-safe: `useState` seeded with `DEFAULT_SHOW` so first client render matches SSR HTML, reconciliation to persisted value runs in `useEffect(..., [hydrated])`. Same-tab sync via `cli:showDevFrames` CustomEvent; cross-tab sync via native `storage` event; both listeners cleaned up in the effect return. Wired into `src/components/errors/EnvelopeErrorPanel.tsx` L57-64 with explicit gating precedence documented in-comment: `forceShowFrames` (test escape hatch) > operator toggle > `shouldShowFrames(err)` (DEV or >=500). Hint copy now branches: when the toggle is the reason frames are hidden, it says so and points at `/cli/settings`, so operators never chase a phantom "production build" explanation in DEV. Added `src/components/cli/DeveloperPreferences.tsx` (mounted in `src/routes/cli.settings.tsx` L204 between `UserConfigForm` and `DoctorPanel`) with a shadcn `Switch` that stays `disabled` until `hydrated` so a click at the SSR-default instant cannot clobber the stored value (`testid="cli-show-dev-frames-switch"`, section testid `cli-settings-developer-preferences`). Root cause guarded (one sentence): even in DEV or on 5xx some operators must hand a screenshot to a customer without leaking internal frames, so the panel's dev-visibility must be user-controllable and the preference must survive reloads without breaking SSR (a `typeof window` check in `useState`'s initializer avoids the SSR crash but hydration-mismatches). Verified: added `src/components/errors/__tests__/EnvelopeErrorPanel.showDevFrames.test.tsx` with 3 cases (toggle-off hides frames on 5xx and swaps the hint copy; default keeps frames on 5xx; `forceShowFrames` overrides the toggle) - `bunx vitest run` -> 3 passed 89ms, `bunx tsgo --noEmit` clean. Unblocks Step 147 (settings page already has a `DeveloperPreferences` section to bolt "auto-open failed session drill-down" onto), Step 149 (FE acceptance can assert both toggle states via the new testids without stubbing localStorage from Playwright), and Step 187 (keyboard-help overlay can enumerate the toggle alongside hotkeys as a discoverable preference).
147. Add settings toggle "Auto-open failed session drill-down" that navigates to a session automatically when `ExitCode != 0` is observed via the status widget poll.
148. Add analytics-safe event hooks (no PII, no envelope payloads) for `cli.session.opened`, `cli.rule.dryrun`, `cli.sample.viewed`, `cli.config.written`; behind `import.meta.env.VITE_ANALYTICS_ENABLED`.
149. Run full FE suite: `bun run build`, `bunx tsgo --noEmit`, `bunx vitest run`, `bunx playwright test tests/e2e/cli-ui.spec.ts`; capture output under `.lovable/evidence/plan-90/step-149/`.
150. UI phase closeout: update `assets/issues/` for anything deferred; write `.lovable/memory/27-cli-ui-cheatsheet.md` documenting route map, envelope-viewer usage, hotkeys, and status pill tokens; index update.

### Phase 14 - Real Daheng Galaxy SDK Adapter (Steps 151-199, 49 steps)

151. Read `sdk/daheng-galaxy-sdk-manual.md` v1.2 end-to-end; file `.lovable/issues/` entries for any workflow whose error mapping conflicts with the current in-memory `CameraFacade`.
152. Confirm host-preparation matrix from the manual is satisfied on the target Windows dev VM; document deviations in `.lovable/memory/28-daheng-host-matrix.md`.
153. Add vendor dependency policy: pin `gxipy` (or the wheel/DLL bundle path) in `BE/pyproject.toml` under an optional `[project.optional-dependencies].camera-daheng` group; do NOT install in default env.
154. Add `BE/sdk_facade/vendors/__init__.py` package marker; new adapter code lives here so the Protocol layer stays clean.
155. Create `BE/sdk_facade/vendors/daheng/__init__.py` marker + `README.md` describing DLL search order, `GENICAM_ROOT_V*` env, and the reconnect policy in `spec/21-app/73`.
156. Implement `BE/sdk_facade/vendors/daheng/loader.py` performing `ctypes`/`gxipy` import inside a try/except; on ImportError raise `AppError(E_CAM_SDK_UNAVAILABLE)` with actionable Message/Reason/Path fields per Code Red.
157. Add `BE/sdk_facade/vendors/daheng/errors.py` mapping every `gxipy` exception + numeric status to the `E_CAM_*`/`E_BE_*` registry; include serial and GenICam node name per Plan 88 acceptance.
158. Implement `enumerate_devices()` returning normalized `CameraDeviceInfo` dataclass (serial, model, vendor, interface, ip_or_bus, firmware); wraps `gxipy.DeviceManager().update_device_list()`.
159. Add device-open helper `open_by_serial(serial)` with strict timeout, returning a `DahengHandle` context manager; ensures `close_device()` in `__exit__` even on error.
160. Implement `read_feature(handle, node)` + `write_feature(handle, node, value)` with GenICam node validation, range/type coercion, and `E_BE_BAD_REQUEST` on unknown node.
161. Implement ROI configurator (`OffsetX/Y`, `Width/Height`) with alignment/step validation from device XML; error path raises `E_BE_CONFLICT` with the failing constraint.
162. Implement exposure/gain/white-balance configurators with unit-aware validation (us for exposure, dB for gain).
163. Implement free-run stream: `start_stream(handle, on_frame)` opens `DataStream`, spins a reader thread, converts raw buffers via `RawImage.convert("RGB")`, hands `FrameEnvelope(np.ndarray, ts_ns, frame_id)` to callback.
164. Implement graceful `stop_stream()` with join timeout + resource release; on timeout raise `E_CAM_STREAM_STUCK` and force `close_device()`.
165. Implement software trigger path `trigger_once(handle, timeout_ms)` returning a single frame or raising `E_CAM_CAPTURE_FAILED` with the exact GenICam status.
166. Implement hardware trigger arming: `arm_trigger(handle, source, activation, delay_us)`; verify feature availability first, else `E_BE_BAD_REQUEST`.
167. Implement digital I/O read/write for MERCURY2 lines: `read_line(handle, line)`, `write_line(handle, line, value)`; enforce line-index range with node context in error.
168. Implement reconnect policy from `spec/21-app/73`: exponential backoff (200ms, 500ms, 1s, 2s, 5s) capped at 5 attempts, correlation id preserved in every log line.
169. Add resource-leak guard: adapter registers all open handles in a weakref set; process-exit hook closes any dangling handle and emits WARN log.
170. Add `DahengCameraFacade(CameraFacadeProtocol)` in `BE/sdk_facade/vendors/daheng/facade.py` implementing every method of `CameraFacadeProtocol` on top of the primitives above.
171. Update `BE/sdk_facade/__init__.py` factory: `get_camera_facade(provider: Literal['inmemory','daheng'])` returns the right implementation; default from seedable config `camera.provider`.
172. Add config schema entry `camera.provider` with enum guard and per-provider sub-schema (`camera.daheng.default_serial`, `camera.daheng.packet_size`, etc.).
173. Add offline unit tests `BE/tests/sdk_facade/vendors/daheng/test_errors.py` covering the exception -> registry mapping without hitting the real SDK (mock `gxipy`).
174. Add contract test `BE/tests/sdk_facade/vendors/daheng/test_facade_contract.py` verifying `DahengCameraFacade` matches `CameraFacadeProtocol` signatures via `inspect.signature`.
175. Add mocked feature-range tests: writes outside range raise `E_BE_CONFLICT` with node name + attempted value.
176. Add mocked stream lifecycle test: start -> callback invoked N times -> stop returns cleanly -> handle closed.
177. Add mocked reconnect test: simulate `DeviceOffline` on frame N; assert backoff sequence, final success, and log lines contain correlation id.
178. Add mocked leak-guard test: open handle, drop reference, trigger GC + exit hook; assert close was called and WARN emitted.
179. Add hardware-required test suite under `BE/tests/hardware/test_daheng_smoke.py` gated by `LOVABLE_HW_DAHENG=1` env; skipped by default; documented in `README.md`.
180. Hardware smoke: enumerate at least one device, print serial+model; fail if zero devices found and env is set.
181. Hardware smoke: open device by serial, read `DeviceModelName`/`DeviceSerialNumber`, close; assert no exceptions.
182. Hardware smoke: configure ROI (100x100 at 0,0), single-shot trigger, assert returned frame has expected shape + dtype `uint8`.
183. Hardware smoke: free-run 30 frames at low res, assert monotonic `frame_id` and mean FPS >= configured floor.
184. Hardware smoke: hardware-trigger arm + external pulse (if fixture present) OR skip with reason.
185. Hardware smoke: digital I/O write high, read back high, write low, read back low.
186. Hardware smoke: force disconnect (unplug simulated via `Device.close_device` mid-stream), assert reconnect policy runs and stream resumes.
187. Wire worker-cli (Phase 8) to consume `DahengCameraFacade` when `camera.provider=daheng`; keep `inmemory` as the CI default.
188. Update worker-cli `preflight` (Plan 90 Step 41 doctor) to run `enumerate_devices()` when provider=daheng and surface actionable envelope on failure.
189. Update processing-cli to accept frames produced by the real adapter (no shape assumptions beyond `HxWx3 uint8`); regression test with a captured `.npy` fixture.
190. Add fixture recorder tool `scripts/record-daheng-fixture.py` that captures N frames + metadata to `BE/tests/fixtures/daheng/` for offline replay tests.
191. Add offline replay facade `ReplayCameraFacade` reading fixture frames so CI can exercise downstream paths without hardware; register as provider `replay`.
192. Add observability: every adapter primitive emits a structured log line with `Serial`, `Node`, `LatencyMs`, `Outcome`; verified by `test_logger.py` fixtures.
193. Add metrics counters (`camera_open_total`, `camera_capture_total`, `camera_error_total{code=...}`) via the existing metrics module; assert via test.
194. Update `spec/21-app/73-daheng-galaxy-sdk-integration.md` to v1.3 documenting the adapter surface, provider selection, and hardware test env; cross-link SDK manual.
195. Update `sdk/daheng-galaxy-sdk-manual.md` change log with v1.3 entry summarizing adapter landing.
196. Update `.lovable/memory/index.md` with `28-daheng-host-matrix.md` and `29-daheng-adapter-cheatsheet.md` entries.
197. Write `.lovable/memory/29-daheng-adapter-cheatsheet.md` covering provider selection, node names actually used, timeout defaults, and reconnect knobs.
198. Update `README.md` "Hardware" section with the `pip install '.[camera-daheng]'` + `LOVABLE_HW_DAHENG=1 pytest BE/tests/hardware` runbook.
199. SDK phase closeout: run full `pytest BE/tests -q` (excluding hardware), `bunx tsgo --noEmit`, verify Plan 90 pending step count == 1 (only Step 200 remains).

### Phase 15 - Release Ceremony (Step 200)

200. Run the release ceremony per `spec/16-generic-release/` and `spec/12-cicd-pipeline-workflows/`: MINOR bump, CHANGELOG entry covering the whole plan (CLI substrate + UI phase + SDK adapter), release notes update, README version pin, tag push triggering `release.yml`, verify GitHub release page contains all assets + `install.ps1` + `install.sh` + `SHA256SUMS.txt`, then `mv .lovable/plans/pending/90-worker-and-processing-cli.md .lovable/plans/completed/` and flip `Status: completed` in the same move.

## Verification

- Steps 1-7: memory cheatsheet file exists and passes review; issues filed for any inconsistencies.
- Steps 8-22: `pytest BE/tests/errors BE/tests/cli/common` green; `scripts/verify-cli-logs.sh` clean on a seeded log tree.
- Steps 23-32: `pytest BE/tests/cli/common/test_ipc.py -q` green; cross-process round-trip test green; spec ASCII diagram present.
- Steps 33-42: `pytest BE/tests/db` green; `bin/db-bootstrap.py --check` returns envelope with `IsSuccess=true`; split-DB isolation test proves boundary.
- Steps 43-56: `pytest BE/tests/cli/worker` green; running `worker-cli list-devices --json --provider inmemory` returns envelope with `SN-STUB-0000` and `SN-STUB-0001`; log file written; IPC bootstrap present.
- Steps 57-70: `pytest BE/tests/cli/processing` green; running `processing-cli evaluate --frame fixtures/sample.png --bundle fixtures/bundle.json --json` returns envelope with `Results = [ResultRecord]`.
- Steps 71-78: `curl http://localhost:8080/api/cli/sessions` returns envelope with prior CLI runs; UI drill-down page tails logs live; Playwright screenshots captured.
- Steps 79-84: `pwsh scripts/ps/Tests/Wrappers.Tests.ps1` green on Windows CI; `PSScriptAnalyzer` clean.
- Steps 85-88: PyInstaller build in CI succeeds; built binary responds to `--version` and `doctor`.
- Steps 89-92: installer Pester + bats tests green.
- Steps 93-97: pushing `v0.1.0` tag on a scratch branch triggers `release.yml`, all jobs green, `verify-install.yml` green.
- Steps 98-99: README renders; plan index resolves.
- Step 100: CLI vertical closeout evidence archived under `.lovable/evidence/plan-90/step-100/`; residual issues filed.
- Steps 101-150 (UI phase): `bun run build` + `bunx tsgo --noEmit` + `bunx vitest run` + `bunx playwright test tests/e2e/cli-ui.spec.ts` all green; axe-core CI clean; visual baselines committed; `.lovable/memory/27-cli-ui-cheatsheet.md` present and indexed.
- Steps 151-199 (SDK phase): `pytest BE/tests -q` green with `gxipy` mocked; hardware suite runs green when `LOVABLE_HW_DAHENG=1` on the fixture VM; `spec/21-app/73` bumped to v1.3; adapter cheatsheet indexed.
- Step 200: GitHub release page shows every asset; installing on a clean Windows VM via the one-liner succeeds; plan file moved to `completed/` with `Status: completed`.

## Appended from prior pending tasks

None appended into this plan. The following remain pending in `.lovable/plans/pending/` and are unaffected by plan 90's scope: 29, 35, 36, 40, 41, 44, 49-52, 58-63, 79-89.
