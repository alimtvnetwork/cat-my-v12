# Command: UI issues closeout sweep

Scope: Plan 73 (`.lovable/plans/pending/73-ui-issues-closeout-sweep.md`).

Verbatim intent: "50-step plan, maximal enforcement, sweep the open UI issues (17-26) to closed and finish Plan 43 residual slice 3 in a single planned pass."

When it applies: whenever a maximal-enforcement 50-step planning prompt (`title: Plan 50, slug: plan-50`) is fired with UI issue closure as the implicit target.

Applies to: every UI/UX issue currently `status: open` under `.lovable/issues/` (17-26 at time of capture).
