/**
 * CaptureVendorType — Hardware capture vendor abstraction.
 * Identifies which SDK adapter is active for the vision pipeline.
 */
export enum CaptureVendorType {
  Daheng = "Daheng",
  Mock = "Mock",
  Replay = "Replay",
}
