# UI Breakages

## Issues Reported by User

1. **Compact Screen UI Broken**: In the modern UI, if we are in a compact screen, the UI looks broken. Controls go out of the screen and do not look compact.
2. **Setup Menu Duplicated**: The setup menu looks like it appears twice.
3. **Window Menu Missing**: The window menu is missing. Should show how many windows are open.
4. **UI Switcher Missing**: There is no button to switch between UI modes (Standard vs Modern) in certain places.
5. **CLI Unknown Text**: At the top of the screen, it says "CLI unknown", but since we are not selecting the backend, it should be hidden.
6. **Layer Numbers**: The layer numbers are not very professional in the modern screen.
7. **Controls Out of Screen**: Controls go far away from the screen, meaning they lack proper bounding/overflow constraints.
8. **Broken Menu/Version Section**: The menu or version section is broken.
