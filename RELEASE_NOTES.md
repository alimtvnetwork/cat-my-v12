## v4.98.0 - 2026-07-21

Plan 90 Step 89: saved-views bookmark list on `/observability/sessions`. New `src/lib/observability/savedViews.ts` (SSR-safe localStorage, cap 20, 64-char names, JSON-parse-hardened, quota-safe writes with `console.warn` breadcrumbs) + a saved-views UI strip with apply/delete chips and an inline "Save current" form. Snapshots the post-middleware search so views stay canonical; apply re-runs `validateSearch` so schema evolution auto-repairs old views. tsgo clean. Version bump: v4.97.0 -> v4.98.0.

## v4.97.0 - 2026-07-21

Plan 90 Step 88: shareable view URL + `retainSearchParams(['debug'])` on `/observability/sessions`. Added `debug` search field, layered `retainSearchParams(["debug"])` before `stripSearchParams(SEARCH_DEFAULTS)` so troubleshooting tags survive navigation without polluting clean URLs, and added a "Copy view URL" header button that copies `window.location.href` via the Clipboard API with sonner success/failure toasts. tsgo clean. Version bump: v4.96.0 -> v4.97.0.

## v4.96.0 - 2026-07-21

Plan 90 Step 87: hardened `/observability/sessions` search validation to the `tanstack-search-params` guideline. Replaced closed `z.enum` + `.min/.max` + raw `.parse` (which crashed the route on any bad URL) with `zodValidator` + `fallback()` on open primitives and a `stripSearchParams(SEARCH_DEFAULTS)` middleware; component whitelists/clamps the loose values so BE stays coherent. Installed `@tanstack/zod-adapter@1.167.0`. tsgo clean. Version bump: v4.95.0 -> v4.96.0.

## v4.95.0 - 2026-07-21

Plan 90 Step 86: `/observability/sessions` now persists filters, sort, limit, and auto-refresh in the URL via a Zod-validated `validateSearch`. Reload, deep-link, and browser back preserve the operator's view; default values are stripped so URLs stay short. Cursor stays out of the URL (opaque, per-query). tsgo clean. Version bump: v4.94.0 -> v4.95.0.

## v4.94.0 - 2026-07-21

Plan 90 Step 85: FE `SessionList` gains a "Load more" affordance driven by the BE `nextCursor` shipped in Step 84. While wiring it, discovered and fixed a pre-existing envelope-shape bug across all three observability server-fns (`sessions`, `logs`, `ipc`): the Zod schemas expected `env.Data`, but the BE Universal Envelope emits `Results: [payload]`. Every observability call had silently thrown `BE_HTTP_200` since Step 75. Schemas now read `Results[0]`, tolerate both single-object and array `Errors` shapes, and surface a loud `BE_ENVELOPE_EMPTY` if the payload is missing. Filter/sort/limit changes reset the cursor so it stays bound to its minting query. Verified: 27 BE pytest cases pass, tsgo clean. Version bump: v4.93.0 -> v4.94.0.

## v4.93.0 - 2026-07-21

Plan 90 Step 84: `GET /observability/sessions` now supports cursor pagination. An opaque base64url `cursor` param (`{v, id, b?}`) drives a strictly-after predicate that matches the existing `expr <dir>, CliInvocationId DESC` tiebreaker, with a bucket-aware branch for `DurationMs` so NULL-sink actives are walked after ended rows. Response payload gains `nextCursor` (emitted only when the page fills `limit`). Malformed cursors raise `E_BE_BAD_REQUEST`. Older-than-500 rows are now reachable under any sort. Verified: 27 pytest cases pass (4 new). Version bump: v4.92.0 -> v4.93.0.

## v4.92.0 - 2026-07-21

Plan 90 Step 83: FE `SessionList` now forwards its current `sort`/`dir` to `GET /observability/sessions` via `getObservabilitySessions`, activating the BE sort shipped in Step 82. Removed the client-side re-sort and the dead `statusRank` helper: BE is now the single source of truth for ordering, so alternative sorts cover the full window instead of only the newest-500 slice. Verified: tsgo clean. Version bump: v4.91.0 -> v4.92.0.

## v4.91.0 - 2026-07-21

Plan 90 Step 82: `GET /observability/sessions` now accepts `sort=StartedAt|CliName|Status|DurationMs` and `dir=asc|desc`. Status ordering matches the FE (active < success < failure); Duration NULLs (still-running) sink to the end regardless of direction so active work stays visible inside the 500-row cap. Envelope echoes `sort` and `dir`. Bad values return `E_BE_BAD_REQUEST`. Verified: 23 pytest cases (7 new). Version bump: v4.90.0 -> v4.91.0.

## v4.90.0 - 2026-07-21

Plan 90 Step 81: SessionList columns Started / CLI / Status / Duration are now click-to-sort with toggleable direction and `aria-sort` announcements. Running rows always sink to the bottom of Duration sorts so active work stays visible. Client-side only (BE `limit` capped at 500). Verified: tsgo clean. Version bump: v4.89.0 -> v4.90.0.

## v4.89.0 - 2026-07-21

Plan 90 Step 80: SessionList rows now expose an "IPC" deep-link column pointing to `/observability/sessions/{CliInvocationId}/ipc` (IpcMonitor). Available on every row, no `RunId` required. Verified: tsgo clean. Version bump: v4.88.0 -> v4.89.0.

## v4.88.0 - 2026-07-21

Plan 90 Step 79: SessionList now highlights active rows and auto-refreshes every 5s while any row is active (default on, operator-toggleable). Idle pages stop polling entirely. Load errors are logged with context via `console.warn`. Verified: tsgo clean. Version bump: v4.87.0 -> v4.88.0.

## v4.87.0 - 2026-07-21

Plan 90 Step 78: SessionList rows now deep-link to `/observability/runs/{RunId}` via a new "Live tail" column. Closes the last gap between the sessions triage screen and the SSE live-tail stack shipped in Steps 72-77. Rows without a `RunId` render a disabled "no run id" label instead of a broken link. Uses TanStack type-safe `<Link params>`. Verified: tsgo clean. Version bump: v4.86.0 -> v4.87.0.

## v4.86.0 - 2026-07-21

Plan 90 Step 77: new route `/observability/runs/{runId}` mounts `LogTailViewer` inside a minimal operator chrome (title, run id, back-link to `/observability/sessions`). Keyed by the disk `RunId` so it's the correct entry point for the SSE stack (BE disk tail -> SSE proxy -> `useSessionLogTail` -> auto-reconnect -> viewer). Distinct from the existing polling logs route which is keyed by DB `cliInvocationId`. `robots: noindex`. Head metadata is route-unique. Verified: tsgo clean. Version bump: v4.85.0 -> v4.86.0.

## v4.85.0 - 2026-07-21

Plan 90 Step 76: `LogTailViewer` React component wires the auto-reconnect SSE hook into an operator-facing pane. Renders JSONL frames keyed by `lastEventId` in a scroll-pinned monospace log, with a status pill (connecting / live / retrying(n) / ended / gave-up) and a manual `Reconnect` button. Surfaces drift as sonner toasts: warning on each new `consecutiveFailures` bump, error when the policy gives up, success on clean end with line count. Every transition also `console.info`s with `runId` for BE-JSONL correlation. Verified: 3 vitest cases green using a fake `EventSource` and mocked `sonner`. Version bump: v4.84.0 -> v4.85.0.

## v4.84.0 - 2026-07-21

Plan 90 Step 75: `useSessionLogTailAutoReconnect` adds an exponential-backoff policy layer over Step 74's SSE hook. On `"error"` it schedules `reconnect()` after `min(500ms * 2^attempt, 15s)` for up to 8 consecutive failures; on `"open"` it resets the counter; on `"ended"` (clean BE `end` frame) it never reconnects. Exposes `reconnectCount`, `consecutiveFailures`, and `gaveUp` so the next step's drift toast has a stable signal. Every scheduled reconnect and give-up is `console.warn`ed with `runId` + attempt context. Verified: 4 vitest cases green (fake timers + injected `FakeEventSource`). Version bump: v4.83.0 -> v4.84.0.

## v4.83.0 - 2026-07-21

Plan 90 Step 74: `useSessionLogTail` React hook binds `EventSource` to the same-origin SSE proxy from v4.82. Collects `data:` frames keyed by `lastEventId`, parses the terminal `end` frame into `{RunId, LineCount, NextSinceLine, Truncated}`, surfaces transport errors as `status="error"` with `console.warn("[useSessionLogTail]")` context, and exposes `reconnect()` for caller-owned retry policy. Factory injection keeps tests hermetic. Verified: 5 vitest cases green. Version bump: v4.82.0 -> v4.83.0.

## v4.82.0 - 2026-07-21

Plan 90 Step 73: Same-origin SSE proxy `/api/cli/sessions/:runId/log` bridges the browser to the BE tail endpoint shipped in v4.81. TanStack server route validates `runId` at the FE origin, forwards `follow`/`since_line`/`max_lines`, streams `text/event-stream` unbuffered (`Cache-Control: no-cache, no-transform`, `X-Accel-Buffering: no`), passes upstream 404 through so the UI still sees `E_BE_NOT_FOUND`, and maps unreachable / non-SSE upstream to `503 E_BE_UPSTREAM_DOWN` / `502 E_BE_UPSTREAM_BAD` with `[cli-log-proxy]` console.error breadcrumbs. Verified: 5 vitest cases green. Version bump: v4.81.0 -> v4.82.0.

## v4.80.0 - 2026-07-21

Plan 90 Step 142: **Reverse envelope adapter + `GET /rules/{id}/set` round-trip.** New `envelopeToProjectRuleset` in `src/lib/rules/envelopeAdapter.ts` rehydrates every `_Legacy*` field from Step 139's forward adapter (id, kind letter, rotation, family, sourceRuleId, isHidden, isLocked), strips those namespaced keys back out of `Params`, falls back to wire `Kind` when metadata is absent (with logged `UnknownKindFallbacks` counter), and reverses `RuleSetId` via `fromRulesetIntId` with a deterministic `rs-{intId}` fallback. New `RuleFacade.get_rule_set(rule_set_id)` on the Protocol (in-memory + vendor) raises `E_BE_NOT_FOUND` on unknown ids. New `GET /rules/{id}/set` route returns the committed envelope wrapped in the standard success envelope, distinct URL from `GET /rules/{id}` (which still returns a `CatRule`) so the two wire shapes never overload. `loadRuleSet.ts` retargeted from `/rules/{id}` to `/rules/{id}/set` so the reload-server conflict resolver + `reconcileDrafts` hit the correct endpoint. Tests: `BE/tests/test_rules_roundtrip.py` (byte-identical envelope after PUT+GET, 404 on missing), `src/lib/rules/__tests__/envelopeRoundtrip.test.ts` -> **3 passed**. Not fixed (deliberate): editor conflict resolver rebinding via `envelopeToProjectRuleset` (lands with samples/audits in Step 143+); SQLite `RuleFacade` (Step 143). Version bump: v4.79.0 -> v4.80.0.

## v4.79.0 - 2026-07-21

Plan 90 Step 139: **Envelope adapter + ruleset-id alias.** New `src/lib/rules/envelopeAdapter.ts` converts a legacy `RuleSet` from `useProjectStore` into a wire-ready `RuleSetEnvelope`: deterministic EditorRuleKind mapping (C/K -> match, R -> presence, S -> absence, E -> measure), `isCategory` rows stripped with reported drop count, lossy fields preserved under namespaced `_Legacy*` params so Step 142's reverse mapping is lossless. New `src/lib/rules/ruleset-id-alias.ts` mints stable positive integer aliases for ruleset string ids (`ca.rulesetIdAlias.v1`) with a `fromRulesetIntId` reverse lookup for boot-toast deep-links. `[envelopeAdapter] serialized` logs on every call. Tests: `bunx vitest run src/lib/rules/__tests__/envelopeAdapter.test.ts` -> **6 passed**. Not fixed (deliberate): Save-button mount in editor drawer (Step 140); boot-toast deep-link (Step 141); reverse adapter + SQLite `RuleFacade` (Step 142). Version bump: v4.78.0 -> v4.79.0.

## v4.78.0 - 2026-07-21

Plan 90 Step 138: **Shell boot mount for `reconcileDrafts()`.** New `src/lib/rules/bootReconcile.ts` runs the reconciliation pass once per page load (StrictMode-safe, in-flight deduped) and emits one sonner `toast.warning` per `server-newer` / `local-newer` entry so drift is impossible to miss at boot. Never throws (fatal path logged as `[bootReconcile] fatal`); structured `[bootReconcile] complete { total, drift, missing, failed }` summary per run. Wired into `src/routes/__root.tsx`. Tests: `bunx vitest run src/lib/rules/__tests__/bootReconcile.test.ts` -> **4 passed**. Not fixed (deliberate): editor drawer mount + toast deep-link (Step 139); SQLite `RuleFacade` (Step 140). Version bump: v4.77.0 -> v4.78.0.

## v4.77.0 - 2026-07-21

Plan 90 Step 137: **`reconcileDrafts()` boot reconciliation.** New `src/lib/rules/reconcileDrafts.ts` enumerates every IndexedDB draft, GETs `/rules/{id}` via `loadRuleSet`, and classifies drift into five kinds (`in-sync` / `server-newer` / `local-newer` / `server-missing` / `load-failed`). Never throws (transport failures land as `load-failed`); optional `purgeMissing` deletes 404 drafts; one structured `[reconcileDrafts]` log line per entry (`console.info` for in-sync, `console.warn` with `{ RuleSetId, Kind, LocalVersion, ServerVersion, Error }` for drift) so silent divergence is impossible. Tests: `bunx vitest run src/lib/rules/__tests__/reconcileDrafts.test.ts` -> **4 passed**. Not fixed (deliberate): shell boot mount + auto-open modal (Step 138); editor drawer mount for `SaveRuleSetButton` (Step 139); SQLite `RuleFacade` (Step 140). Version bump: v4.76.0 -> v4.77.0.

## v4.76.0 - 2026-07-21

Plan 90 Step 136: **Save-button + conflict-resolver wiring.** New `src/lib/rules/loadRuleSet.ts` (typed `LoadRuleSetError`, mirrors server envelope to IDB with `Origin: "server"`), `src/lib/rules/useSaveConflictResolvers.ts` (binds `useSaveRuleSet` to the three `SaveConflictModal` props: reload GETs + resets, overwrite rebases `Version` + re-saves, cancel resets; failures throw instead of swallow, catches log `{ code, RuleSetId }`), and `src/features/rules/save/SaveRuleSetButton.tsx` (composed button + modal; `aria-busy`, `onSaved(committed)` deduped via `savedAt` ref). Tests: `bunx vitest run src/lib/rules/__tests__/useSaveConflictResolvers.test.ts src/features/rules/save/__tests__/SaveConflictModal.test.tsx src/lib/rules/__tests__/useSaveRuleSet.test.ts` -> **14 passed**. Not fixed (deliberate): editor drawer mount (Step 137); boot reconciliation (Step 138); SQLite `RuleFacade` (Step 139). Version bump: v4.75.0 -> v4.76.0.

## v4.75.0 - 2026-07-21

Plan 90 Step 135: **`SaveConflictModal` bound to `useSaveRuleSet` conflict state.** Presentational component renders only when `state.kind === "conflict"`; shows wire `E_BE_CONFLICT` code + `backendMessage` verbatim; three resolvers (`onReloadServer`, `onOverwriteLocal`, `onCancel`) injected as props so the modal never fetches, never touches IndexedDB, never calls `saveRuleSet`. `busy` ref serialises clicks; resolver throws are logged with `{ kind, error }` context, never swallowed. `role="alertdialog"` + `--ca-*` / `--hmi-*` tokens only. Tests: `bunx vitest run src/features/rules/save/__tests__/SaveConflictModal.test.tsx` -> **4 passed**. Not fixed (deliberate): editor Save-button wiring (Step 136); reload/overwrite resolver implementations (Step 136); boot reconciliation via `listDraftIds()` (Step 137). Version bump: v4.74.0 -> v4.75.0.

## v4.74.0 - 2026-07-21

Plan 90 Step 134: **FE `useSaveRuleSet` hook (state machine over `saveRuleSet`).** Explicit `SaveState` union `idle | saving | saved | conflict | error`; `save()` transitions saving -> saved on 2xx (mirrored to IndexedDB via `putDraft`), -> conflict on `E_BE_CONFLICT` (409) with `localEnvelope` retained for the Step-135 modal, -> error on any other `SaveRuleSetError`. Monotonic seq ref discards stale results; console logs surface conflict + error paths with `{ code, httpStatus, RuleSetId }` context. Tests: `bunx vitest run src/lib/rules/__tests__/useSaveRuleSet.test.ts` -> **6 passed**. Not fixed (deliberate): no editor call-site wiring (Step 135); no conflict-resolution modal UI (Step 135); no boot reconciliation (Step 136). Version bump: v4.73.0 -> v4.74.0.

## v4.73.0 - 2026-07-21

Plan 90 Step 133: **BE `PUT /rules/{id}` + `RuleFacade.save_rule_set` + FE `saveRuleSet` client.** New route validates the body with `parse_envelope`, enforces `path == body.RuleSetId`, and returns the committed envelope with a bumped server `Version`, `DraftMeta.Origin="server"`, and a fresh UTC `UpdatedAt`. `InMemoryRuleFacade` raises `E_BE_CONFLICT` (409) on stale-Version submissions; `VendorRuleFacade` raises `E_SDK_INIT_FAILED`. FE `src/lib/rules/saveRuleSet.ts` PUTs the identical PascalCase envelope, mirrors the committed record back into IndexedDB via `putDraft`, and throws a typed `SaveRuleSetError` that carries `code` / `httpStatus` / `backendMessage` from the Universal Response Envelope. Tests: 37 pass. Version bump: v4.72.0 -> v4.73.0.

## v4.72.0 - 2026-07-21

Plan 90 Step 132: **FE IndexedDB draft store `src/lib/rules/draftStore.ts` mirroring the Step-131 `RuleSetEnvelope` byte-for-byte.** New browser-only module wraps `idb-keyval` under key `rs-draft:<RuleSetId>` with `putDraft` / `getDraft` / `deleteDraft` / `listDraftIds`. Inline `assertValid` runs the same schema checks as the BE `parse_envelope` so bad payloads throw in the browser before hitting the wire. Quick edits stay local (Origin: "indexeddb"); the Save button (Step 133) reuses this exact envelope. Tests: 12 pass. Version bump: v4.71.0 -> v4.72.0.

## v4.71.0 - 2026-07-21

Plan 90 Step 131: **Ruleset draft-save contract (`RuleSetEnvelope`) + spec `spec/21-app/80-ruleset-draft-save.md`.** New pure module `BE/app/domain/rule_set.py` freezes the JSON shape that both IndexedDB drafts and SQLite commits must use: `{SchemaVersion, RuleSetId, Name, Version, Enabled, Rules[{Id, Kind, Enabled, Shape, Tolerance, Params}], DraftMeta{ClientId, UpdatedAt, Origin}}`. `parse_envelope(raw)` raises `E_BE_BAD_REQUEST` on every violation; no new error codes introduced. Flow (spec): quick edits -> IndexedDB with `Origin: "indexeddb"`; Save button -> PUT /rules/{id} with identical envelope -> SQLite upsert -> response mirrored back to IndexedDB with `Origin: "server"`. Tests: 11 pass. Version bump: v4.70.0 -> v4.71.0.

## v4.70.0 - 2026-07-21

Plan 90 Step 130: **Rollback-on-critical-failure module + `bin/install-rollback.py` CLI.** New pure module `BE/app/installer_rollback.py` exposes `plan_rollback` (returns LIFO-ordered actions added by the failed upgrade attempt, identified by `(Name, StartedAt)` tuples so repeat-name entries are precise), `restore_manifest` (atomic same-dir tmp + `os.replace`), and `load_backup`. New CLI `bin/install-rollback.py` with `--install-root`, `--backup-path`, `--failed-action`, `--dry-run` prints single-line JSON `{FailedAction, ActionsToReverse, PriorVersion, CurrentVersion, BackupPath, RestoredPath, DryRun}`; exits 0 / 2 / 50. One new registered error code `E_INSTALL_ROLLBACK_FAILED` -> 503 covers missing/unreadable/invalid backup, diverged history, empty `failed_action`, and atomic-write failure. Deliberately refuses to rollback against a diverged history (backup entry missing from current) rather than reversing wrong entries. Root cause guarded (one sentence): Step 129 shipped the upgrade planner + manifest backup but nothing consumed the `BackupPath` when a subsequent critical action failed, so a half-applied upgrade left `install.json` recording partial success with no supported path back to the prior good state. Verified: `pytest BE/tests/app/test_installer_rollback.py -q` -> **16 passed** (4 planner + 4 restore + 3 load-backup + 5 CLI subprocess). Full `BE/tests/app/` suite: **581 passed, 1 skipped**. Version bump: v4.69.0 -> v4.70.0.

## v4.69.0 - 2026-07-21

Plan 90 Step 129: **Wire `bin/install-upgrade-plan.py` into both installer wrappers between the doctor and the plan renderer, and add `--force-reinstall` / `--allow-downgrade` wrapper flags with a dedicated exit-code range.** `packaging/installers/install.sh` gains `--force-reinstall` / `--allow-downgrade` flags and an INSTALL-only block that invokes the Step-128 CLI with `--install-root`, `--new-version`, `--backup`, mapping CLI 40/41/42 -> wrapper 7/8/9. `packaging/installers/install.ps1` gains `[switch]$ForceReinstall` / `[switch]$AllowDowngrade` and reserves 9534/9535/9536 in the 9500-9599 wrapper-only range with the parallel 40/41/42 mapping. Block placement is strict on both wrappers: AFTER the doctor (so a broken preflight aborts before `install.json` is touched) and BEFORE the plan renderer + per-action recorder loop (so a blocked downgrade never records anything). Skipped on `--uninstall` (no version comparison to make). Root cause guarded (one sentence): Step 128 shipped the planner + CLI but nothing dispatched it, so repeat `--install` still had no typed decision layer. Verified: `pytest BE/tests/app/test_installer_wrapper_upgrade_wiring.py BE/tests/app/test_installer_upgrade.py -q` -> **37 passed** (11 new contract tests + 26 from Step 128). Full `BE/tests/app/` suite: **565 passed, 1 skipped**. Version bump: v4.68.0 -> v4.69.0.

Plan 90 Step 128: **Upgrade-in-place decision planner + manifest backup + `bin/install-upgrade-plan.py` CLI.** New pure module `BE/app/installer_upgrade.py` exposes `UpgradeAction` (`fresh-install` / `upgrade` / `reinstall-same` / `downgrade-allowed`), `UpgradePolicy(is_force_reinstall, is_downgrade_allowed)`, frozen `UpgradeDecision`, `parse_version(raw) -> (major, minor, patch)` accepting `v4.67.0` / `4.67` / `4` / `4.67.0-rc.1`, `plan_upgrade(*, existing, new_version, policy)` (returns fresh/upgrade/reinstall-same/downgrade-allowed, raises `E_INSTALL_DOWNGRADE_BLOCKED` otherwise), and `backup_manifest(install_root, *, now)` copying `install.json` -> `install.json.bak.<UTC>` atomically. Two new registered error codes: `E_INSTALL_DOWNGRADE_BLOCKED` -> 409 Conflict, `E_INSTALL_UPGRADE_INVALID` -> 422. New CLI `bin/install-upgrade-plan.py` prints single-line PascalCase JSON; exit codes 0/2/40/41/42. Root cause guarded (one sentence): a second `--install` had no decision path, so it either silently downgraded or crashed on `init_manifest`. Verified: `pytest BE/tests/app/test_installer_upgrade.py -q` -> **26 passed** (7 parse-version, 7 planner, 3 backup, 5 CLI subprocess). Full `BE/tests/app/` suite: **554 passed, 1 skipped**. Version bump: v4.67.0 -> v4.68.0.

Plan 90 Step 127: **Wire the Step-126 PATH-link CLI into `installer_plan.py`, both installer wrappers, `bin/install-doctor.py`, and the manifest audit trail.** `plan_install_actions` gains `binaries_dir: str | None = None` and now returns three actions per phase: INSTALL = `[db-bootstrap, path-link, retention-timer]` (path-link critical, `install --binaries-dir <dir> --platform {windows,posix}`), UNINSTALL = `[retention-timer, path-link, db-bootstrap]` (path-link non-critical, `uninstall --platform ...`). Missing `binaries_dir` on INSTALL raises `E_CLI_USAGE`. Both installer wrappers forward the resolved binaries dir into the planner heredoc; `bin/install-doctor.py` grows optional `--binaries-dir` (wrappers pass it through) and falls back to `<unset>` on install so preflight still runs when a dev-loop caller omits it. `path-link` rows now appear in `install.json` automatically via the existing per-action recorder loop. Root cause guarded (one sentence): Step 126 shipped the CLI but nothing dispatched it, so shims were never created on install and never removed on uninstall. Verified: `pytest BE/tests/app/test_installer_plan.py BE/tests/app/test_installer_doctor_and_record.py -q` -> **38 passed** (+5 new path-link cases + 3 generalized order/doctor cases). Version bump: v4.66.0 -> v4.67.0.

Plan 90 Step 126: **PATH-link lifecycle module + CLI.** New pure module `BE/app/installer_path.py` (`LinkPlatform`, `LinkAction`, `default_link_dir`, `plan_link_actions`, `apply_link_install`, `apply_link_uninstall`) iterates the frozen `BE.app.installer_binaries.BINARIES` inventory to compute per-binary shim paths under `%LOCALAPPDATA%\vision-app\bin` (Windows) or `~/.local/share/vision-app/bin` (POSIX). Windows install writes an atomic `.cmd` shim (`@call "<abs-source>" %*`) so no Developer-Mode / admin symlink privilege is required; POSIX install materializes a `symlink` via same-dir tmp + `os.replace` for idempotent replacement. Uninstall is idempotent and refuses to remove a directory / special file at the link path. New error code `E_INSTALL_PATH_LINK_FAILED` (HTTP 503). New CLI `bin/install-path-link.py` with `install` / `uninstall` subcommands, `--binaries-dir` / `--link-dir` / `--platform` (auto-detect), numeric exits `0` / `2` / `30`. Root cause guarded (one sentence): Steps 117-125 built and verified the onefile `.exe` payloads but left them stranded in an out-of-PATH release directory, so an operator finishing `install.ps1` still had to type an absolute path and any second install silently shadowed the previous shim without a recorded uninstall trail. Verified: new `BE/tests/app/test_installer_path.py` (13 cases: planner Windows + POSIX + arg validation, `default_link_dir` env resolution, POSIX symlink install + idempotent replace, Windows `.cmd` shim body, missing-source raise, uninstall idempotent + directory refusal, plus 3 CLI numeric-exit cases) -> **13 passed**. Not wired into `installer_plan.py` or the installer wrappers yet (Step 127). Version bump: v4.65.0 -> v4.66.0.

## v4.65.0 - 2026-07-21

Plan 90 Step 125: **`verify-install` CI gate lands + Step-124 SHA256SUMS "skipped" fallback removed from both installers.** New `.github/workflows/verify-install.yml` (two jobs: `posix` on `ubuntu-latest`, `windows` on `windows-latest`, paths-scoped `pull_request` + `push: main`) fabricates a fake release directory whose exe payloads and `SHA256SUMS.txt` are driven by `BE.app.installer_binaries.BINARIES` (POSIX leg text-mode `<hex>  <name>`, Windows leg binary-mode `<hex> *<name>.exe`) and runs each installer end-to-end for three cases: HAPPY (`--verify-only` / `-VerifyOnly` -> `0`), TAMPERED (one byte flipped -> `6` on bash / `9533` on ps1), MISSING BINARIES DIR (`--install` / `-Install` without `APP_BINARIES_DIR` -> `2`). Both installers now carry the guard `phase=install AND binaries_dir empty -> exit 2` and a new short-circuit flag (`--verify-only` / `-VerifyOnly`) so CI never needs a real `.venv`, doctor, or plan renderer; `install.ps1` also gains `$EXIT_USAGE = 2`. The Step-124 `SHA256SUMS cross-check skipped` log branches are gone from both scripts. Root cause guarded (one sentence): Step 124 wired both installers through `bin/install-verify-sums.py` but nothing in CI actually ran `install.sh` / `install.ps1` end-to-end, so a broken checksum block (missing exit translation, wrong flag, silent skip re-introduced) would only be caught by a human on Windows. Verified: new `BE/tests/app/test_verify_install_workflow.py` (6 cases: workflow parses, both jobs assert the HAPPY / TAMPERED / MISSING triad, both installers stripped of the skip fallback and carry the new flag, plus a bash smoke asserting `0`/`6`/`2`) -> **6 passed**; local dry-run of `install.sh` against a fabricated `/tmp/ci-release` -> exits `0`, `6`, `2`. Version bump: v4.64.0 -> v4.65.0.

## v4.64.0 - 2026-07-21

Plan 90 Step 124: **`SHA256SUMS.txt` cross-check inside `packaging/installers/install.ps1` and `install.sh`.** Introduces `BE/app/installer_checksums.py` (`parse_sums_file`, `verify_release_binaries`, `VerifiedBinary`) with a strict GNU coreutils parser that accepts both text mode (`<hex>  <name>`) and binary mode (`<hex> *<name>`) so the Windows and Linux release-workflow legs converge on one format, refuses duplicate filenames, and iterates `BE.app.installer_binaries.BINARIES` so a new frozen entry participates automatically. New CLI `bin/install-verify-sums.py` maps `AppError` to numeric exits: `10` = `E_INSTALL_MANIFEST_MISSING` (sums file), `11` = `E_INSTALL_MANIFEST_INVALID` (malformed / duplicate / inventory-missing-from-sums), `12` = `E_INSTALL_MANIFEST_MISSING` with `Reason=binary-file-missing`, `13` = `E_CLI_CHECKSUM_MISMATCH` (spec 77 §4). Both installers gain a pre-doctor block that runs the verifier when `-BinariesDir` / `APP_BINARIES_DIR` is set and collapses any non-zero verify exit to `9533` on PowerShell and `6` on bash; when the binaries dir is unset both installers log a clear `SHA256SUMS cross-check skipped` line so the dev-loop install-from-source flow keeps working until Step 125's `verify-install` CI job makes the artefact directory mandatory. Verified: `python3 -m pytest BE/tests/app/test_installer_checksums.py -q` -> **14 passed** (parser text + binary mode, missing sums, malformed lineno, duplicate filename, happy-path windows + posix, missing binary, mismatch, inventory-missing-from-sums, plus 5 CLI numeric-exit contract cases). Version bump: v4.63.0 -> v4.64.0.

## v4.63.0 - 2026-07-21

Plan 90 Step 123: **Release workflow scaffold `.github/workflows/release.yml` with windows-latest + ubuntu-latest matrix, PyInstaller onefile builds, and per-platform SHA256SUMS.txt.** Root cause guarded (one sentence): Steps 117-122 landed the PyInstaller specs, `install.json` `Binaries` schema, `record-binary` CLI, and the Pester exit-code gate, but nothing on the release path actually built or published the onefile exes, so every downstream consumer (SHA256 cross-check in `install.ps1`, `verify-install` CI job, signing job, release page) was validating synthetic artefacts only. Minimum correct fix: (1) new `.github/workflows/release.yml` with `workflow_dispatch` trigger (tag trigger `on: push: tags: [v*]` is documented but commented out until Step 125's `verify-install` gate lands so we can never publish an unverified release); (2) `concurrency: group: release-${{ github.ref }} cancel-in-progress: true` so a rapid re-dispatch cannot race two builds writing the same artefact name; (3) `build` job with `fail-fast: false` matrix (`windows-latest` -> `.exe` outputs, `ubuntu-latest` -> POSIX ELF outputs, both using the SAME `.spec` files from Step 117 so the frozen entry surface is identical); (4) `PYTHONHASHSEED=0` + `PYTHONDONTWRITEBYTECODE=1` + pinned `pyinstaller==6.11.1` keep SHA256 stable across re-runs on the same host (paired with the Step-117 `strip=False`, `upx=False` spec invariants); (5) `Emit BINARIES inventory` step reads `BE.app.installer_binaries.BINARIES` as JSON so the build loop and the SHA256 loop iterate the same source of truth (a new `BinaryEntry` added to the frozen tuple is picked up here with zero workflow edits); (6) `Build each onefile spec` iterates that JSON and runs `pyinstaller <spec> --clean --noconfirm --distpath dist --workpath build` (--clean forces a fresh build/ dir so a cached .pyc from a prior matrix leg cannot leak into another arch); (7) `Compute SHA256SUMS.txt` emits `<hex><space><space><filename>` per binary in GNU coreutils format so `sha256sum -c` on Linux and PowerShell `Get-FileHash` comparison in Step-124's `install.ps1` both parse it, and hard-fails when a build artefact is missing (no silent skips); (8) `actions/upload-artifact@v4` with `if-no-files-found: error` and 14-day retention per platform; (9) `publish` job gated on `publish_draft == 'true' AND release_tag != ''` (two-input gate makes accidental publication a syntax error) uses `softprops/action-gh-release@v2` to cut a _draft_ release with `prerelease` auto-flagged on `-rc` / `-beta` tag suffixes, flattens per-platform artefact bundles under `release/` prefixed with the platform, emits one authoritative combined `SHA256SUMS.txt`, and requires `permissions: contents: write`; (10) release body documents that binaries are NOT Authenticode-signed (signing lands in Step 141) so consumers know `install.json` rows will carry `Signed=false`. Tests: new `BE/tests/app/test_release_workflow_scaffold.py` (5 cases) - workflow file exists, declares matrix + concurrency + two-input publish gate, every `BINARIES` `SpecPath` resolves on disk (parametrized so a new entry auto-covers itself), and the workflow imports `BE.app.installer_binaries` (contract that the inventory-driven loop is preserved). Verified: `pytest BE/tests/app/test_release_workflow_scaffold.py -q` -> **5 passed**; YAML parses cleanly under `pyyaml`, matrix and triggers reflect the intent (`jobs: ['build','publish']`, `triggers: ['workflow_dispatch']`, matrix = `[windows-latest .exe, ubuntu-latest '']`); inventory JSON round-trip produces the expected two-row payload (`db-bootstrap`, `retention-run`); Pester harness unchanged and green at **11 passed** from Step 122. Not fixed (deliberate scope): no Authenticode signing (Step 141 signed Windows runner); no `install.ps1` / `install.sh` SHA256 cross-check (Step 124); no end-to-end `verify-install` CI job (Step 125); tag-trigger commented out until (125) lands - all deliberate per the plan sequencing. Version bump: v4.62.0 -> v4.63.0.

## v4.62.0 - 2026-07-21

Plan 90 Step 122: **Extend the Pester harness to cover `Register-RetentionTask.ps1` 9520 / 9521 / 9524.** Root cause guarded (one sentence): Step 120 only exercised `Invoke-DbBootstrap.ps1` and `Invoke-RetentionRun.ps1`, so `Register-RetentionTask.ps1` exits 9520 (schtasks.exe missing) and 9524 (neither pwsh.exe nor powershell.exe on PATH) could silently regress on any `Common.psm1` or `Resolve-PwshExe` edit and nothing in CI would catch it. Minimum correct fix: (1) `Invoke-Wrapper` gains `-ExtraArgs [string[]]` and `-PathOverride [string]` so a call site can pass `-Install` to the wrapper and rewrite `$env:PATH` around the child `pwsh` spawn (restored in `finally` so parallel tests do not leak env state); (2) new helper `New-FakeSchtasks -FakeRepo` materialises an executable `schtasks.exe` shim (POSIX gets `chmod +x`) in `<FakeRepo>/fakebin/` so `Get-Command schtasks.exe` resolves on POSIX pwsh and `Assert-Schtasks` passes without needing the real Windows binary (the stub is never actually invoked; `Render-Xml` exits at 9521 / 9524 first); (3) new `Describe 'Register-RetentionTask.ps1 exit-code contract'` block with three `It` cases: `9520` (PathOverride=''), `9521` (template intentionally absent while venv-python + `Invoke-RetentionRun.ps1` are populated so template-missing is the only trigger), and `9524` (all three Render-Xml preflight artifacts present so the flow reaches `Resolve-VisionAppPwshExe`, which returns `$null` because PATH contains only the fake schtasks dir); (4) each case asserts both `ExitCode = Expected-Exit -Full <code>` (POSIX 8-bit truncation aware: 9520 -> 48, 9521 -> 49, 9524 -> 52) and a stderr grep for the `[NNNN]` marker plus the human-readable substring, so the assertion survives POSIX exit-code truncation. 9522 (renderer rejected knobs) and 9523 (schtasks /Create failed) are deliberately deferred to the signed-runner release workflow: 9522 needs a working `BE.app` import graph inside the venv python, 9523 needs a schtasks stub that returns non-zero AND a writable `%TEMP%` shape; both land with Step 121 (release workflow scaffold). Verified: `nix run nixpkgs#powershell -- -NoProfile -Command "Invoke-Pester -Path tests/pester/Wrappers.Tests.ps1 -Output Detailed"` -> **11 passed, 0 failed** (was 8; +3 Register cases). No BE code changed, so `pytest BE/ -q` unchanged at 1256 passed / 2 skipped. Not fixed (deliberate scope): 9522 / 9523 coverage; Windows-only exit-code full 32-bit assertions (CI still runs on ubuntu-latest until Step 121 wires a `windows-latest` matrix leg); no PSScriptAnalyzer rule added specifically for `Register-RetentionTask.ps1` (the existing Step-116 job already covers it). Version bump: v4.61.0 -> v4.62.0.

## v4.61.0 - 2026-07-21

- Plan 90 Step 120 shipped: **Pester harness for `scripts/ps/` wrapper exit-code contract + `pester-wrappers` CI job.**
- New `tests/pester/Wrappers.Tests.ps1` (Pester 5/6): 8 tests, 3 `Describe` blocks. Common.psm1 sanity (exports present, `Get-VisionAppRepoRoot` returns real checkout, `Get-VisionAppVenvPython` returns OS-appropriate path). `Invoke-DbBootstrap.ps1` 9510/9511. `Invoke-RetentionRun.ps1` 9530/9531.
- Fake-repo harness: `New-FakeRepo` copies `Common.psm1` + the wrapper under test into `<tempRoot>/scripts/ps/` so the wrapper's two-parent walk resolves back to a controllable root. `Invoke-Wrapper` spawns a child pwsh, captures `$LASTEXITCODE` + redirected stderr.
- Exit-code portability: `Expected-Exit` honours POSIX 8-bit truncation (`9510 -> 38`, `9511 -> 39`, `9530 -> 58`, `9531 -> 59`) while pinning the full 32-bit code on Windows. Every case also grep-matches the `[NNNN]` stderr marker which is truncation-immune.
- New CI job `pester-wrappers` in `.github/workflows/ci.yml`: ubuntu-latest, installs Pester >= 5.0.0, runs `Invoke-Pester -Path tests/pester -Output Detailed -PassThru`, fails on `FailedCount > 0`.
- Root cause guarded (one sentence): Step 116 wired PSScriptAnalyzer so wrapper syntax is gated, but nothing ran the wrappers to prove the numeric exit contract still fires on missing-venv / missing-target-script.
- Verified locally: `nix run nixpkgs#powershell -- -NoProfile -Command "Invoke-Pester -Path tests/pester/Wrappers.Tests.ps1 -Output Detailed"` -> **8 passed, 0 failed**.

## v4.60.0 - 2026-07-21

- Plan 90 Step 119 shipped: **`bin/install-record.py record-binary` subcommand + backward-compat argv shim.**
- CLI refactored to argparse subparsers: `record-action` (unchanged Step-106 behaviour) and `record-binary` (new). Flat pre-Step-119 invocations still work: an argv shim in `_parse_args` prepends `record-action` when the first token starts with `--`, so `packaging/installers/install.ps1` and `install.sh` keep working with no coordinated rev.
- `record-binary` flags: `--install-root`, `--app-version`, `--platform`, `--name`, `--exe-name`, `--exe-path`, `--signed {true,false}`, optional `--cert-thumbprint` and `--timestamped-at`. Calls `compute_binary_signature` then `record_binary`; enforces `--signed true` requires `--cert-thumbprint` before touching disk; emits one JSON stdout line `{Name, Sha256, SizeBytes, Signed}` so the release workflow can cross-check `SHA256SUMS.txt` (Step 121).
- New exit code `4` = referenced exe missing on disk (distinct from `2`=validation and `3`=manifest unwritable).
- Root cause guarded (one sentence): before Step 119 the only writer of `install.json` was the flat action-append path, so `manifest.Binaries` stayed empty forever and the Step-118 `BinaryChecksumMismatch` doctor check was structurally unreachable in production.
- Tests: 7 new cases in `BE/tests/app/test_install_record_binary_cli.py`.
- Verified: full BE suite `pytest BE/ -q` -> **1256 passed, 2 skipped** (+7 vs v4.59.0).

## v4.59.0 - 2026-07-21

- Plan 90 Step 118 shipped: **`install.json` schema v2 + `installer_signing` helper + doctor tamper cross-check.** New `ManifestBinaryRecord` persists one row per shipped exe with SHA256, size, signed-flag, cert thumbprint, and RFC 3161 timestamp. `MANIFEST_SCHEMA_VERSION` bumped to 2; v1 manifests read fine and upgrade on next write.
- New `BE/app/installer_signing.py`: `sha256_of_file` (streaming, 1 MiB chunks) + `compute_binary_signature(name, exe_name, exe_path, signed, cert_thumbprint, timestamped_at)` returning a validated `ManifestBinaryRecord`.
- New `record_binary(install_root, binary)` upserts by `Name`; `latest_binary(manifest, name)` mirrors `latest_action`.
- `run_doctor` cross-checks every persisted Binary row: `BinaryFileMissing` ERROR when the recorded Path is gone, `BinaryChecksumMismatch` ERROR when the on-disk SHA256 or size drifts from the manifest.
- Root cause guarded (one sentence): Step 117 defined the `BINARIES` inventory but neither `install.json` nor any doctor path fingerprinted the produced `.exe`, so a tampered or replaced binary on disk was invisible to consumers and every uninstall assumed the on-disk file was still the one the release published.
- Tests: 12 new cases in `BE/tests/app/test_installer_signing.py`; two Step-105 tests updated for the v2 schema.
- Verified: full BE suite `pytest BE/ -q` -> **1249 passed, 2 skipped** (+11 vs v4.58.0).

## v4.58.0 - 2026-07-21

- Plan 90 Step 117 shipped: **PyInstaller onefile specs + `installer_binaries` inventory + doctor enforcement.** New `packaging/pyinstaller/db-bootstrap.spec` and `retention-run.spec` freeze each CLI into a deterministic onefile console `.exe` (`upx=False` for stable SHA256, `console=True` for envelope-on-stdout, stable output names). The bootstrap spec bundles `BE/db/migrations` as `datas` so the frozen exe runs without a repo checkout on the target host.
- New pure module `BE/app/installer_binaries.py` exposes a frozen `BINARIES` tuple (Name, SpecPath, EntryScript, ExeName, Purpose) plus `binary_presence(repo_root)`. `run_doctor(..., repo_root=repo_root)` now populates `DoctorReport.Binaries` and emits `BinarySpecMissing` / `BinaryEntryMissing` ERROR findings when a spec or its entry script is missing; `to_dict` and `render_human` surface the new inventory.
- 8 new tests in `BE/tests/app/test_installer_binaries.py` pin inventory shape, immutability, real-checkout presence, empty-root behaviour, doctor severity/count, `to_dict` payload, backward compat when `repo_root` is omitted, and spec-file invariants.
- Root cause guarded (one sentence): shipping loose `.py` files forced every Windows operator to pre-install a matching Python + venv before any wrapper could run, so a partial interpreter surfaced as an opaque wrapper `9530=venv-missing` exit with no fallback binary and nothing on the release path failed fast when a spec was missing.
- Verified: full BE suite `pytest BE/ -q` -> **1238 passed, 2 skipped** (+9 vs v4.57.0).

## v4.57.0 - 2026-07-21

- Plan 90 Step 116 shipped: **`scripts/ps/Common.psm1` shared PowerShell helpers + PSScriptAnalyzer CI.** New module exports `Get-VisionAppRepoRoot`, `Get-VisionAppIsWindows`, `Get-VisionAppVenvPython`, `Resolve-VisionAppPwshExe`. Three wrappers now `Import-Module Common.psm1` and delegate the duplicated blocks. `WRAPPERS` inventory grew to 4 entries so `WrapperMissing` catches a Common.psm1 deletion. New `psscriptanalyzer` CI job lints `scripts/ps/` on every PR. Tests: 4 new cases (52 passing in installer suite, +4). Full BE: 1229 passing.

## v4.56.0 - 2026-07-21

- Plan 90 Step 115 shipped: **Installer wrapper inventory + doctor enforcement.** New `BE/app/installer_wrappers.py` module lists every shippable PowerShell wrapper (`Invoke-DbBootstrap`, `Invoke-RetentionRun`, `Register-RetentionTask`) as a frozen tuple with Name/Path/Platform/Purpose.
- `run_doctor(..., repo_root=repo_root)` now enforces that inventory: every Windows wrapper missing from disk becomes a `WrapperMissing` ERROR that blocks the installer, and `DoctorReport.Wrappers` carries the full presence table into `install.json` consumers and the operator-facing `render_human` summary.
- `bin/install-doctor.py` adds `--repo-root` (defaults to the CLI file's repo root) and both `packaging/installers/install.ps1` and `install.sh` forward it, so every install/uninstall on either platform now fails fast when the wrapper set is incomplete instead of dying inside the action loop.
- 11 new tests in `BE/tests/app/test_installer_wrappers.py` pin the inventory shape, presence rules, WrapperMissing severity, POSIX-skips-Windows behaviour, and backward compatibility when `repo_root` is omitted.
- Verified: full BE suite `pytest BE/ -q` -> **1225 passed, 2 skipped** (+11 vs v4.55.0).

## v4.55.0 - 2026-07-21

- Plan 90 Step 114 shipped: **Retarget the Windows retention scheduled task at the Step-113 wrapper.** `packaging/windows/vision-app-retention-task.xml.tmpl` now emits `<Command>${PWSH_EXE}</Command>` + `<Arguments>-NoProfile -ExecutionPolicy Bypass -File "${WRAPPER_SCRIPT}" --loop --interval-hours ${INTERVAL_HOURS} --retention-days ${RETENTION_DAYS}</Arguments>` so scheduled runs inherit the wrapper's 9530/9531 stderr contract instead of shelling straight into `python`.
- `BE/app/retention_installer.py::render_windows_task_xml(...)` kwargs renamed `python_exe` -> `pwsh_exe`, `retention_script` -> `wrapper_script`; the XML injection guard (no `"` `<` `>` `\x00`) still applies to both, so hardcoded quotes around `${WRAPPER_SCRIPT}` cannot be broken by a caller-supplied path.
- `scripts/ps/Register-RetentionTask.ps1` grows a `Resolve-PwshExe` helper (prefers `pwsh.exe`, falls back to `powershell.exe`, else exits **9524** via `[Console]::Error.WriteLine` so `$ErrorActionPreference='Stop'` cannot swallow the code) and a wrapper-presence `Test-Path` preflight that reuses the existing 9521 slot.
- Spec `spec/21-app/79-installer-retention-timing.md` updated: signature line for `render_windows_task_xml` now shows `pwsh_exe` / `wrapper_script`, validation bullet widened to cover all four path kwargs.
- Root cause guarded (one sentence): Step 113 shipped the wrapper but nothing pointed the scheduled task at it, so every registered task still ran raw `python bin/retention-run.py` and lost the wrapper-log discipline.
- Verified: `pytest BE/tests/app/test_retention_installer.py -q` -> **25 passed**; `xml.etree` parses the rendered template with `Command.text == pwsh.exe` and `Arguments.text` containing `-File`, `Invoke-RetentionRun.ps1`, `--loop`, `--interval-hours 12`, `--retention-days 30`.

- Plan 90 Step 113 shipped: **`scripts/ps/Invoke-RetentionRun.ps1` PowerShell wrapper for `bin/retention-run.py`.** Mirrors `scripts/ps/Invoke-DbBootstrap.ps1` (Plan 90 Step 40) byte-for-byte outside the target script name, exit-slot numbers, and examples: `[CmdletBinding()]` + `ValueFromRemainingArguments`, `Set-StrictMode -Version Latest`, `$ErrorActionPreference = 'Stop'`, venv Python selected by `$IsWindows` (Windows PowerShell 5.1 default because that variable is absent there), argv forwarded via `& $VenvPython @argv` so stdout carries the Universal Envelope JSON to `ConvertFrom-Json` unchanged, and child `$LASTEXITCODE` propagated unchanged (0 = clean, 3 = domain error + halt row already persisted by Step 112 to `Mode="loop-halt"`, 4 = usage).
- Two new wrapper-only exit slots inside the reserved 9500-9599 range (`.lovable/memory/26-split-db-cli-cheatsheet.md` §12; existing: 9510-9511 db-bootstrap, 9512 cli-import, 9520-9523 register-retention-task): `9530 = venv-missing` (message includes resolved venv path; emitted via `[Console]::Error.WriteLine` NOT `Write-Error` so `$ErrorActionPreference='Stop'` cannot swallow the explicit exit), `9531 = script-missing` (guards partial checkout / botched install).
- Root cause guarded (one sentence): Step 112 made the retention audit stream durable across CLI halts, but Windows operators still had no wrapper, so `Register-RetentionTask.ps1` scheduled tasks pointed at raw `python bin/retention-run.py` and lost the wrapper-log discipline the rest of the fleet enforces.
- Verified: `python3 bin/retention-run.py --help` still prints usage (wrapper does not touch the child); `grep -c EXIT_` -> 4 (2 constants + 2 exit sites, matching canonical wrapper); full BE suite `pytest BE/ -q` -> **1214 passed, 2 skipped** (unchanged from Step 112, correctly: Windows-only wrapper, no Python surface changed).
- Docstring `.EXAMPLE` blocks match the real CLI flags (`--retention-days`, `--dry-run`, `--loop`, `--interval-hours`) verified against `--help`; a draft that used placeholder `--mode` flags was corrected before commit.

- Plan 90 Step 112 shipped: **Wire scheduler halt `AppError` into the retention audit stream as `Mode="loop-halt"` rows.** New writer surface in `BE/app/retention_audit.py`: `ALLOWED_MODES=("single-shot","loop","loop-halt")` (single source of truth shared with the endpoint), `build_halt_row(err, *, pass_index, timestamp_utc=None)` emits `{TimestampUtc, Mode:"loop-halt", PassIndex, ErrorCode, ErrorMessage, ErrorDetails}` intentionally disjoint from `RetentionOutcome` fields, and `append_halt(logs_root, err, *, pass_index, timestamp_utc=None)` writes one row via `append_and_roll` (same 1 MiB ceiling as `append_pass`), best-effort: on `OSError`/`ValueError` it logs ERROR `retention.audit.halt_write_failed` and returns `None` so the CLI's `DomainError` exit code is preserved. `build_row` now rejects `mode="loop-halt"` and points callers at `build_halt_row` (guard against smuggling halts through the outcome path). `bin/retention-run.py` loop branch: when `run_scheduled` returns `(outcomes, err)` with `err is not None`, it calls `_audit_append_halt(logs_root, err, pass_index=len(outcomes)+1)` BEFORE `_emit_error_envelope`, so the halt row is durable even if the operator kills the process before reading stdout. `GET /observability/retention` accepts `mode=loop-halt` (endpoint `_ALLOWED_MODES` widened to match the writer). Root cause guarded (one sentence): Step 111 exposed successful passes but nothing recorded failed-and-halted passes, so an operator seeing "no new pass rows since Tuesday" could not distinguish "scheduler off" from "scheduler crashed on Tuesday". Minimum correct fix: two new pure functions in the writer, a 4-line wire-in in the CLI loop-error branch, one-word widening of the endpoint mode whitelist; zero changes to `run_retention`, `run_scheduled`, `jsonl_rotator`, envelope, or existing routes.
- Verified: `pytest BE/tests/app/test_retention_audit.py BE/tests/routes/test_observability_retention.py -q` -> **30 passed** (was 21; +9); full BE suite -> **1214 passed, 2 skipped** (was 1201; +13).
- New tests (9 cases): `ALLOWED_MODES` tuple shape; halt row shape asserts every key and asserts `RunSessionsDeleted`/`ArtifactsUnlinked` are ABSENT (disjoint invariant); default ISO timestamp; `build_halt_row` rejects non-`AppError`; rejects `pass_index` in `{0,-1,1.0,"1"}`; `build_row(mode="loop-halt")` rejects with redirect message; `append_halt` writes single row with `ErrorCode="E_BE_INTERNAL"` and `AppendedCount=1`; interleaved 2 passes + 1 halt via `read_pair` preserves order `["loop","loop","loop-halt"]` with `PassIndex=[1,2,3]`; `append_halt` with `append_and_roll` monkeypatched to raise `OSError(28,"no space")` returns `None`, does NOT raise, and logs ERROR `retention.audit.halt_write_failed`; endpoint `?mode=loop-halt` returns `total=1 available=2` and the halt row carries `Mode="loop-halt"` `ErrorCode="E_BE_INTERNAL"` `ErrorMessage="db is locked"` `ErrorDetails={"K":"v"}` `PassIndex=2`.

- Plan 90 Step 111 shipped: **Expose the retention audit stream over HTTP as `GET /observability/retention`.** New `BE/routes/observability/retention.py` reads `<APP_LOG_ROOT>/retention.log` + `retention.log.1` via `BE.app.jsonl_rotator.read_pair`, optionally filters by `mode` (`single-shot`|`loop`), keeps the newest `limit` rows (default 50, hard ceiling 500), and returns them newest-first inside the standard Universal Envelope. Response `Data` includes `items`, `total`, `available` (pre-filter row count across both generations), `limit`, `currentPath`, `previousPath`, `hasCurrent`, `hasPrevious` (the two booleans distinguish "never ran" from "ran with zero deletes"). Missing files are NOT an error; poison lines are surfaced as `{"_Raw","_ParseError"}` (never silently dropped). Validation: `E_BE_BAD_REQUEST` for `limit<1|>500` or `mode` outside `{single-shot,loop}` (case-sensitive). Log-root failures wrap as `E_BE_INTERNAL` with `CauseCode` preserved; `read_pair` `OSError` -> `E_BE_INTERNAL` with `CurrentPath`/`PreviousPath`/`OsError` in details and ERROR log `retention_audit_read_failed`. `X-Correlation-Id` echoes on the response header. Registered in `BE/main.create_app` alongside runs/logs/sessions/ipc. Root cause guarded (one sentence): Step 110 persisted per-pass rows but nothing exposed them over HTTP, so operators not on the box and the future FE observability view had no way to answer "did last night's pass run?". Minimum correct fix: one read-only route module + one register line; zero changes to writer, primitive, envelope, or existing routes.
- Verified: `pytest BE/tests/routes/test_observability_retention.py -q` -> **10 passed**; full BE suite -> **1201 passed, 2 skipped** (was 1191; +10).
- New tests (10 cases): empty state matches `audit_paths` output; five appends -> newest-first `PassIndex=[5,4,3,2,1]`; `limit=3` returns newest three with `available=10`; `mode=loop` filter drops `single-shot` rows and reports `total=2 available=3`; `limit=0`/`limit=9999` -> 400 `Errors.Code=E_BE_BAD_REQUEST`; `mode=Loop` (wrong case) -> 400; incoming correlation header echoes back; rotation via monkeypatched 200-byte ceiling produces `hasPrevious=true` and returns what remains on disk (`available=2`, `PassIndex=[3,2]`); poison line between two valid rows is surfaced with `_Raw="not-json-line"` and `_ParseError` present, `available=3`.
- Not fixed (deliberate scope): no follow / long-poll mode (operators tail live via `bin/log-tail.py --current <APP_LOG_ROOT>/retention.log`); no cursor pagination beyond `limit` (each generation is bounded to 1 MiB so a full response is always small); no FE consumer yet (Plan 90 Steps 131+); no loop-halt `AppError` capture yet.

## v4.51.0 - 2026-07-21

- Plan 90 Step 110 shipped: **Persist per-pass retention outcomes to a rotated JSONL audit stream.** New `BE/app/retention_audit.py` (`append_pass`, `build_row`, `audit_paths`, `AUDIT_FILENAME="retention.log"`, `PREVIOUS_FILENAME="retention.log.1"`) writes one wire-shape row (`TimestampUtc`, `Mode`, `PassIndex`, plus full `RetentionOutcome.to_wire()`) per pass via `BE.app.jsonl_rotator.append_and_roll` with a 1 MiB per-generation ceiling. `bin/retention-run.py` resolves `logs_root = resolve_root("log", ..., ensure=True)` and calls the writer at the end of `_one_pass` for both single-shot (`Mode="single-shot"`, `PassIndex=1`) and loop (`Mode="loop"`, PassIndex increments) modes; a `pass_counter`/`audit_mode` closure keeps state without touching `run_retention` or `run_scheduled`. Best-effort semantics: `OSError`/`ValueError` are swallowed, logged ERROR `retention.audit.write_failed path=... err=...`, and return `None`, so a full disk never fails a retention pass that already committed DB deletes + unlinks. `audit_paths` deliberately does NOT mkdir so Step 111's read endpoint has zero side effects. Root cause guarded: pre-Step-110, `bin/retention-run.py` emitted outcomes only via stdout/stderr and the in-memory list, so a crash mid-loop lost every pass since the last envelope and Step 111 had no persistent source. Minimum correct fix: one new writer module + a two-line wire-in inside `_one_pass`; no changes to `run_retention`, `run_scheduled`, `jsonl_rotator`, or the envelope shape. Verified: `pytest BE/ -q` -> **1191 passed, 2 skipped** (was 1171; +20). New tests `BE/tests/app/test_retention_audit.py` (20 cases) cover `audit_paths` no-mkdir, PascalCase + ISO timestamp row shape, explicit timestamp passthrough, mode/pass_index validation, single-row append, three-pass ordering, parent dir auto-creation, rotation at a monkeypatched 200-byte ceiling, `OSError` + `ValueError` swallow-and-log, sorted-keys serialization, and default timestamp regex. Not fixed (deliberate scope): no read endpoint yet (Step 111 mounts `GET /observability/retention` over `read_pair`); no follow / `-f` mode (operators tail via `bin/log-tail.py --current <log>/retention.log`); no per-pass `AppError` capture from `run_scheduled` (loop-halt error still emits envelope `Error.*`; wire in when Step 111 needs it).

## v4.50.0 - 2026-07-21

- Plan 90 Step 109 shipped: **Unified `bin/log-tail.py` CLI over any `jsonl_rotator` current+previous pair.** Reads `<name>.log` and `<name>.log.1` via `BE.app.jsonl_rotator.read_pair` (previous-then-current for monotonic time) and prints the last N entries. Args: `--current <path>` (required), `--previous <path>` (optional, defaults to `<current>.1`), `--limit N` (default 20), repeatable `--filter Key=Value` equality filter, `--format json|human`. Poison lines from the primitive surface as `{"_Raw", "_ParseError"}` (human prefixes `POISON`), never silently truncated. Exit codes: 0 printed, 2 invalid usage (missing `--current`, `--limit <= 0`, malformed / empty-key `--filter`), 3 OSError from the primitive (e.g. `--current` is a directory). Missing files return an empty list at exit 0. Root cause guarded: Step 107's `bin/install-log-tail.py` hard-coded the installer manifest schema, so every future JSONL audit stream (retention loop summaries in Step 110, IPC audit tails, observability rings, release automation trails) would need its own bespoke tail helper (drift + operator UX fragmentation). Minimum correct fix: one thin CLI over the Step 108 primitive; no changes to `jsonl_rotator.py`, `install_log_rotator.py`, or the installer tail (kept for its schema-aware human formatting). Verified: `pytest BE/ -q` -> **1171 passed, 2 skipped** (was 1155; +16). New tests `BE/tests/app/test_log_tail_cli.py` (16 cases) drive the CLI via subprocess with `PYTHONPATH=<repo>` and cover previous-then-current ordering, `--limit` returns last N, `--filter Kind=A` equality, default-previous fallback, missing-files -> empty output, human format sorted-key serialization, poison-line surfacing, exit 2 on bad `--limit` / malformed `--filter` / empty filter key / missing `--current`, exit 3 on directory-as-current, empty-current -> empty output (json+human parametrized), filter-on-missing-key matches nothing, `--limit` larger than total returns all. Not fixed (deliberate scope): no follow / `-f` mode (add when a stream needs it); no time/range filters (Step 110 will define once retention writes land); `bin/install-log-tail.py` retained (still emits richer schema-aware human lines for installer actions).

## v4.49.0 - 2026-07-21

- Plan 90 Step 108 shipped: **Shared JSONL rotator primitive.** New `BE/app/jsonl_rotator.py` exposes `append_and_roll(current, previous, rows, *, max_bytes) -> RollOutcome{IsRolled, AppendedCount, BytesAppended}`, `read_jsonl(path)`, and `read_pair(current, previous)` (previous-then-current for monotonic time). Same durability contract Step 107 baked into the installer, now dependency-free: raises stdlib `OSError` / `ValueError`, does not import `AppError`, so any future JSONL audit stream (retention loop summaries, IPC audit tails, observability rings) reuses one implementation instead of re-deriving weaker copies. Invariants pinned by tests: roll BEFORE append (`os.replace`, atomic on POSIX + Windows, clobbers stale `.1`), every append is `flush() + fsync()`, empty `rows` is a full no-op with zero I/O, poison lines surface as `{_Raw, _ParseError}`. `BE/app/install_log_rotator.py` refactored to delegate: `_append_jsonl` / `_roll_if_oversize` / `_read_jsonl_lines` removed; `rotate_manifest` now calls the primitive and wraps `OSError -> E_INSTALL_MANIFEST_UNWRITABLE`, `ValueError -> E_INSTALL_MANIFEST_INVALID`; public surface (`rotate_manifest`, `read_archive_entries`, `RotationOutcome`, defaults) unchanged so Step 107 CLIs and tests pass verbatim. Root cause guarded: Step 107's roll+append logic lived inside the installer, so any other subsystem wanting the same guarantees had to copy the code (drift) or roll a weaker version (silent data loss). Verified: `pytest BE/ -q` -> **1155 passed, 2 skipped** (was 1136; +19). New tests `BE/tests/app/test_jsonl_rotator.py` (19 cases) cover sorted-key output, empty-input no-op, parent auto-create, no-roll below cap, roll-at-or-over-cap, roll replaces stale previous, parametrized rejection of bad `max_bytes` (0, -1, 1.5, True, "1024", None), OSError surfacing when parent path is a regular file, `read_jsonl` missing-file, blank-line skip + poison surfacing, `read_pair` ordering, both-missing, and a monkey-patched regression pin (`test_installer_rotator_delegates_to_primitive`) asserting `install_log_rotator.rotate_manifest` calls THROUGH `jsonl_rotator.append_and_roll` so no future refactor can silently re-derive the logic. Not fixed (deliberate scope): no new consumers wired yet (waits on Step 109 unified tail helper and Step 110 retention status route); no cross-platform file locking (single-writer contract kept from Step 107).

- Plan 90 Step 107 shipped: **Installer log rotation + `install-log-tail` helper.** New `BE/app/install_log_rotator.py` bounds `install.json.Actions` via `rotate_manifest(install_root, *, max_actions=500, archive_max_bytes=5 MiB)`; overflow moves to an append-only JSONL `install-history.log`, rolling to `install-history.log.1` when the archive crosses its byte budget (single-generation). Ordering: roll-first, append-second, atomic manifest rewrite last, so a failed append leaves the manifest oversized (loud AppError) rather than losing a row. Wired into `bin/install-record.py`: rotation runs best-effort after every successful append with new `--max-actions` / `--archive-max-bytes` flags. New `bin/install-log-tail.py` (exit 0/2/3) reads manifest + optional archive, filters by name/phase/status, tails oldest-first, renders `human` or `json`. Poison archive lines surface as `{_Raw, _ParseError}` so the audit view cannot be silently truncated. Reuses existing wire codes (`E_INSTALL_MANIFEST_INVALID` / `E_INSTALL_MANIFEST_UNWRITABLE`), no codes churn. Root cause guarded: Step 106 wired every installer action into append-only `install.json`, so a long-lived host would accumulate `Actions` unboundedly, eventually turning every manifest rewrite into a multi-MB fsync. Verified: `pytest BE/ -q` -> **1136 passed, 2 skipped** (was 1116; +20). New tests `BE/tests/app/test_install_log_rotator.py` (20 cases) cover no-op paths, oldest-overflow moved with newest-N retained in order, idempotency, rolling on oversize with new `.log` after roll (regression guard for the initial roll-after-append bug), poison-line surface, validation rejections, monotonic archive read order, and full CLI subprocess coverage including `install-record.py --max-actions 3` end-to-end. Spec `spec/21-app/79-installer-retention-timing.md` §Manifest rotation documents the two-file archive contract. Not fixed (deliberate scope): route surface (Step 110), PowerShell-hosted end-to-end (parity via shared Python CLIs), pluggable archive backends.

## v4.47.0 - 2026-07-21

- Plan 90 Step 106 shipped: **Installer pre-flight doctor + per-action manifest recording, wired into both orchestrators.** New module `BE/app/installer_doctor.py` cross-references `install.json` (Step 105) against the currently planned action list and emits machine-graded `DoctorFinding` records with codes `ManifestAbsent` (info), `PlatformMismatch` (error), `PreviousCriticalFailure` (error), `OrphanInstalledAction` (warning). New CLIs `bin/install-doctor.py` (exit 0/20/21/3/2) and `bin/install-record.py` (validates `--args-json`, computes `IsSuccess=(exit_code == 0)`, auto-bootstraps the manifest via `record_action`). Both wrappers rewritten: `install.sh` and `install.ps1` now run the doctor before the action loop (refusing on errors, refusing on warnings without `--force-warn`/`-ForceWarn`) and call `install-record.py` after every action with observed `StartedAt`/`CompletedAt`/`DurationMs`/`ExitCode`/`IsCritical`. Manifest-record failures are non-fatal (logged) so a transient FS error cannot brick the install; the doctor detects the gap on the next run. Root cause guarded: Step 105 gave us the manifest but nothing on the install path consulted it or fed it, leaving `install.json` empty and letting repeat installs stomp half-installed systems silently. Verified: `pytest BE/ -q` -> **1116 passed, 2 skipped** (was 1104; +12). New tests `BE/tests/app/test_installer_doctor_and_record.py` (12 cases) pin every finding code + severity, the render-human output, and both CLIs end-to-end via subprocess. Spec `spec/21-app/79-installer-retention-timing.md` §Doctor + §Per-action manifest recording documents the exit-code table. Not fixed (deliberate scope): PowerShell end-to-end tests (parity via shared Python CLIs), retention-status GET route (Step 110), release upgrader (Step 112).

## v4.46.0 - 2026-07-21

- Plan 90 Step 105 shipped: **Install manifest reader/writer (`install.json`)**. New module `BE/app/install_manifest.py` gives the top-level installer a single append-only audit trail for every action Step 104 orchestrates, so a later `--uninstall` can reverse exactly what was installed (not more, not less). PascalCase JSON on disk, `SchemaVersion=1` pinned, `Actions[]` mirrors Step-104 `InstallerAction` plus outcome fields. Atomic writes (same-dir tmp + `os.fsync` + `os.replace`) leave the previous manifest byte-identical on any failure. New wire codes: `E_INSTALL_MANIFEST_INVALID` (422), `E_INSTALL_MANIFEST_UNWRITABLE` (503), `E_INSTALL_MANIFEST_MISSING` (404). Public API: `init_manifest` (refuses overwrite), `record_action` (append-only, auto-creates on first call with bootstrap args), `read_manifest` / `read_manifest_strict`, `write_manifest`, `latest_action`, `installed_action_names` (respects Phase + IsSuccess of the last entry per name). Root cause guarded: pre-Step-105 Step-104 orchestrated actions without a receipt, so a later uninstall from a newer release could not know which side-effects to reverse. Verified: `pytest BE/ -q` -> **1104 passed, 2 skipped** (was 1076; +28). New tests `BE/tests/app/test_install_manifest.py` (25 cases) pin PascalCase schema, SchemaVersion rejection, atomic-write no-leftover-tmp, byte-identical rollback on simulated replace failure, append-only invariant across install+uninstall, validator surface (empty name, bad phase, non-str arg, bool/negative duration, naive datetime), and `installed_action_names` semantics. Not fixed (deliberate scope): shell-wrapper wiring so `install.ps1`/`install.sh` call `record_action` per step (Step 106), retention status route cross-check (Step 110), manifest-based upgrade planner (Step 112).

## v4.45.0 - 2026-07-21

- Plan 90 Step 104 shipped: **Top-level installer orchestrator wiring**. New pure planner `BE/app/installer_plan.py` exposes `plan_install_actions(*, platform: InstallerPlatform, phase: InstallerPhase, interval_hours=24, retention_days=30) -> list[InstallerAction]` where `InstallerAction = (name, script, args, critical)` (frozen dataclass). Ordering contract: INSTALL runs `db-bootstrap` first and `retention-timer` last (registrar must not launch the loop against a half-installed system); UNINSTALL runs `retention-timer` first (stop the loop before schema teardown) and `db-bootstrap` last. Install-phase retention step is `critical=True`; uninstall-phase retention step is `critical=False` so idempotent registrar failures do not orphan schema. Validation parity with Steps 102/103: `interval_hours ∈ [1,168]` int-not-bool, `retention_days ∈ [1,3650]` int-not-bool, non-enum platform/phase all raise `AppError(E_CLI_USAGE)`. New shell wrappers `packaging/installers/install.ps1` and `packaging/installers/install.sh` query the SAME planner via `python -c` heredoc so Windows and POSIX can never drift. `install.ps1`: `SupportsShouldProcess`, dispatches `.ps1` via `pwsh -NoProfile -File` and `.py` via venv Python, aborts critical failures with reserved wrapper exit code 9531 (9530 planner failure). `install.sh`: `set -euo pipefail`, iterates via jq-or-Python, exit 4 critical / 3 planner / 2 usage. Root cause guarded: Step 103's registrars were unreachable from a top-level install. Verified: `pytest BE/ -q` -> **1076 passed, 2 skipped** (was 1055; +21). New tests `BE/tests/app/test_installer_plan.py` (21 cases) pin install/uninstall order on both platforms, criticality flags, arg wiring, and OOR/bool validation. Not fixed (deliberate scope): `install.json` manifest write (Step 105), pre-flight doctor step (Step 106), GH-release download + SHA256 verify (Step 111+).

## v4.44.0 - 2026-07-21

- Plan 90 Step 103 shipped: **Installer registration of the retention timer**. New spec `spec/21-app/79-installer-retention-timing.md`; new pure renderer `BE/app/retention_installer.py` exposes `render_systemd_service`, `render_systemd_timer`, `render_windows_task_xml` with the same range/type validation as the CLI (bool rejected, `interval_hours ∈ [1,168]`, `retention_days ∈ [1,3650]`, `randomized_delay_min ∈ [0,60]`, path/command strings reject NUL and `<`, `>`, `"`). Templates vendored under `packaging/systemd/vision-app-retention.{service,timer}.tmpl` and `packaging/windows/vision-app-retention-task.xml.tmpl`. Operator wrappers: `scripts/systemd/install-retention-timer.sh` (per-user unit under `~/.config/systemd/user/`, `--install|--uninstall|--status`, idempotent, prints `loginctl enable-linger` hint) and `scripts/ps/Register-RetentionTask.ps1` (`-Install|-Uninstall|-Status|-WhatIf`, delegates rendering to Python, writes UTF-16LE-with-BOM as `schtasks /Create /XML` requires, wrapper-only exit codes 9520-9523 in the reserved 9500-9599 range). systemd: `.service` is `Type=simple` running `retention-run --loop`, `Restart=on-failure`, `KillSignal=SIGTERM`; `.timer` uses `OnBootSec=2min` + `OnUnitActiveSec=${IntervalHours}h` + `RandomizedDelaySec=${RandomizedDelayMin}min` (Persistent). Windows: Task XML v1.4 under `\vision-app\retention`, `LogonTrigger` + `BootTrigger` (PT2M delay), `InteractiveToken`/`LeastPrivilege` (no elevation), `ExecutionTimeLimit=PT0S`, restart-on-failure 3x1min. Root cause guarded: Step 102's `--loop` existed but nothing launched it on operator machines. Verified: `pytest BE/ -q` -> **1055 passed, 2 skipped** (was 1030; +25). Not fixed (deliberate scope): `install.ps1` / `install.sh` manifest wiring (Step 104), `GET /observability/retention/status` (Step 110), JSONL rotation (Step 109), cross-user / system-wide install.

## v4.43.0 - 2026-07-21

- Plan 90 Step 102 shipped: **Retention scheduling contract**. New spec `spec/21-app/78-retention-schedule.md` pins the two-mode contract; new module `BE/app/retention_scheduler.py` provides `run_scheduled(*, interval_hours, single_pass, max_passes=None, sleeper=time.sleep, stop_event=None) -> (list[RetentionOutcome], AppError | None)` as a pure in-process ticker (no FS, no DB, no signal handlers). Loop mode added to `bin/retention-run.py` via `--loop`, `--interval-hours` (default env `APP_RETENTION_INTERVAL_HOURS` or 24, range `[1,168]`), and `--max-passes` (optional cap). Signals: CLI installs `SIGINT`/`SIGTERM` handlers that set a `threading.Event`; current pass finishes before clean exit. Interval sleep is chopped into <=1s slices so shutdown aborts within one second. `AppError` in a pass stops the loop (do not pound a broken DB); non-`AppError` propagates -> `IoError=4`. Bad flag combos (`--interval-hours` or `--max-passes` without `--loop`, out-of-range interval, non-int env) now raise `E_CLI_USAGE` and exit code `Usage=2`. Loop emits ONE Universal Envelope at process exit with all per-pass outcomes; on failure the envelope carries partial `Results` plus the offending `AppError` in `Errors`. Root cause guarded: Step 101 shipped the delete path but nothing invoked it, so unbounded row + artifact growth was inevitable. Verified: `pytest BE/ -q` -> **1030 passed, 2 skipped** (was 1009; +21). New tests: `BE/tests/app/test_retention_scheduler.py` (17 cases) + `BE/tests/cli/retention/test_retention_run_cli.py` (4 cases). Not fixed (deliberate scope): systemd/Scheduled-Task registration (Step 103), `GET /observability/retention/status` (Step 110), JSONL rotation (Step 109).

## v4.42.0 - 2026-07-21

- Plan 90 Step 101 shipped: **Task-DB retention / vacuum pass**. `BE/app/retention.py` exposes `run_retention(conn, *, results_root, retention_days, now_epoch=None, dry_run=False) -> RetentionOutcome` and `bin/retention-run.py` wraps it with a Universal-Envelope CLI (`--retention-days`, `--results-root`, `--db-root`, `--dry-run`; defaults from env `APP_RETENTION_DAYS`/30, `APP_DATA_ROOT`/results, `APP_DB_ROOT`). Fetches artifact `RelPath` list BEFORE cascading `DELETE FROM RunSession` so `ON DELETE CASCADE` cannot erase the on-disk unlink plan. Idempotent file unlink (missing = OK); per-file `OSError` captured into `RetentionOutcome.UnlinkFailures` without rolling back the DB delete (DB is source of truth, files are cache). Two-layer traversal guard (`..`, absolute, drive letter, backslash, symlink escape) refuses the file but still purges the parent row so a poisoned artifact row cannot pin retention forever. DB deletes batch 500 IDs per `BEGIN IMMEDIATE`. `retention_days<1` -> `E_BE_BAD_REQUEST`; missing Task-DB table -> `E_BE_INTERNAL` with `Hint="python bin/db-bootstrap.py"`. Every pass emits one INFO log `retention.pass.completed` with counters. Root cause guarded: pre-Step-101 the row count and artifact tree grew unbounded, guaranteeing eventual query degradation and disk exhaustion. Verified: `pytest BE/ -q` -> **1009 passed, 2 skipped** (was 992; +17). New tests in `BE/tests/db/test_retention.py` cover input validation, missing-table 500, empty DB, fresh-row preservation, expired purge with FK cascade, dry-run non-mutation, idempotent missing file, JSONL sidecar unlink, partitioning, 5 traversal refusals, strict `<` cutoff boundary, wire shape.

## v4.41.0 - 2026-07-21

- Plan 90 Step 100 shipped: **`GET /observability/runs*` read-only Task-DB surface**. `BE/routes/observability/runs.py` mounts four envelope-wrapped endpoints on the FastAPI app (`BE/main.py:59`): `GET /observability/runs` (list, filters `verdict|task_id|run_id`, limit ceiling 200, default 50), `GET /observability/runs/{id}` (single-run detail, 404 on unknown), `GET /observability/runs/{id}/rules` (per-rule verdicts ordered by `COALESCE(OrderIndex, INT_MAX)`), `GET /observability/runs/{id}/rules/{rid}/artifacts` (FrameArtifact rows; `rid=0` sentinel for run-level `RuleResultId IS NULL`; cross-run RuleResultId is 404 to prevent evidence leakage). Column names match migration 0026 exactly: `RuleKind` and `MetricsJson`. Missing Task-DB table surfaces as `E_BE_INTERNAL` (500) with `Hint="python bin/db-bootstrap.py"` and an ERROR log line `runs_query_failed`; never a silent empty list. Every success emits INFO with `CorrelationId`, `operation`, filters, count; `X-Correlation-Id` header echoed on response. Bad params -> `E_BE_BAD_REQUEST` (400); unknown parents -> `E_BE_NOT_FOUND` (404). Root cause guarded: pre-Step-100 all persisted rows from Steps 96-99 were invisible to the FE and operators. Verified: `pytest BE/ -q` -> **992 passed, 2 skipped** (was 979; +13). New tests in `BE/tests/routes/test_observability_runs.py` cover empty list, newest-first ordering, verdict filter, 400 on bad verdict, 400 on limit>200, single-run detail, 404 on unknown run/rule/cross-run, sentinel 0 for run-level artifacts, missing-table 500 with Hint, and CorrelationId round-trip.

## v4.40.0 - 2026-07-21

- Plan 90 Step 99 shipped: **`evaluate` -> Task-DB persistence wiring**. `BE/cli/processing/commands/evaluate.py` now drives `write_run_session` + `write_rule_results` + `write_frame_artifacts` after the JSONL fsync, keyed by the same `RunId` the JSONL uses. New `--task-db-root` CLI flag; defaults to `APP_DB_ROOT` env var; DB persistence is skipped silently when neither is set so existing JSONL-only tests stay green. Per-judgment `Artifacts` have their freshly-known `RuleResultId` back-filled from the writer's `WriteBatchOutcome.Rows` so FrameArtifact rows FK-link exactly to the RuleResult; run-level artifacts (SourceFrame) keep `RuleResultId=NULL`. Writers run sequentially, not in one wrapped transaction, because every writer's own `BEGIN IMMEDIATE` + composite-key `INSERT OR IGNORE` makes a crash between calls fully recoverable: a re-run of the same JSONL under the same RunId is a byte-identical no-op end-to-end (spec 24 §1 JSONL is source of truth; DB heals from JSONL on replay). Attribute-safe `getattr(ns, "task_db_root", None)` prevents `AttributeError` in the `watch` command which constructs its own Namespace. Every DB write emits an INFO log with `TaskRunSessionId` + `WasInserted` + per-writer inserted/skipped counts; every writer AppError emits a single ERROR log `evaluate.db.write_failed` before re-raising so silent DB failures are impossible. Root cause guarded: pre-Step-99 the writers existed but nothing called them, so the Step 100 observability route and Step 141+ FE detail drawer would query empty tables forever. Verified: `pytest BE/ -q` -> **979 passed, 2 skipped** (was 974; +5). New tests in `BE/tests/cli/processing/test_evaluate_db_persist.py` cover: zero-rule bundle persists `RunSession(Verdict=Pass, RuleCount=0)`; replay is idempotent; skipped when no DB root; env-only trigger; DROP TABLE causes both AppError raise AND ERROR log.

## v4.39.0 - 2026-07-21

- Plan 90 Step 98 shipped: **Task-DB `FrameArtifact` writer** (`BE/app/db/writers/frame_artifact.py` + migration `BE/db/migrations/task/0027_task_frame_artifacts.sql`). Migration declares `FrameArtifact` with dual FKs: `RunSessionId NOT NULL REFERENCES RunSession ON DELETE CASCADE` and `RuleResultId NULL REFERENCES RuleResult ON DELETE CASCADE` (nullable so SourceFrame-level artifacts stay attached to the run when no single rule owns them); `ArtifactKind` CHECK IN ('SourceFrame','RoiCrop','Overlay','Reference','DebugMask'); `Sha256` CHECK length=64; `Bytes` CHECK >= 0; `UNIQUE (RunSessionId, RelPath)` gives idempotent `INSERT OR IGNORE` with RelPath as identity so two artifacts with identical bytes but distinct provenance stay distinct rows; secondary indexes on `RunSessionId`, `ArtifactKind`, and partial `RuleResultId WHERE RuleResultId IS NOT NULL` for the per-rule evidence lookup. Writer accepts BOTH PascalCase and camelCase field aliases via `_pick`; pre-validates the WHOLE batch (Kind, POSIX RelPath policy matching storage_facade rejecting leading `/`, drive letter, `..` segment, backslash, NUL; hex64 lowercased Sha256; non-negative int Bytes; RuleResultId int-or-None; no duplicate RelPath in batch) BEFORE `BEGIN IMMEDIATE` so any bad row aborts atomically. Guards unknown `RunSessionId` and unknown `RuleResultId` up-front as `E_BE_NOT_FOUND` and rejects a `RuleResultId` whose parent `RunSessionId` differs from the batch as `E_BE_BAD_REQUEST` (mis-linkage guard prevents cross-run evidence contamination). Sha256 is lowercased on write. Frozen `WriteBatchOutcome(RunSessionId, InsertedCount, SkippedCount, Rows)` + `FrameArtifactRow(FrameArtifactId, RelPath, ArtifactKind, RuleResultId, Sha256, Bytes, WasInserted)` give per-row signal for Step 99 evaluator wiring. Root cause guarded: pre-Step-98 the ROI crop / overlay / reference image lived only as loose files under `results/<RunId>/artifacts/`, so the FE detail drawer, retention GC, and the observability route had no queryable table to enumerate a rule's evidence. Verified: `pytest BE/ -q` -> **974 passed, 2 skipped** (was 950; +24 new cases in `BE/tests/db/test_frame_artifact_writer.py`).

## v4.38.0 - 2026-07-21

- Plan 90 Step 97 shipped: **Task-DB `RuleResult` writer** (`BE/app/db/writers/rule_result.py` + migration `BE/db/migrations/task/0026_task_rule_results.sql`). FK'd on `RunSession(RunSessionId) ON DELETE CASCADE`; `UNIQUE (RunSessionId, RuleId)` gives idempotent `INSERT OR IGNORE` replay per spec 24 §4 "at most one judgment per rule per run". Writer accepts BOTH PascalCase and camelCase field aliases via `_pick` helper so evaluator callers and JSONL-export callers both work; pre-validates the entire batch (Verdict, RuleId, ElapsedMs finite/non-negative, Metrics JSON-serializable, no duplicate RuleId) BEFORE opening `BEGIN IMMEDIATE`, so any bad row aborts atomically without leaving a partial run in the DB; rejects unknown parent up-front as `E_BE_NOT_FOUND` instead of relying on a raw FK error; extracts ErrorCode + RuleKind from top-level or `Details`/`details` for evaluator compatibility; `MetricsJson` serialized with `sort_keys=True, separators=(",",":")` for stable snapshots. Frozen `WriteBatchOutcome(RunSessionId, InsertedCount, SkippedCount, Rows)` + `RuleResultRow(RuleResultId, RuleId, Verdict, WasInserted, ErrorCode)` give per-row signal so Step 98 FrameArtifact writer can decide whether to (re-)link ROI evidence. Root cause guarded: pre-Step-97 per-rule verdicts lived only in the JSONL export, so the FE detail drawer, the FrameArtifact writer, and the observability route had no queryable per-rule table. Verified: `pytest BE/ -q` -> **950 passed, 2 skipped** (was 931; +19 new cases in `BE/tests/db/test_rule_result_writer.py`).

## v4.37.0 - 2026-07-21

- Plan 90 Step 96 shipped: **Task-DB `RunSession` writer** (`BE/app/db/writers/run_session.py` + migration `BE/db/migrations/task/0025_task_run_sessions.sql`). Single pure `write_run_session(conn, record, *, mode, results_jsonl_path=None, now_epoch=None) -> WriteOutcome` transforms one evaluator record into one Task-tier `RunSession` row; `INSERT OR IGNORE` on unique `RunId` gives idempotent JSONL replay (same `RunSessionId` returned, `WasInserted=False`); `BEGIN IMMEDIATE`/`COMMIT` with explicit `ROLLBACK` on `sqlite3.Error`; Python-boundary validation returns `E_BE_BAD_REQUEST` with the offending `Field` in `Details` instead of raw sqlite CHECK errors bubbling as `E_BE_INTERNAL`; `bool`-as-`int` rejected (isinstance guard); DB tier itself enforces spec 24 §3 counter invariants via two `CHECK` constraints; `aggregate_runs` (Step 95) reused to compute `TimeoutCount` + `PromotedErrorCode` so per-run DB row and dashboard tile stay in lockstep; `CapturedAt` ISO-8601 parses to epoch seconds with WARNING on unparseable input (never crashes the write); one log line per outcome (INFO insert / WARNING duplicate / ERROR + re-raise), never silent. Root cause guarded: pre-Step-96 `evaluate` only wrote JSONL to disk, so `RuleResult`/`FrameArtifact` writers had no FK, `GET /observability/runs` had no table, and the FE history view sat on air. Split-DB memory §9 upheld: no cross-tier FK, `RunId` is the join key; grep test pins writer imports to the Task tier. Verified: `pytest BE/ -q` -> **931 passed, 2 skipped** (was 916; +15 new cases in `BE/tests/db/test_run_session_writer.py`).

## v4.36.0 - 2026-07-21

- Plan 90 Step 95 shipped: **Per-code / per-rule budget + problem telemetry aggregator** (`BE/app/rules/kernel/dashboard.py`). Single pure `aggregate_runs(records)` folds N run records into a `DashboardAggregate` with `RunCount`, `JudgmentCount`, `MalformedJudgmentCount`, dedicated `TimeoutCount`, `VerdictCounts`, `ErrorCodeCounts`, `ProblemCodeCounts` (split against the closed `BundleProblemCode` set with `UnknownProblemCodes` quarantine), and `LatencyByRule` / `LatencyByKind` / `LatencyOverall` `{Count, Sum, P50, P95, Max}`. Percentiles use linear interpolation on sorted samples (no numpy). Defensive input handling: bools/NaN/inf/negative/string latencies rejected, malformed judgments counted not crashed, both `Details` and `details` accepted, `to_dict()` sorts every map for snapshot-stable JSON. Root cause guarded: Steps 83-94 produced raw signals but no canonical fold, so every downstream tile would have re-implemented percentile math and enum lookups and drifted. Verified: `pytest BE/ -q` -> **916 passed, 2 skipped** (was 899; +17 new cases in `BE/tests/app/rules/test_dashboard.py`).

## v4.35.0 - 2026-07-21

- Plan 90 Step 94 shipped: **Problem[] taxonomy tightened via `BundleProblemCode` StrEnum** (`BE/app/rules/kernel/problems.py`, `BE/app/rules/kernel/loader.py`). All 33 loader `Problems[].Code` values are now enum members; `_problem()` guards with `TypeError` if a caller passes a bare string. New AST-based guard test walks `loader.py` and proves (a) every `_problem(...)` call passes `PC.<name>`, (b) every registered enum member is reachable from at least one call site (no dead codes), (c) the runtime guard rejects raw strings. Root cause guarded: 34 free-form Code literals across loader could drift or typo silently. Wire values unchanged (PascalCase preserved). Verified: `pytest BE/ -q` -> **899 passed, 2 skipped** (was 894; +5 new cases in `BE/tests/app/rules/test_problem_taxonomy.py`).

## v4.34.0 - 2026-07-21

- Plan 90 Step 93 shipped: **Explicit rule-ref cycle guard** (`BE/app/rules/kernel/loader.py`) per spec 21-app/33 §6. New `_validate_ref_cycles` runs iterative DFS over the MathExpression ref graph and emits one `RuleRefCycle` problem per distinct cycle (deduped by frozenset of participating ids, chain preserved in `Details.Chain`, sorted ids in `Details.RuleIds`). `_validate_dag` now suppresses redundant `RuleRefForward` on edges inside a reported cycle so authors see one actionable "these rules form a loop" message instead of one misleading "reorder this rule" hint. Self-refs (len==1 cycles) still report as `RuleRefForward` to preserve the pre-existing single-edge diagnostic. Root cause guarded: multi-hop cycles like A->B->C->A previously surfaced only as one `RuleRefForward` on the closing edge, hiding the loop itself. Verified: `pytest BE/ -q` -> **894 passed, 2 skipped** (was 888; +6 new cases in `BE/tests/app/rules/test_loader_cycles.py`).

## v4.33.0 - 2026-07-21

- Plan 90 Step 92 shipped: **`ResultReady` IPC now promotes per-rule `ErrorCode` and `ErrorCount`** (`BE/cli/common/ipc_models.py`, `BE/cli/processing/commands/evaluate.py`, `BE/cli/processing/commands/watch.py`) per spec 21-app/33 §5 + 21-app/76 + 03-error-manage/02. `ResultReadyPayload` grows optional `ErrorCount: int = 0` (`ge=0`) and `ErrorCode: str | None = None` (`min_length=1`), preserving pre-Step-92 wire compat. New helper `_promote_error_code(judgments)` scans `Details.ErrorCode` on each Judgment with a fixed priority order (`E_RULE_TIMEOUT` first, then tolerance / eval / bundle codes, then first-seen fallback) so watchers and the FE `GlobalErrorModal` can key alerting off one top-level string instead of re-reading the JSONL. `watch.py` reuses the same helper (single source of truth). Verified: `pytest BE/ -q` -> **888 passed, 2 skipped** (was 878; +10 new cases in `BE/tests/cli/processing/test_result_ready_error_code.py`).

## v4.32.0 - 2026-07-21

- Plan 90 Step 91 shipped: **Per-rule `TimeoutMs` watchdog** (`BE/app/rules/kernel/{engine,models,loader}.py`, `BE/errors/codes.py`) per spec 21-app/33 §5 + 36 §5. `RuleSpec` grows `timeout_ms: int | None = None`; loader parses `timeoutMs` / `TimeoutMs` as positive int and rejects zero, negative, float, string, bool, list, dict as `RuleTimeoutMsInvalid`. Engine `_evaluate_one` now hands off to `_timeout_judgment` when `latency_ms > timeout_ms`, replacing the judgment with `Verdict.ERROR` carrying `ErrorCode=E_RULE_TIMEOUT`, `ReasonCode=RuleTimeout`, `TimeoutMs`, `ActualLatencyMs`, `OriginalVerdict`, and preserving original `LatencyMs` / `PredicateVersion` / `RoiHash`. One `logging.warning("rule.timeout ...")` per breach via `rule_kernel.engine` so CLI JSONL logs surface every timeout. New wire code `E_RULE_TIMEOUT` mapped to HTTP 422. Timeout Errors count toward `error_count` and trigger short-circuit stop with `stop_reason=FirstError`. Post-hoc by design: Python cannot preempt CPU-bound numpy work, so spec-mandated detection replaces preemption. Zero-change for legacy bundles without `TimeoutMs`.
- Tests: `BE/tests/app/rules/test_timeout.py` (15 cases): loader accept for both casings, default `None`, parametrized reject sweep (0, -5, 1.5, "150", True, False, list, dict), engine no-op, within-budget verdict preserved, over-budget conversion with all detail keys asserted + `caplog` warning line captured, short-circuit stop fires on timeout Error.
- Verified: `pytest BE/ -q` -> `878 passed, 2 skipped` (was 862; +16 net).
- Unblocks Step 92 (CLI `evaluate` IPC error envelope surfaces `E_RULE_TIMEOUT`), Step 95 (per-rule budget telemetry dashboard), Step 96 (TaskDB `RuleResult` persists `TimeoutMs` + `ActualLatencyMs`).

## v4.31.0 - 2026-07-21

## v4.30.0 - 2026-07-21

- Plan 90 Step 89 shipped: **Tolerance resolver** (`BE/app/rules/kernel/tolerance.py`) implementing spec 21-app/34. New public surface: `ToleranceKind` (`ScalarRange|PercentRange|XyBox|MatchPercent`), `ToleranceProfile`, `ResolvedTolerance`, `build_profile_map(profiles_json, task_id)`, `resolve_for_rule(rule, profiles, task_id)`. Enforces §3 per-kind validation (min<=max, 0..100 for percent bounds, positive half-extents and integer centers for XyBox, allowed `Inclusive` values), §4 rule-kind compat matrix (with `XyBox` structurally rejected on all rule-kind refs since it binds to Regions per §67), §5 resolution including optional `SecondaryToleranceRef`, and §7 error taxonomy: `ToleranceInvalid`, `ToleranceCrossTask`, `ToleranceUnresolved` (all via `E_TOLERANCE_UNRESOLVED` with distinct `ReasonCode`), `ToleranceIncompatible` (via `E_TOLERANCE_INCOMPATIBLE`). Uniqueness: `ProfileId` and `ProfileName` unique within a Task. Cross-task guard accepts optional `TaskId:profileId` prefix in refs and rejects foreign prefixes.
- New wire codes in `BE/errors/codes.py`: `E_TOLERANCE_UNRESOLVED` and `E_TOLERANCE_INCOMPATIBLE`, both mapped to HTTP 422 per spec 40 §A.7.
- Engine hook (`BE/app/rules/kernel/engine.py`): opt-in pre-flight when `ctx.metadata["ToleranceProfiles"]` is present. Resolver runs once for every non-Inactive rule; failures produce a SKIPPED judgment carrying `StatusReason` + `ErrorCode` and demote the rule to effective-Inactive (spec 34 §7 forbids silent pass on unresolved thresholds). Effective-inactive rules do not trigger short-circuit stop and do not count toward pass/fail/error; resolved windows are injected into `ctx.metadata["ResolvedTolerances"][rule.id]` for evaluators to consume. Legacy call sites without `ToleranceProfiles` in metadata are untouched.
- Tests: `BE/tests/app/rules/test_tolerance.py` (34 cases): §3 validation per kind including bool-vs-int, negative bounds, inverted ranges, unknown-kind, missing ids, duplicate ids; §4 compat matrix positive and negative including `Count` secondary; §5 missing/unknown ref, task-scoped ref accept + cross-task reject; §7 all four ReasonCodes surface via the correct wire code; engine deactivation for unresolved and incompatible refs, opt-out when profiles absent, metadata injection of `ResolvedTolerances`, and non-triggering of short-circuit stop for deactivations.
- Verified: `python -m pytest BE/ -q` -> `850 passed, 2 skipped` (was `816 passed, 2 skipped`; +34 net).
- Unblocks Step 90 (bundle loader validates cross-rule DAG using resolved tolerances), Step 91 (per-rule timeouts read resolved tolerance kind), Step 96 (TaskDB `RuleResult` persists `ResolvedToleranceSnapshotJson`).

## v4.29.0 - 2026-07-21

- Plan 90 Step 88 shipped: `MathExpression` predicate (spec 33 §3.6) lands under `BE/app/rules/evaluators/math_expression.py`, replacing the Step 81 stub and closing the six-of-six rule-kind matrix for v1 (only `OcrText` remains `RuleDisabledInV1`). Engine now injects `PriorJudgments: dict[rule_id, RuleJudgment]` into a cloned `RuleContext.metadata` and accumulates judgments as rules execute; forward refs (spec 33 §6) are structurally impossible and surface as `RuleBadInput`. Safe-arithmetic evaluator via `ast.parse(mode="eval")` + strict allowlist walk (zero `eval`, zero `builtins`): allowed = numeric literals, unary `-`, binary `+ - * /`, `min`/`max`/`abs`/`round`, `Rule.<id>.<key>` chain. Rejected as `RuleBadInput`: `**` `//` `%` bitwise compare boolop ternary lambda comprehensions strings bool-literals bare-names non-`Rule` attribute chains kwargs wrong-arity divide-by-zero non-finite. Outputs `Value` (6dp) and `ResolvedInputsJson` (deduped first-seen). Verdict: Pass when `MinValue <= Value <= MaxValue`, else Fail (`RuleOutOfRange`). Verified: `pytest BE/ -q` -> `816 passed, 2 skipped` (+44). Unblocks Step 89 (tolerance resolver reuses safe evaluator), Step 90 (loader validates rule DAG), Step 96 (TaskDB persists Value/ResolvedInputsJson).

## v4.28.0 - 2026-07-21

- Plan 90 Step 87 shipped: `GraphicDisplayCheck` predicate (spec 33 §3.5) lands under `BE/app/rules/evaluators/graphic_display_check.py`, replacing the Step 81 stub. NCC core extracted from Count into shared `BE/app/rules/kernel/template.py` (`luma`, `ncc_score_map`, `nms_peaks`). Params: `MinMatchPercent` (0..100), `AllowRotationDeg` (non-empty subset of `{0,90,180,270}`; out-of-set -> `RuleBadInput`), `AllowMirror` (strict bool). Search orbit = rotations x mirror via `np.rot90`/`np.fliplr`; rotated variants that overflow ROI are skipped; if none fits -> `RuleBadInput`. Winner recorded as `AppliedRotationDeg` + `AppliedMirror`. Pass when best `MatchPercent >= MinMatchPercent`, else Fail (`RuleBelowThreshold`). Verified: `pytest BE/ -q` -> `772 passed, 2 skipped` (+19). Unblocks Step 88 (`MathExpression`), Step 90 (loader validates all non-Ocr kinds), Step 96 (TaskDB persists all six kinds).

## v4.27.0 - 2026-07-21

- Plan 90 Step 86 shipped: `Count` predicate (spec 33 §3.3) lands under `BE/app/rules/evaluators/count.py`, replacing the Step 81 stub. Reads `ctx.metadata["Patterns"][rule.id]` (numpy ndarray, ndim matches ROI, spatial dims fit inside ROI; missing/mismatched/oversized -> `AppError(E_RULE_EVAL_FAILED, ReasonCode=RuleBadInput)`, never fabricated). NCC via `sliding_window_view` on unweighted luma, zero-variance tiles score 0, negative correlation clipped to 0. Greedy NMS zeros a pattern-sized neighbourhood per peak; capped at 512 matches. Outputs `MatchCount`, `MatchesJson` (frame-space centroids, sorted MatchPercent desc). Pass when `MinCount <= MatchCount <= MaxCount`, else Fail (`RuleAboveThreshold` over, `RuleBelowThreshold` under). Pure numpy, no scipy. Verified: `pytest BE/ -q` -> `753 passed, 2 skipped` (+23). Unblocks Step 87 (`GraphicDisplayCheck`), Step 90 (loader), Step 96 (TaskDB), Step 131+ (FE Count authoring).

## v4.26.0 - 2026-07-21

- Plan 90 Step 85 shipped: `FlawDetect` predicate (spec 33 §3.2) lands under `BE/app/rules/evaluators/flaw_detect.py`, replacing the Step 81 stub. Reads `ctx.metadata["References"][rule.id]` (numpy ndarray, same shape as ROI; missing/mismatched -> `AppError(E_RULE_EVAL_FAILED, ReasonCode=RuleBadInput)` with `RefShape`/`RoiShape` echoed, never fabricated). Sensitivity 0..100 -> threshold `max(1, round(255 * (1 - S/100)))`. Optional `MaskRegions[].XyBox` zeroed out of deviation mask. Pure-numpy 4-connectivity connected components (no scipy dep). Outputs `FlawCount`, `LargestFlawAreaPx`, `FlawCentroidsJson` (frame coords, sorted AreaPx desc, capped at 128). Pass when `FlawCount <= MaxAllowedFlawCount`, else Fail (`ReasonCode=RuleAboveThreshold`). Verified: `pytest BE/ -q` -> `730 passed, 2 skipped` (+21). Unblocks Step 86 (`Count`), Step 90 (loader), Step 96 (TaskDB), Step 156+ (vendor adapter).

## v4.0.0 - 2026-07-21

- Plan 88 Step 11 shipped: `BE/errors/codes.py` central `ErrorCode` enum (19 codes, 5 families) + `default_http_status()` + `is_registered()`. Version rolls 3.999.0 → 4.0.0. Unblocks Step 12 `AppError` and Step 13 handlers.

## v3.999.0 - 2026-07-21

- Plan 88 Step 10 shipped: `BE/envelope.py` frozen wire envelope + `X-Correlation-Id` mint helper. Unblocks Steps 11-19.

## v3.998.0 - 2026-07-21

- Plan 88 Step 9 shipped: `BE/config.py` typed settings (pydantic-settings, `BE_` prefix, frozen, cached). Unblocks Steps 10-17.

## v3.997.0 - 2026-07-21

- Plan 88 Step 8 shipped: `BE/README.md` documents layout, run commands, envelope contract, and the facade rule. Unblocks Steps 9-17.

## v3.996.0 - 2026-07-21

- Plan 88 Step 7 shipped: `BE/` FastAPI skeleton (19 files) importable end-to-end. Unblocks Steps 8-17 (README, config, envelope, error handlers, `/health`, `/meta`).

## v3.995.0 - 2026-07-21

- Plan 88 Step 6 shipped: backend language locked to Python + FastAPI in `docs/plans/88/decisions.md` (D-001). Unblocks Step 7 `BE/` scaffold.

## v3.994.0 - 2026-07-21

- Home CTA now uses near-white text on the purple button (was dark navy on purple), fixing the low-contrast look on "Create your first project" / "Continue …".

## v3.993.0 - 2026-07-21

- Repo-wide auto-format pass: Prettier + ESLint `--fix` normalized 645 files; `guidelines:check` residual dropped from 2861 problems to 24 real ones (11 errors, 13 warnings) now visible as the true worklist.

## v3.992.0 - 2026-07-21

- Floating Properties panel defaults now 420×380 (was 340×560): fixes right-edge clipping of the BOUNDS aspect-lock, `+ Add condition`, and Mask/Focus chips, and removes the big empty band under `More options`.

## v3.991.0 - 2026-07-21

- Properties Bounds card: restored inner padding + tight column gap and taller input rows so X/Y/W/H values are no longer vertically clipped and the empty band before Acceptance is gone.

## v3.990.0 - 2026-07-21

- Rules panel rows: added left padding (12px base, 10px compact) so the rule icon sits away from the row edge instead of touching the border.

## v3.989.0 - 2026-07-21

- CI enforces coding guidelines: `prettier --check` gate on changed files runs before ESLint in `frontend-checks`; new `bun run guidelines:check` composes format + strict typecheck + lint for local parity.

## v3.988.0 - 2026-07-21

- Plan 88 Step 5: `docs/diagrams/backend-v1/README.md` embeds all five Mermaid diagrams inline in reading order with `lov-artifact` tags, giving downstream steps one canonical page to cite.

## v3.987.0 - 2026-07-21

- Plan 88 Step 4: five Mermaid diagrams + index under `docs/diagrams/backend-v1/` freeze the topology for the accepted backend request. Mode toggle, request lifecycle with `X-Correlation-Id`, SDK facade import boundaries, error propagation with reserved code families, and launcher sequence. Cited as source of truth by Steps 5-15 and 43-90.

## v3.986.0 - 2026-07-21

- Plan 88 Step 3: Backend Implementation Request v1 frozen as `accepted`. All conflicts flagged in Step 2's gap log are resolved in the request body and mirrored into the guideline digest. Error codes now `E_<AREA>_<CONDITION>`, AppError PascalCase with no wire stack, SDK facade folders under `BE/app/facades/` + `src/lib/facades/`, localStorage key `ui.backend.baseUrl`, config knobs + correlation-ID + egress warn + launcher env contract promoted into the request. Unblocks Steps 4-7.

## v3.984.0 - 2026-08-12

- Plan 88 Step 125: Added Backend Mode toggle and initial BE scaffold. Implemented `BE/` skeleton with FastAPI, SDK facade rules, and the `SEED` vs `BACKEND` mode toggle for the UI. (Draft, unshipped).

## v3.982.0 - 2026-07-21

Fixed the two broken UI issues from `assets/ui/70-*`, `71-*`, `72-*`:

- **Giant purple horizontal scrollbar in Layers/Preview/Properties panels**: `panel-chrome-body` was using `overflow-auto` (both axes) with no styled scrollbar, so any minor horizontal overflow surfaced as a chunky OS-native bar. Switched to `overflow-y-auto overflow-x-hidden editor-scroll-fancy` so every dockable panel uses the same thin 8px accent scrollbar and never shows a horizontal scrollbar.
- **Redundant Properties Legend strip**: removed `StatusLegend` from `PropertiesPanel.tsx`. Tone is already conveyed by each card's left stripe (BOUNDS green, ACCEPTANCE amber, etc.) and the Verdict pill in the identity row, so the legend was pure duplication eating ~48px of scarce panel height.

## v3.981.0 - 2026-07-21

- Fixed the broken Properties rule sections shown in the latest screenshots. The panel now contains nested cards inside the rail, prevents right-edge clipping, keeps Mask upload/help text from overlapping, gives detector editors a compact contained layout, and reserves scroll space above the bottom status footer.

## v3.980.0 - 2026-07-21

- The Rules panel gutter is fixed at the shell level. The remaining left strip came from a generic editor aside section rule that added 16px padding around the Rules and Categories sections, not from the row itself. Rules sections now opt out of that padding, so headers and rows align to the list edge, and the visual regression test now covers the full shell path that caused the issue.

## v3.970.0 - 2026-07-20

- Fixed the wide empty gap on the left side of the Layers panel: rule rows now sit flush with the `RULES` / `CATEGORIES` section headers. The drag handle collapses to a 6px hairline (35% opacity) when idle and expands to full 16px on hover/focus/selection, so the grab affordance is still discoverable without wasting a 30px column.

## v3.961.0 - 2026-07-20

- Top-bar actions now show inline keyboard shortcut chips: Reset (⌘⇧0), Search (⌘K), Save (⌘S), Publish (⌘⇧P). Modifier auto-swaps to `Ctrl` on non-Mac; chips hide on narrow viewports and in Compact density.

## v3.960.0 - 2026-07-20

- Editor top bar: Reset, Search, and Save are now grouped into one segmented pill with hairline dividers. Publish stays as the standalone primary action. Save picks up the primary accent color while the document is dirty.

## v3.959.0 - 2026-07-20

- The Rules panel now opens with the correct active-tab title on first render, not the stale `Rules` title, and the compact editor layout was verified in-browser.

## v3.958.0 - 2026-07-20

- The screenshot issues are fixed at component level: Properties section actions are no longer rendered, Rules gets a real tab scrollport and dynamic title, and the Tools rail is clamped through every layout layer.

## v3.957.0 - 2026-07-20

- The left Tools rail is now truly fixed to a 52px icon-only palette, including old saved layouts, so the wide empty gutter cannot come back. Properties nested section chrome is tightened in docked inspectors too.

## v3.956.0 - 2026-07-20

- Tightened the editor side panels to match the critique: the Tools rail is now a narrow icon-only palette, Rules scrolls inside its panel without pushing the footer away, and Properties removes leftover visual noise while keeping integer pixel bounds and a compact split Add condition control.

## v3.955.0 - 2026-07-20

- The armed tool now shows a clear accent stripe on its leading edge (left in the vertical rail, bottom in horizontal docks), a stronger primary tint, and a soft glow, so the active tool is obvious at a glance.

## v3.954.0 - 2026-07-20

- Tools rail feels grouped and denser: tighter 2px vertical gap between tiles plus subtle hairline dividers between Geometry, Read, and Compute groups (and before the New/More actions). Also fixed a CSS build break from a missing `}` in the previous release.

## v3.953.0 - 2026-07-20

- The `+` and `...` tiles in the left tools rail now show tiny "New" / "More" captions under the icon so their purpose is obvious at a glance; tooltips + aria labels also upgraded to "New rule" and "More tools".

## v3.952.0 - 2026-07-20

- Left tools rail header shows the full "Tools" label without truncating. Widened the left dock default and dropped the vestigial grip glyph so chevron + title + close fit cleanly.

## v3.948.0 - 2026-07-20

- Editor top bar buttons compacted to 28px height with 14px icons.

## v3.947.0 - 2026-07-20

- Rules/Layers panel: killed two chrome bands (RulesRailHeader "N layers" strip + Inspector CollapsiblePanelSection wrapper) and removed the redundant "N selected" chip. Selected rows no longer pin the action cluster visible, so rule names get their full horizontal space back on narrow rails; actions still show on hover/focus.

## v3.946.0 - 2026-07-20

- Rules/Layers panel: removed the redundant "Layers · Run order" band so the panel now has only its title plus two sticky section heads (Rules / Categories). Rule names no longer truncate aggressively (fixed the flex/min-width chain on the name span, shrink-locked the source-chain and validation chips). Order badge slimmed to 18x16 to give the name more room, and the empty Categories state shrank to a single "None." line.

## v3.945.0 - 2026-07-20

- Tools panel now stays clean at any width. When the rail is very narrow, tiles stay icon-only with hover tooltips, and the panel scrolls vertically instead of clipping. When docked as a top or bottom strip, tools wrap to a second row instead of getting cut off.

## v3.944.0 - 2026-07-20

- Tool tiles now look consistent whether selected or not: same size, same shadow, same spacing. The active tool is shown only by a tinted background and accent border.

## v3.943.0 - 2026-07-20

- Tools and Inspector cleanup: the Tools palette now uses uniform compact icon tiles, hover no longer moves controls anywhere, Inspector tabs have consistent padding, and Layers rows have tighter aligned action controls with cleaner selected states.

## v3.942.0 - 2026-07-20

- UI density now covers the whole editor, not just the right rail. Switching Settings > UI density between Comfortable and Compact retunes the left tools rail, the editor top bar (Save / Publish / Search / Reset buttons and the Saved/Unsaved pill), and any floating panel you drag out, using the same padding, header height, and icon sizing rules already applied to the right rail. Comfortable is unchanged.

## v3.941.0 - 2026-07-20

- One UI density knob for the whole right rail. Settings > UI density (Comfortable / Compact) now unifies padding, header heights, chevron and icon-button sizes, and toolbar spacing across every panel inside the rail: Layers, Rules, Inspector tabs, and Properties sub-panels. Comfortable keeps today's look; Compact tightens rows and header chrome so more fits on smaller displays. Your choice is remembered per browser.

## v3.940.0 - 2026-07-20

- Cleaner toolbars. Only the most-used actions stay inline; the rest slide under a "…" menu. Layers keeps Delete and Import SVG on the bar; Group, Ungroup, Merge, and the Mark-present / Mark-absent stampers move into the overflow. The rule editor keeps Undo, Redo, and the Snap on/off toggle inline; alignment guides, grid pitch, and the snap debug HUD move into the overflow.

## v3.939.0 - 2026-07-20

- Status bar is now opt-in. New installs open with the footer hidden; turn it on from View -> Toggle Status Bar. Your existing preference is preserved.

## v3.938.0 - 2026-07-20

- Cleaner right rail: one Inspector panel with Preview / Layers / Properties tabs (was three stacked headers), non-essential row buttons only appear on hover/selection, and the duplicate status bar is gone. Only the machine-status footer remains.

## v3.930.0 - 2026-07-20

- Rules list now shows what kind each rule is (Circle, Rect, OCR, String, Expression) with a small colored tag on the left of every row, so you can scan the list without opening each rule. Rows with no configured condition keep a muted "Rule" tag.

## v3.929.0 - 2026-07-20

- Fixed one remaining spot where clicking a rule set from a project's home page produced a long uuid URL instead of the short integer form. All project/ruleset navigation now shows short numbers.

## v3.928.0 - 2026-07-20

- URLs across the app now show short integers for **projects, rule sets, trial runs, and categories** (not just rules). Old uuid/slug URLs still resolve so bookmarks keep working. Behavior is identical apart from the address bar being readable.

## v3.927.0 - 2026-07-20

- Plan 88 is closed. Verified on the live preview that the homepage scrolls correctly and the status bar sits flush at the bottom (no overlap), and confirmed reduced-motion rendering. Screenshots saved under `/tmp/browser/plan88/`.

## v3.926.0 - 2026-07-20

- Rail panel sections open/close smoothly (160ms) and expose keyboard shortcuts: **H** hides a panel, **Del** closes it. Reduced-motion users get instant transitions. No visual change when a panel is already fully open.

## v3.925.0 - 2026-07-20

- Right rail can shrink to 160px (was 200px) via the existing `DockResizeHandle`. Properties "Bounds" is now split into two independently collapsible subsections — Position (X, Y) and Size (W, H) — each with hide/close controls and restore entries in the Window menu. Chip strip navigation still treats them as one "Bounds" scroll target.

## v3.924.0 - 2026-07-20

- Plan 88 Step 6: nested Properties subsection headers (Bounds / Acceptance / Mask / Focus / Kind) are now visually demoted — dashed separators, 8px left indent, muted uppercase caption title that brightens only when open — so parent panels and their subsections read as distinct hierarchy levels. Doubled 1px seams between the parent body and the first child are gone. CSS-only change scoped to `.rail-panel-body .rail-panel-section` so top-level Preview / Layers / Properties heads are untouched.

## v3.923.0 - 2026-07-20

- Plan 88 Step 7: Properties inspector subsections (Bounds / Acceptance / Mask / Focus / Kind) are now individually collapsible, hideable, and closable via `CollapsiblePanelSection` with `properties.*` storage keys. Bounds + Acceptance open by default; Mask, Focus, Kind start collapsed. `WindowMenu` "Rail panels" gains five restore entries so closed subsections come back with one click. Chip-strip navigation and inspector search still work because the outer `data-inspector-section` wrapper is preserved.

## v3.922.0 - 2026-07-20

- Plan 88 Step 8: `WindowMenu` "Rail panels" section restores hidden Preview / Layers / Properties panels via `useRailPanelState`. Each item shows a check when visible, an "Hidden" tag otherwise; toggling dispatches `broadcastRailPanelHidden` which the primitive listens for. Hide/close controls added in Step 5 are now reversible from the UI, not just localStorage. Fixed a TS2451 collision with `useWorkspaceLayoutStore.restorePanel` by aliasing the hook's methods.

## v3.921.0 - 2026-07-20

- Plan 88 Step 5: `InspectorSurface.tsx` now wraps Preview, Layers, and Properties in `CollapsiblePanelSection` (namespaced keys `inspector.preview` / `inspector.layers` / `inspector.properties`). Each section header gains Hide (eye) + Close (X) controls, and hidden panels appear in `useRailPanelState().hiddenPanels` ready for WindowMenu restore in Step 8.

## v3.920.0 - 2026-07-20

- Plan 88 Step 4: `useRailPanelState` hook (`src/hooks/useRailPanelState.ts`) aggregates hidden rail panels into a single `Set<string>` sourced from localStorage (`hmi.rail.panel:*.hidden`), kept fresh via the `RAIL_PANEL_SET_HIDDEN_EVENT` CustomEvent and the browser `storage` event (cross-tab). Exposes `hiddenPanels`, `isHidden`, `hidePanel`, `restorePanel`, `setPanelHidden` for `WindowMenu` (Step 8) and future keyboard shortcuts. Logs `I_UI_RAIL_PANEL_SET_HIDDEN`, `W_UI_RAIL_PANEL_STATE_READ_FAILED`, `W_UI_RAIL_PANEL_STATE_EMPTY_KEY` per the observability rule.

## v3.919.0 - 2026-07-20

- Rules rail Compact CAD Toolbar theme. New `src/components/editor/rail/RulesRailHeader.tsx` replaces the duplicate "Rules"/"RULES" headers and the two decorative button rows (Export/Import + Expand-all/Collapse-all) with a single 28px dense header: monospace "N layers" count on the left, ⋯ icon menu on the right containing Export rule set, Import rule set, Expand all sections, Collapse all sections. `RightRail.tsx` drops the redundant `editor-rail-header` block and the `RuleSetIOBar`. `InspectorSurface.tsx` drops the standalone Expand/Collapse toolbar row. `.inspector-section-head` tightened to caption size with 6px vertical padding so section titles align with panel body left edge instead of feeling like a separate island. Properties panel search (`InspectorSearch`) already ships, no wiring needed. Pin advanced to v3.919.0.

## v3.915.0 - 2026-07-20

- Automated screenshot regression for Home: new `tests/e2e/home_visual_regression.py` captures the `main` hero, the workflow grid, and the utility strip at four viewports (375, 640, 900, 1440) with `reduced_motion="reduce"` and an 800ms post-networkidle settle so `clamp()`-driven paddings and font-load reflow stabilize before the shot. Twelve deterministic PNGs land in `tests/reports/home-regression/`. Extended `tests/e2e/diff_heatmaps.py` `SOURCES` to include the new directory: first run seeds baselines under `tests/reports/baselines/home-regression/`; later runs emit per-shot red-tinted heatmaps plus a side-by-side composite and fail the job when >20f pixels change (`THRESHOLD_RATIO = 0.02`, `PIXEL_DELTA = 12`). Wired both scripts into `.github/workflows/ci.yml` between `item_rows_long_labels.py` and the dev-server stop step, and added `tests/reports/home-regression/**` to the failure-artifact upload path so regressions are inspectable in the run summary. Verified end-to-end: seeded 12 baselines then re-ran capture+diff to confirm a clean 0-failure pass. This catches the two regressions the user hit twice this session (hero mt-hmi-5/mt-hmi-10 collapsing to 0, and object-cover stretching the reference photo) before they ship. Pin advanced to v3.915.0 in `README.md`.

## v3.914.0 - 2026-07-20

- Image aspect-ratio integrity pass: no user photo is stretched or cropped anywhere. Root cause: (1) `drawReference` in `src/lib/editor/render/frame.ts` called `ctx.drawImage(ref, x, y, IMAGE_BOUNDS.width, IMAGE_BOUNDS.height)`, forcing every uploaded/sample photo into the fixed 1280x720 editor viewport regardless of source ratio (this is the "stressed" PCB the user reported); (2) ten DOM image previews used `object-cover` which crops the frame on any non-matching ratio. Fix (canvas): switched `drawReference` to a contain-fit that scales the source by `Math.min(w/srcW, h/srcH)`, centers it inside `IMAGE_BOUNDS`, and letterboxes/pillarboxes the remainder with `tokens.panel`, so the photo keeps its native ratio and the coordinate space stays 1280x720 for rules. Fix (DOM): replaced `object-cover` with `object-contain bg-ca-panel` in `src/components/settings/ReferenceImageCard.tsx` (main preview, history thumbs, sample tiles), `src/components/projects/SampleCarousel.tsx` (chip + preview), `src/components/projects/ProjectEditorSections.tsx` (cover), `src/routes/projects.$projectId.ai-testing.tsx` (row thumb), `src/components/hmi/ViewportImageControls.tsx` (chip thumb), and `src/components/hmi/CameraPreview.tsx` (live view). `src/components/hmi/MachineFrame.tsx` was intentionally left as-is because it is a decorative background overlay (`opacity-70`), not a user photo. Pin advanced to v3.914.0 in `README.md`.

## v3.913.0 - 2026-07-20

- Top menu (home nav) hover stability + padding pass: eliminated hover jitter and normalized item padding across all `MenubarTrigger` items in `src/components/nav/TopMenuBar.tsx`. Root causes: (1) the trigger used `focus-visible:outline-2 focus-visible:outline-offset-2` which layered an outside outline that visually nudged neighboring items on keyboard focus transitions; (2) trigger height was `h-7` inside an `h-7` bar leaving no visual breathing room and encouraging perceived shift; (3) `overflow-x-auto` on the `Menubar` allowed a phantom horizontal scroll to appear mid-hover on borderline widths; (4) padding was inconsistent (`px-1.5` on nav triggers, `px-1.5` + `mr-1` icon spacing on Window trigger). Fix: standardized all triggers to `h-6 px-hmi-2 leading-none` with `gap-1` for the icon+label Window trigger, removed the offset outline in favor of the existing inset `hmi-focus-ring-inset` (no layout impact), swapped `overflow-x-auto` -> `overflow-hidden` on the bar, gave the bar `gap-0.5 p-0.5` for even inter-item rhythm, and pinned `transition-colors duration-150 ease-out` so the color swap on hover/open is smooth without touching box metrics. No component structure or behavior changed; only visual polish. Pin advanced to v3.913.0 in `README.md`.

## v3.912.0

The Home page now has a consistent vertical rhythm across screen sizes. The "Getting started" section header uses the same display typeface, weight, and casing as the workflow card titles, sized one tier below, so the baselines line up cleanly whether you are on a tight laptop or a wide monitor.

## v3.911.0

The Home page is now cleaner and less bulky. The huge visible title and extra descriptive sentence are gone, while the main button gets a proper gap before the workflow cards below it. The page still has a hidden accessibility heading, so screen readers keep a clear page label.

## v3.910.0

The **Rules** setup page is now hardened against mixed category rows. Even if the underlying shared library snapshot contains both kinds, `/setup/rules` filters categories out at the route boundary and only renders real rules. The editor side rail header now says **Rules** instead of **Rules and Categories**, so category management is consistently separate at `/setup/categories`.

## v3.909.0

The Setup area now separates **Rules** from **Categories**. Clicking Rules opens a rules-only list. Categories no longer appear as a tab or mixed section there. Use **Manage Categories** to open the dedicated category page, where you can view existing categories, create a new category, duplicate one, open it, rename it, edit notes, or delete it when allowed. Built-in categories stay locked.

## v3.908.0

Fixed a distracting UI bug where buttons and chips across the app (most visibly the Home "Create your first project" button and the Shortcuts / Density & theme / All projects chips) always showed a bright teal outline, as if they were stuck in a focus state. Now the outline only appears when you tab to an element with the keyboard, matching the rest of the app. Nothing else changed.

## v3.907.0

The **UI Craft** polish plan (30 steps) is now fully done and archived. Everything shipped in the prior releases (Home hero refresh, Projects card accents, Rulesets inline test-run, AI Testing 3-pane + skeleton, Properties HUD, motion tokens, focus ring, density toggle, `UI Craft Demo` seed profile, Playwright smoke, before/after doc) is unchanged; this release just closes the plan out. No app-visible changes.

## v3.906.0

Added a **before/after verification page** for the recent UI polish at `docs/verification/ui-craft-improvements.md`. It lists each polished surface (Home, Projects, Rulesets, AI Testing, Settings) with its baseline and post-improvement screenshot, the polish steps that landed on it, and a single command (`python3 tests/e2e/ui_craft_smoke.py`) to reproduce the whole walk. No changes to the app UI itself.

## v3.905.0

Added an end-to-end smoke test for the **UI Craft Demo** profile. Running `python3 tests/e2e/ui_craft_smoke.py` now applies the demo profile and walks Home, Projects, Rulesets, AI Testing, and Settings, saving a screenshot of each and failing loudly if any page crashes. The demo profile is also now listed in the command palette as "Seed: UI craft demo" so you can apply it without dev tools. No changes to the app UI itself.

## v3.904.0

A new **UI Craft Demo** seed profile is now available from the command palette (developer commands, "Apply seed: UI Craft Demo"). Applying it drops in one showcase project that uses every rule kind on a single canvas, two demo images (a golden and a defect variant), and a sample warning entry in Error History, so you can walk the recent UI polish (Home hero strip, project card accents, Properties HUD, AI Testing skeleton, motion tokens) end-to-end without hand-authoring rows. All existing profiles (Default PCB, SOIC, Connector, Blister, Empty, Error) are unchanged.

## v3.903.0

While the AI Testing page is finishing its first run, the Metrics and Timeline cards now show a pulsing skeleton with the live "Aggregating X/Y (Z
