# SDK Drops

This directory (`sdk/`) contains raw, unedited SDK drops from hardware vendors.

## Rules

- **Raw drops only, never edited**: Files in this directory must be committed exactly as they are received from the vendor (binary safe). Do not reformat, lint, or modify them in any way.
- See `BE/sdk_facade/` for the adapter layer that wraps these SDKs.
