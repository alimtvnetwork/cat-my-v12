%% Method: capture.frame.subscribe
%% Group schema: ../../schemas/ipc/capture.schema.json
%% Map row: ../../05-ui-to-backend-map.md (components/hmi/Viewport.tsx)
sequenceDiagram
    autonumber
    participant UI as components/hmi/Viewport.tsx
    participant IPC as pywebview IPC
    participant W as Python worker
    participant DB as SQLite / vendor
    UI->>IPC: capture.frame.subscribe req (mount(WS))
    IPC->>W: dispatch { cid, method, params }
    W->>DB: perform work
    DB-->>W: result or error
    W-->>IPC: res { cid, code, data }
    IPC-->>UI: I_* on success / E_* on failure
