%% Method: setup.roi.write
%% Group schema: ../../schemas/ipc/setup.schema.json
%% Map row: ../../05-ui-to-backend-map.md (routes/setup.roi.tsx)
sequenceDiagram
    autonumber
    participant UI as routes/setup.roi.tsx
    participant IPC as pywebview IPC
    participant W as Python worker
    participant DB as SQLite / vendor
    UI->>IPC: setup.roi.write req (save)
    IPC->>W: dispatch { cid, method, params }
    W->>DB: perform work
    DB-->>W: result or error
    W-->>IPC: res { cid, code, data }
    IPC-->>UI: I_* on success / E_* on failure
