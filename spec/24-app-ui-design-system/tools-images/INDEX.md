# Tools-images explainers index

Per Plan 40. One MD per screenshot in `assets/tools-images/` (50 files).
Explainers are written for an AI reader and follow the template in
`00-md-template.md`.

## Status

Scaffold-only as of v3.217.0. Only the template and this index are
present; per-image explainers (01-50) are NOT written yet. Writing them
requires viewing each screenshot; no explainer is fabricated from the
filename alone.

## Files

See `.lovable/plans/pending/40-tools-images-spec-docs.md` steps 1-50 for
the canonical filename-to-explainer mapping. Do not duplicate it here.

## Rules for future slices

- One explainer per slice batch (5-10 images) to keep review tractable.
- Every explainer MUST cite the source file path and use `00-md-template.md` verbatim.
- If the image cannot be viewed, mark the entry blocked in the plan file and do not invent content.
