"""
Vendor-specific hardware SDK adapter implementations.

These adapters implement the `CameraFacadeProtocol` interface but remain
strictly encapsulated in their respective directories so the Protocol layer
stays clean.
"""
